generator client {
  provider = "prisma-client-js"
  output   = "./generated/prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model add_rcloud_user_follow_record {
  id       Int     @id @default(autoincrement())
  type     Boolean
  t_id     Int
  admin_id Int
  user_id  Int
  title    String  @db.VarChar(255)
  content  String  @db.VarChar(255)
  ctime    Int
}

model cc_exclusivelog {
  id          Int       @id @default(autoincrement())
  appId       String    @db.VarChar(45)
  enableTime  DateTime  @default(now()) @db.Timestamp(0)
  status      Boolean   @default(true)
  disableTime DateTime? @db.Timestamp(0)

  @@unique([appId, status], map: "appId_status")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cl_test {
  orgId Int
  dau   Int

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dcp_industry {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  name        String   @default("") @db.VarChar(64)
  status      Int      @default(1) @db.TinyInt
  create_by   Int      @default(0) @db.UnsignedInt
  update_by   Int      @default(0) @db.UnsignedInt
  create_time DateTime @default(now()) @db.Timestamp(0)
  update_time DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dcp_private_customer {
  id              Int      @id @default(autoincrement()) @db.UnsignedInt
  customer_id     Int      @default(0) @db.UnsignedInt
  industry_id     Int      @default(0) @db.UnsignedInt
  user_id         Int      @default(0) @db.UnsignedInt
  name            String   @default("") @db.VarChar(32)
  sale_id         Int      @default(0) @db.UnsignedInt
  sale_name       String   @default("") @db.VarChar(32)
  sale_mobile     String   @default("") @db.VarChar(32)
  type            Int      @default(1) @db.UnsignedTinyInt
  remark          String   @default("") @db.VarChar(256)
  is_self_develop Int      @default(0) @db.UnsignedTinyInt
  status          Int      @default(1) @db.TinyInt
  create_by       Int      @default(0) @db.UnsignedInt
  update_by       Int      @default(0) @db.UnsignedInt
  create_time     DateTime @default(now()) @db.Timestamp(0)
  update_time     DateTime @default(now()) @db.Timestamp(0)
  config          String?  @db.Text

  @@index([sale_id], map: "idx_sale_id")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dcp_private_customer_archives {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  clue_id     Int     @default(0) @db.UnsignedInt
  create_time Int     @default(0)
  update_time Int     @default(0)
  is_del      Boolean @default(false)
  admin_id    Int     @default(0)
  remark      String? @db.Text
  files       String? @db.Text
  config      String  @default("") @db.VarChar(200)

  @@index([clue_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dcp_private_customer_clue {
  id                      Int      @id @default(autoincrement()) @db.UnsignedInt
  clue_id                 Int      @default(0) @db.UnsignedInt
  dcp_private_customer_id Int      @default(0) @db.UnsignedInt
  name                    String   @default("") @db.VarChar(256)
  sale_id                 Int      @default(0) @db.UnsignedInt
  sale_name               String   @default("") @db.VarChar(32)
  source                  Int      @default(1) @db.UnsignedTinyInt
  remark                  String   @default("") @db.VarChar(256)
  status                  Int      @default(1) @db.TinyInt
  create_by               Int      @default(0) @db.UnsignedInt
  update_by               Int      @default(0) @db.UnsignedInt
  create_time             DateTime @default(now()) @db.Timestamp(0)
  update_time             DateTime @default(now()) @db.Timestamp(0)
  config                  String?  @db.Text

  @@index([dcp_private_customer_id], map: "idx_customer_id")
  @@index([sale_id], map: "idx_sale_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dcp_private_customer_license {
  id                           Int      @id @default(autoincrement()) @db.UnsignedInt
  license_id                   Int      @default(0) @db.UnsignedInt
  dcp_private_customer_clue_id Int      @default(0) @db.UnsignedInt
  license_index                String   @default("") @db.VarChar(32)
  license                      String   @default("") @db.VarChar(256)
  effective_time               DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  expire_time                  String   @default("") @db.VarChar(64)
  sale_id                      Int      @default(0) @db.UnsignedInt
  sale_name                    String   @default("") @db.VarChar(32)
  remark                       String   @default("") @db.VarChar(256)
  active_state                 Int      @default(0) @db.TinyInt
  is_active                    Int      @default(0) @db.TinyInt
  state                        Int      @default(0) @db.UnsignedTinyInt
  status                       Int      @default(1) @db.UnsignedTinyInt
  env_type                     Int      @default(0) @db.TinyInt
  create_by                    Int      @default(0) @db.UnsignedInt
  update_by                    Int      @default(0) @db.UnsignedInt
  create_time                  DateTime @default(now()) @db.Timestamp(0)
  update_time                  DateTime @default(now()) @db.Timestamp(0)

  @@index([dcp_private_customer_clue_id], map: "idx_customer_clue_id")
  @@index([sale_id], map: "idx_sale_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dcp_private_customer_license_identify {
  id                              Int      @id @default(autoincrement()) @db.UnsignedInt
  dcp_private_customer_license_id Int      @default(0) @db.UnsignedInt
  identify_id                     Int      @default(0)
  identify                        String   @default("") @db.VarChar(256)
  effective_time                  DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  expire_time                     String   @default("") @db.VarChar(64)
  remark                          String   @db.LongText
  product_type                    Int      @default(0) @db.TinyInt
  state                           Int      @default(0) @db.UnsignedTinyInt
  status                          Int      @default(1) @db.TinyInt
  create_by                       Int      @default(0) @db.UnsignedInt
  update_by                       Int      @default(0) @db.UnsignedInt
  create_time                     DateTime @default(now()) @db.Timestamp(0)
  update_time                     DateTime @default(now()) @db.Timestamp(0)

  @@index([identify_id], map: "idx_identify_id")
  @@index([dcp_private_customer_license_id], map: "idx_license_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dm_mobile {
  id           Int    @id @default(autoincrement())
  MobileNumber String @db.VarChar(20)
  MobileArea   String @db.VarChar(50)
}

model dw_name_mapping {
  id   Int     @id
  name String? @db.VarChar(45)
}

model liebao_cc_exclusivelog {
  id          Int       @id @default(autoincrement())
  appId       String    @db.VarChar(45)
  enableTime  DateTime  @default(now()) @db.Timestamp(0)
  status      Boolean   @default(true)
  disableTime DateTime? @db.Timestamp(0)

  @@unique([appId, status], map: "appId_status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mp_menu {
  id          Int       @id @default(autoincrement())
  user_id     Int
  content     String?   @db.Text
  create_time DateTime? @default(now()) @db.Timestamp(0)
  ison        Int       @default(0) @db.TinyInt
  mp_content  String?   @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_activity_prize {
  id            Int      @id @default(autoincrement())
  activity_code String   @db.VarChar(50)
  code          String   @db.VarChar(50)
  title         String   @db.VarChar(50)
  grade         String   @db.VarChar(20)
  default_flag  Int      @default(0)
  create_time   DateTime @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_activity_product_address {
  id            Int      @id @default(autoincrement())
  activity_code String   @db.VarChar(50)
  user_id       Int?
  name          String   @db.VarChar(50)
  mobile        String   @db.VarChar(11)
  city          String   @db.VarChar(100)
  address       String   @db.VarChar(100)
  remark        String?  @db.VarChar(255)
  create_time   DateTime @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_activity_user {
  id             Int      @id @default(autoincrement())
  activity_code  String   @db.VarChar(50)
  user_id        Int
  mobile         String   @db.VarChar(11)
  draw_number    Int      @default(0)
  activity_start DateTime @db.DateTime(0)
  activity_end   DateTime @db.DateTime(0)
  create_time    DateTime @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_activity_user_prize_ref {
  id            Int      @id @default(autoincrement())
  activity_code String   @db.VarChar(50)
  user_id       Int?
  mobile        String   @db.VarChar(11)
  prize_code    String   @db.VarChar(50)
  create_time   DateTime @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_activity_winner_history {
  id            Int      @id @default(autoincrement())
  activity_code String   @db.VarChar(50)
  user_id       Int
  mobile        String   @db.VarChar(11)
  prize_code    String   @db.VarChar(50)
  prize_name    String?  @db.VarChar(100)
  create_time   DateTime @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_alert_contact {
  id           Int     @id @default(autoincrement())
  name         String  @db.VarChar(10)
  org_id       Int
  country_code String  @default("86") @db.VarChar(10)
  phone_no     String  @db.VarChar(32)
  email        String  @db.VarChar(50)
  deleted      Boolean @default(false)
  create_by    String? @db.VarChar(32)
  create_at    BigInt
  update_at    BigInt?

  @@index([name], map: "popc_alert_contact_name_index")
  @@index([org_id, deleted], map: "popc_alert_contact_org_id_deleted_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_alert_notification {
  id        Int       @id @default(autoincrement())
  log_id    String    @db.VarChar(110)
  app_id    String    @db.VarChar(10)
  channel   String    @db.VarChar(10)
  status    String    @default("INITIAL") @db.VarChar(10)
  content   String    @db.Text
  response  String?   @db.Text
  create_at DateTime? @default(now()) @db.DateTime(0)

  @@index([app_id, log_id], map: "popc_alert_notification_app_id_log_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_alert_record {
  id             Int     @id @default(autoincrement())
  trace_id       String  @db.VarChar(32)
  title          String  @db.VarChar(88)
  contact_name   String  @db.VarChar(10)
  org_id         Int     @default(0)
  app_id         Int?
  data_center    String? @db.VarChar(10)
  rule_id        Int
  metric_type    String  @db.VarChar(50)
  metric_value   String? @db.VarChar(32)
  alert_status   String  @db.VarChar(10)
  threshold      Int
  percentage     Float?
  is_mute        Boolean @default(false)
  notify_channel String  @db.VarChar(32)
  trigger_time   BigInt
  send_time      BigInt
  log_id         String? @db.VarChar(110)
  extra_info     String? @db.MediumText
  create_at      BigInt

  @@index([create_at, app_id], map: "popc_alert_record_create_at_app_id_index")
  @@index([trigger_time], map: "rcloud_alert_record_trigger_time_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_alert_rule {
  id                   Int     @id @default(autoincrement())
  name                 String  @db.VarChar(32)
  org_id               Int
  app_id               Int
  biz_type             String  @db.VarChar(15)
  metric_type          String  @db.VarChar(50)
  metric_config        String  @db.MediumText
  notify_channels      String  @db.VarChar(32)
  alert_interval       Int
  disabled             Boolean @default(false)
  enabled_days_of_week String  @default("[1,2,3,4,5,6,7]") @db.VarChar(15)
  enabled_stime        String  @db.VarChar(8)
  enabled_etime        String  @db.VarChar(8)
  deleted              Boolean @default(false)
  contact_id           Int
  contact_name         String  @db.VarChar(10)
  webhook_id           Int?
  create_by            String  @db.VarChar(32)
  create_at            BigInt
  update_at            BigInt?

  @@index([contact_id], map: "popc_alert_rule_contact_id_index")
  @@index([name], map: "popc_alert_rule_name_index")
  @@index([app_id, biz_type, deleted, disabled, metric_type], map: "popc_join_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_alert_rule_2 {
  id                   Int     @id @default(autoincrement())
  name                 String  @db.VarChar(32)
  org_id               Int
  app_id               Int
  metric_type          String  @db.VarChar(10)
  metric_config        String  @db.MediumText
  notify_channels      String  @db.VarChar(32)
  alert_interval       Int
  disabled             Boolean @default(false)
  enabled_days_of_week String  @default("[1,2,3,4,5,6,7]") @db.VarChar(15)
  enabled_stime        String  @db.VarChar(8)
  enabled_etime        String  @db.VarChar(8)
  deleted              Boolean @default(false)
  contact_id           Int
  contact_name         String  @db.VarChar(10)
  create_by            String  @db.VarChar(32)
  create_at            BigInt
  update_at            BigInt?

  @@index([contact_id], map: "popc_alert_rule_contact_id_index")
  @@index([name], map: "popc_alert_rule_name_index")
  @@index([app_id, metric_type, deleted, disabled], map: "popc_join_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_alert_webhook {
  id          BigInt    @id @default(autoincrement())
  type        String    @db.VarChar(10)
  webhook_url String    @db.VarChar(255)
  status      Int       @default(1) @db.TinyInt
  create_at   DateTime? @default(now()) @db.DateTime(0)
  update_at   DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_api_freq {
  id                 Int       @id @default(autoincrement())
  api                String    @db.VarChar(255)
  api_name           String    @db.VarChar(50)
  default_frequency  Int
  unit_frequency     Int       @default(0)
  service_id         Int       @default(0)
  oversea_service_id Int       @default(0)
  time_unit          String    @db.VarChar(10)
  tunable            Boolean   @default(false)
  frequency_desc     String?   @db.VarChar(100)
  create_at          DateTime  @default(now()) @db.DateTime(0)
  update_at          DateTime? @db.DateTime(0)

  @@index([service_id], map: "popc_api_freq_service_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_api_freq_bak {
  id                Int       @id @default(autoincrement())
  api               String    @db.VarChar(255)
  api_name          String    @db.VarChar(50)
  default_frequency Int
  unit_frequency    Int       @default(0)
  service_id        Int       @default(0)
  time_unit         String    @db.VarChar(10)
  tunable           Boolean   @default(false)
  frequency_desc    String?   @db.VarChar(100)
  create_at         DateTime  @default(now()) @db.DateTime(0)
  update_at         DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_app_migrate {
  id           Int      @id @default(autoincrement())
  old_org_id   Int      @default(0)
  new_org_id   Int      @default(0)
  app_id       Int      @default(0)
  database     String   @default("") @db.VarChar(100)
  table        String   @default("") @db.VarChar(100)
  update_sql   String   @default("") @db.VarChar(500)
  count_sql    String   @default("") @db.VarChar(500)
  max_id_sql   String   @default("") @db.VarChar(500)
  update_count Int      @default(0)
  expect_count Int      @default(0)
  max_id       Int      @default(0)
  create_time  DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_common_activity {
  id            BigInt  @id @default(autoincrement())
  code          String? @default("") @db.VarChar(100)
  name          String  @default("") @db.VarChar(100)
  description   String  @default("") @db.VarChar(500)
  config        Json?
  start_date    Int     @default(0)
  end_date      Int     @default(0)
  status        Int     @default(0) @db.TinyInt
  creation_time BigInt  @default(0)
  update_time   BigInt  @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_common_activity_user {
  id            BigInt  @id @default(autoincrement())
  org_id        BigInt  @default(0)
  user_id       BigInt  @default(0)
  app_id        BigInt  @default(0)
  item_id       BigInt? @default(0)
  activity_id   BigInt  @default(0)
  config        Json?
  status        Int     @default(0) @db.TinyInt
  remark        String  @default("") @db.VarChar(500)
  creation_time BigInt  @default(0)
  update_time   BigInt  @default(0)

  @@index([org_id], map: "IDX_org_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_config {
  id            BigInt  @id @default(autoincrement())
  org_id        BigInt  @default(0)
  app_id        BigInt  @default(0)
  key           String  @db.VarChar(63)
  val           String  @db.VarChar(255)
  status        Int     @default(1) @db.TinyInt
  description   String? @db.VarChar(127)
  source        Int     @db.TinyInt
  creation_time BigInt
  update_time   BigInt

  @@index([org_id, key], map: "popc_conf_org")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_confirm_popup {
  id            BigInt @id @default(autoincrement())
  org_id        BigInt @default(0)
  user_id       BigInt @default(0)
  app_id        BigInt @default(0)
  type          String @default("") @db.VarChar(255)
  version       Int    @default(0)
  value         String @default("") @db.VarChar(255)
  creation_time BigInt
  update_time   BigInt

  @@index([org_id], map: "IDX_org_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_error_code {
  id         Int      @id @default(autoincrement())
  error_code Int
  code_type  String   @db.VarChar(10)
  message_cn String   @db.VarChar(100)
  message_en String   @db.VarChar(110)
  detail_cn  String   @db.Text
  detail_en  String   @db.Text
  create_at  DateTime @default(now()) @db.DateTime(0)

  @@index([code_type], map: "popc_error_code_code_type_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_imrobot_config {
  id             BigInt  @id @default(autoincrement())
  app_id         BigInt
  supplier_id    BigInt
  config_json    Json
  secret_id      BigInt
  model_id       BigInt
  model          String  @db.VarChar(64)
  name           String  @db.VarChar(64)
  im_user_id     String  @db.VarChar(64)
  profile_photo  String  @db.VarChar(1024)
  status         Int?    @db.TinyInt
  last_user_id   BigInt
  description    String? @db.VarChar(255)
  creation_time  BigInt
  update_time    BigInt
  context_length Int     @default(0) @db.TinyInt
  data_center    String  @db.VarChar(16)

  @@index([app_id], map: "imrobot_app")
  @@index([supplier_id], map: "imrobot_supplier")
  @@index([update_time], map: "imrobot_update_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_imrobot_model {
  id               BigInt  @id @default(autoincrement())
  code             String  @db.VarChar(32)
  supplier_id      BigInt
  model            String  @db.VarChar(64)
  online_status    Int     @default(1) @db.TinyInt
  available_status Int     @default(1) @db.TinyInt
  description      String? @db.VarChar(255)
  creation_time    BigInt
  update_time      BigInt

  @@index([supplier_id], map: "imrobot_supplier")
  @@index([update_time], map: "imrobot_update_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_imrobot_secret {
  id            BigInt  @id @default(autoincrement())
  supplier_id   BigInt
  config_json   Json
  app_id        BigInt
  status        Int?    @db.TinyInt
  last_user_id  BigInt
  description   String? @db.VarChar(255)
  creation_time BigInt
  update_time   BigInt
  data_center   String  @db.VarChar(16)

  @@index([app_id], map: "imrobot_app")
  @@index([supplier_id], map: "imrobot_supplier")
  @@index([update_time], map: "imrobot_update_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_imrobot_supplier {
  id               BigInt  @id @default(autoincrement())
  code             String  @db.VarChar(32)
  name_zh          String  @default("") @db.VarChar(32)
  name_en          String  @default("") @db.VarChar(32)
  scope            String  @default("all") @db.VarChar(32)
  online_status    Int     @default(1) @db.TinyInt
  available_status Int     @default(1) @db.TinyInt
  description      String? @db.VarChar(255)
  creation_time    BigInt
  update_time      BigInt

  @@index([update_time], map: "imrobot_update_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_menu {
  id            BigInt  @id @default(autoincrement())
  name_zh       String  @default("") @db.VarChar(31)
  name_en       String  @default("") @db.VarChar(31)
  icon          String? @default("") @db.VarChar(31)
  p_id          BigInt
  code          String? @unique(map: "menu_code") @db.VarChar(31)
  description   String? @default("") @db.VarChar(2048)
  l_path        String? @default("") @db.VarChar(255)
  doc           String? @default("") @db.VarChar(255)
  p_path        String  @default("") @db.VarChar(127)
  type          String  @db.VarChar(15)
  status        Int     @default(1) @db.TinyInt
  weigh         Int     @default(0)
  summary       String? @db.VarChar(2048)
  sub_function  String? @db.LongText
  creation_time BigInt

  @@index([p_path], map: "menu_path")
  @@index([p_id], map: "menu_pid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_menu_20231114 {
  id            BigInt  @id @default(autoincrement())
  name_zh       String  @default("") @db.VarChar(31)
  name_en       String  @default("") @db.VarChar(31)
  icon          String? @default("") @db.VarChar(31)
  p_id          BigInt
  code          String? @unique(map: "menu_code") @db.VarChar(31)
  description   String? @default("") @db.VarChar(511)
  l_path        String? @default("") @db.VarChar(255)
  doc           String? @default("") @db.VarChar(255)
  p_path        String  @default("") @db.VarChar(127)
  type          String  @db.VarChar(15)
  status        Int     @default(1) @db.TinyInt
  weigh         Int     @default(0)
  creation_time BigInt

  @@index([p_path], map: "menu_path")
  @@index([p_id], map: "menu_pid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_menu_function {
  id          BigInt  @id @default(autoincrement())
  name_zh     String  @default("") @db.VarChar(100)
  menu_code   String  @db.VarChar(31)
  summary     String? @default("") @db.VarChar(2048)
  status      Int     @default(1) @db.TinyInt
  create_time BigInt

  @@index([menu_code], map: "fk_menu_code")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_menu_url {
  id            BigInt @id @default(autoincrement())
  menu_id       BigInt
  service       String @db.VarChar(31)
  method        String @db.VarChar(15)
  url           String @db.VarChar(255)
  creation_time BigInt
  status        Int    @default(1) @db.TinyInt

  @@index([menu_id], map: "popc_m_u_menu")
  @@index([url], map: "popc_m_u_url")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_email_history {
  id           Int      @id @default(autoincrement())
  task_id      Int
  user_id      BigInt
  account_type Int
  account_id   BigInt
  email        String   @default("") @db.VarChar(512)
  title        String   @default("") @db.VarChar(255)
  content      String   @db.LongText
  created      DateTime @default(now()) @db.Timestamp(0)

  @@index([account_id], map: "ix_account_id")
  @@index([task_id], map: "ix_task_id")
  @@index([user_id], map: "ix_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_inbox {
  id           Int                    @id @default(autoincrement())
  task_id      Int
  user_id      BigInt
  account_type Int
  account_id   BigInt
  type         popc_notice_inbox_type
  title        String                 @default("") @db.VarChar(255)
  content      String                 @db.LongText
  read_status  Int                    @default(0)
  read_time    DateTime?              @db.Timestamp(0)
  created      DateTime               @default(now()) @db.Timestamp(0)
  is_del       Int                    @default(0)

  @@index([account_id], map: "ix_account_id")
  @@index([task_id], map: "ix_task_id")
  @@index([user_id], map: "ix_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_send_direct {
  id          Int      @id @default(autoincrement())
  template_id Int
  param       String?  @db.LongText
  range       String   @db.LongText
  send_mode   String   @db.VarChar(16)
  remarks     String?  @db.VarChar(255)
  status      Int
  exec_time   DateTime @default(now()) @db.Timestamp(0)
  task_id     Int
  operator    String   @db.VarChar(255)
  created     DateTime @default(now()) @db.Timestamp(0)

  @@index([task_id], map: "ix_task_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_send_history {
  id           Int      @id @default(autoincrement())
  task_id      Int?
  user_id      BigInt?
  app_id       BigInt?
  account_type Int
  account_id   BigInt
  mode         Int
  target       String?  @db.VarChar(512)
  title        String   @default("") @db.VarChar(255)
  content      String   @db.LongText
  result       String?  @db.Text
  created      DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_subscription {
  id            Int                                     @id @default(autoincrement())
  template_type popc_notice_subscription_template_type?
  template_id   Int?
  send_mode     String                                  @db.VarChar(16)
  user_id       BigInt
  account_type  Int
  account_id    BigInt
  status        Int
  operator      String?                                 @default("") @db.VarChar(255)
  updated       DateTime                                @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_task {
  id               Int       @id @default(autoincrement())
  template_id      Int
  template_code    String    @db.VarChar(255)
  template_version Int?
  source           String    @db.VarChar(255)
  remarks          String?   @db.VarChar(255)
  send_mode        String    @db.VarChar(16)
  detail_count     Int?
  status           Int
  created          DateTime  @default(now()) @db.Timestamp(0)
  start_time       DateTime? @db.Timestamp(0)
  end_time         DateTime? @db.Timestamp(0)
  execution_time   BigInt?   @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_task_details {
  id      Int      @id @default(autoincrement())
  task_id Int
  user_id Int?
  app_id  Int?
  params  String?  @db.LongText
  status  Int      @default(0)
  created DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_template {
  id             Int                       @id @default(autoincrement())
  code           String                    @db.VarChar(128)
  name           String                    @db.VarChar(255)
  type           popc_notice_template_type
  title          String?                   @db.VarChar(255)
  inbox_content  String                    @db.LongText
  email_content  String                    @db.LongText
  sms_content    String                    @db.LongText
  use_frame      Int                       @default(0)
  version        Int
  status         Int
  desc           String?                   @db.VarChar(512)
  operator       String                    @db.VarChar(255)
  last_send_time DateTime?                 @db.Timestamp(0)
  updated        DateTime                  @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_template_frame {
  id       Int      @id @default(autoincrement())
  type     String   @db.VarChar(24)
  content  String   @db.LongText
  operator String   @db.VarChar(255)
  updated  DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_notice_template_version {
  id               Int      @id @default(autoincrement())
  template_id      Int
  template_version Int
  template_obj     String   @db.LongText
  operator         String   @db.VarChar(255)
  created          DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_oe_clue {
  id                BigInt   @id @default(autoincrement())
  clue_id           BigInt   @unique(map: "oe_clue_id")
  data_type         String   @db.VarChar(7)
  data              String   @db.VarChar(2048)
  sync_freq         Int      @default(0) @db.TinyInt
  clue_create_time  BigInt
  clue_request_time BigInt
  status            Int      @default(0) @db.TinyInt
  fxxk_data_id      String   @default("") @db.VarChar(31)
  create_time       DateTime @default(now()) @db.Timestamp(0)
  update_time       DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_org_role {
  id            BigInt @id @default(autoincrement())
  role_id       BigInt
  org_id        BigInt @default(0)
  app_id        BigInt @default(0)
  creation_time BigInt
  status        Int    @default(1) @db.TinyInt

  @@unique([org_id, app_id, role_id], map: "popc_r_a_unique")
  @@index([app_id], map: "popc_r_a_app")
  @@index([org_id], map: "popc_r_a_org")
  @@index([role_id], map: "popc_r_a_role")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_org_whitelist {
  id            BigInt @id @default(autoincrement())
  org_id        BigInt @unique(map: "popc_r_a_org")
  creation_time BigInt
  status        Int    @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_rcuser_app {
  id            BigInt  @id @default(autoincrement())
  org_id        BigInt
  user_id       BigInt
  app_id        BigInt
  status        Int     @default(1) @db.TinyInt
  creation_time BigInt
  update_time   BigInt?

  @@index([app_id], map: "popc_u_a_app")
  @@index([org_id], map: "popc_u_a_org")
  @@index([user_id], map: "popc_u_a_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_rcuser_master {
  id              BigInt  @id @default(autoincrement())
  name            String  @default("") @db.VarChar(127)
  phone           String  @db.VarChar(30)
  email           String  @default("") @db.VarChar(127)
  status          Int     @default(1) @db.TinyInt
  creation_time   BigInt
  org_id          BigInt
  last_app_id     BigInt?
  last_time       BigInt?
  password        String  @default("") @db.VarChar(511)
  country_code    String  @default("86") @db.VarChar(7)
  source          String  @default("") @db.VarChar(15)
  update_pwd_time BigInt?

  @@index([email], map: "rcuser_m_email")
  @@index([org_id], map: "rcuser_m_org")
  @@index([phone], map: "rcuser_m_phone")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_rcuser_slave {
  id              BigInt  @id @default(autoincrement())
  name            String  @default("") @db.VarChar(127)
  org_id          BigInt
  email           String  @default("") @db.VarChar(127)
  phone           String? @default("") @db.VarChar(15)
  status          Int     @default(0) @db.TinyInt
  invite_time     BigInt?
  creation_time   BigInt
  last_app_id     BigInt?
  last_time       BigInt?
  password        String? @default("") @db.VarChar(511)
  country_code    String? @default("") @db.VarChar(7)
  update_pwd_time BigInt?

  @@index([email], map: "rcuser_s_email")
  @@index([org_id], map: "rcuser_s_org")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_recordation {
  id            BigInt @id @default(autoincrement())
  org_id        BigInt @default(0)
  last_user_id  BigInt @default(0)
  app_id        BigInt @default(0)
  type          Int    @default(0) @db.TinyInt
  number        String @default("") @db.VarChar(255)
  status        Int    @default(0) @db.TinyInt
  creation_time BigInt
  update_time   BigInt

  @@index([app_id], map: "IDX_app_id")
  @@index([org_id], map: "IDX_org_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_referral_relation {
  id               BigInt    @id @default(autoincrement())
  referrer_id      BigInt
  referrer_org_id  BigInt
  referred_user_id BigInt
  referred_org_id  BigInt
  status           Int       @default(1)
  note             String?   @db.VarChar(110)
  expire_at        DateTime  @db.DateTime(0)
  create_at        DateTime  @default(now()) @db.DateTime(0)
  update_at        DateTime? @db.DateTime(0)

  @@unique([referrer_id, referred_user_id], map: "popc_referral_relation_pk")
  @@unique([referred_org_id, referred_user_id], map: "popc_referral_relation_referred_org_id_referred_user_id_uindex")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_referral_transaction {
  id               BigInt    @id @default(autoincrement())
  referrer_id      BigInt
  referrer_org_id  BigInt
  referred_user_id BigInt
  referred_org_id  BigInt
  money            Decimal?  @default(0.00) @db.Decimal(10, 2)
  refund           Decimal   @default(0.00) @db.Decimal(10, 2)
  transaction_at   DateTime? @db.DateTime(0)
  effect_at        DateTime? @db.DateTime(0)
  type             Int       @default(1)
  status           Int
  note             String?   @db.VarChar(110)
  create_at        DateTime  @default(now()) @db.DateTime(0)
  update_at        DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_referrer {
  id        BigInt    @id @default(autoincrement())
  user_id   BigInt?
  org_id    BigInt
  code      String    @unique(map: "popc_referrer_pk") @db.VarChar(10)
  status    Int       @default(1)
  note      String?   @db.VarChar(110)
  create_at DateTime  @default(now()) @db.DateTime(0)
  expire_at DateTime  @db.DateTime(0)
  update_at DateTime? @db.DateTime(0)

  @@unique([org_id, user_id], map: "popc_referrer_pk_2")
  @@index([user_id, status], map: "popc_referrer_user_id_status_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_referrer_account {
  id            BigInt    @id @default(autoincrement())
  referrer_id   BigInt
  account_no    String    @db.VarChar(50)
  account_type  Int?
  entity        String    @db.VarChar(110)
  entity_branch String    @db.VarChar(110)
  deleted       Int       @default(0)
  create_at     DateTime  @default(now()) @db.DateTime(0)
  update_at     DateTime? @db.DateTime(0)

  @@index([referrer_id, account_type, deleted], map: "withdraw_filter_idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_referrer_reward {
  id              BigInt    @id @default(autoincrement())
  referrer_id     BigInt
  referred_count  Int
  referred_money  Decimal   @db.Decimal(10, 2)
  reward_pct      Int
  pretax_reward   Decimal   @db.Decimal(10, 2)
  aftertax_reward Decimal   @db.Decimal(10, 2)
  billing_month   Int
  status          Int       @default(0)
  note            String?   @db.VarChar(110)
  internal_note   String?   @db.VarChar(110)
  apply_at        DateTime? @db.DateTime(0)
  create_at       DateTime  @default(now()) @db.DateTime(0)
  update_at       DateTime? @db.DateTime(0)

  @@unique([referrer_id, billing_month], map: "popc_referrer_reward_referrer_id_billing_month_uindex")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_replenish_menu {
  id            BigInt  @id @default(autoincrement())
  org_role_id   BigInt  @default(0)
  app_role_id   BigInt  @default(0)
  user_role_id  BigInt
  menu_id       BigInt
  creation_time BigInt
  type          Int     @default(1) @db.TinyInt
  status        Int     @default(1) @db.TinyInt
  description   String? @db.VarChar(127)
  prompt_zh     String? @db.VarChar(255)
  prompt_en     String? @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_role {
  id            BigInt  @id @default(autoincrement())
  name          String  @default("") @db.VarChar(31)
  code          String  @db.VarChar(31)
  type          String  @db.VarChar(15)
  org_id        BigInt  @default(0)
  creation_time BigInt
  description   String? @default("") @db.VarChar(63)
  status        Int     @default(1) @db.TinyInt

  @@index([org_id, type, code], map: "role_org_code")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_role_menu {
  id            BigInt  @id @default(autoincrement())
  role_id       BigInt
  menu_id       BigInt
  exclude       Boolean @default(false)
  creation_time BigInt  @default(0)
  status        Int     @default(1) @db.TinyInt

  @@index([menu_id], map: "popc_r_m_menu")
  @@index([role_id], map: "popc_r_m_role")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_search_history {
  id        Int      @id @default(autoincrement())
  query     String   @db.Text
  module    String?  @db.VarChar(20)
  hits      Int      @default(0)
  user_id   String   @db.VarChar(100)
  create_at DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model popc_user_role {
  id            BigInt  @id @default(autoincrement())
  org_id        BigInt
  user_id       BigInt
  role_id       BigInt
  creation_time BigInt  @default(0)
  status        Int     @default(1) @db.TinyInt
  update_time   BigInt?

  @@index([org_id], map: "popc_u_r_org")
  @@index([role_id], map: "popc_u_r_role")
  @@index([user_id], map: "popc_u_r_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_access_log {
  id          Int     @id @default(autoincrement())
  url         String  @db.VarChar(255)
  date        Int
  method      String  @db.VarChar(10)
  create_time Int     @default(0)
  params      String? @db.VarChar(255)
  site        String  @default("developer") @db.VarChar(255)
  user_id     Int

  @@index([date], map: "date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_access_log_bak20220525 {
  id          Int     @id @default(autoincrement())
  url         String  @db.VarChar(255)
  date        Int
  method      String  @db.VarChar(10)
  create_time Int     @default(0)
  params      String? @db.VarChar(255)
  site        String  @default("developer") @db.VarChar(255)
  user_id     Int

  @@index([date], map: "date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_active_apply {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  com_name      String  @default("") @db.VarChar(255)
  user_name     String  @default("") @db.VarChar(255)
  mobile        String  @default("") @db.VarChar(255)
  product_name  String  @default("") @db.VarChar(255)
  audit         Boolean @default(false)
  is_registered Boolean @default(false)
  email         String  @default("") @db.VarChar(255)
  app_key       String  @default("") @db.VarChar(255)
  test_app_key  String  @default("") @db.VarChar(255)
  create_time   Int     @default(0) @db.UnsignedInt
  type          Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_active_source {
  id          Int      @id @default(autoincrement())
  active_type Int      @default(1)
  sourceLink  String   @default("") @db.VarChar(255)
  sourceKey   String   @default("") @db.VarChar(255)
  return_url  String   @default("") @db.VarChar(255)
  coupon_type String   @default("[]") @db.VarChar(255)
  start       DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  end         DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model rcloud_activity_blog {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  blog_id    Int
  pic        String   @db.VarChar(64)
  admin      Int
  url        String   @db.VarChar(64)
  status     Int      @default(1) @db.TinyInt
  name       String   @db.VarChar(64)
  createtime DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_activity_fifteen_free_apply {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int @default(0)
  app_id      Int @default(0)
  server      Int @default(0)
  is_del      Int @default(2) @db.TinyInt
  create_by   Int @default(0)
  update_by   Int @default(0)
  create_time Int @default(0)
  update_time Int @default(0)

  @@index([app_id], map: "idx_app_id")
  @@index([user_id], map: "idx_user_id")
}

model rcloud_activity_shumei_account {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int    @default(0) @db.UnsignedInt
  app_key      String @default("") @db.VarChar(255)
  access_key   String @default("") @db.VarChar(255)
  organization String @default("") @db.VarChar(255)
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_activity_statistic {
  id          Int      @id @default(autoincrement())
  type        Int      @default(0) @db.TinyInt
  subtype     String   @default("") @db.VarChar(50)
  user_id     Int      @default(0)
  service     Int      @default(0)
  app_id      Int      @default(0)
  date        Int      @default(0)
  ip          String   @default("") @db.VarChar(100)
  value       Int      @default(1)
  data        String   @default("") @db.VarChar(5000)
  create_time DateTime @default(now()) @db.Timestamp(0)
  update_time DateTime @default(now()) @db.Timestamp(0)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_app_review {
  id       Int    @id @default(autoincrement()) @db.UnsignedInt
  appid    Int    @db.UnsignedMediumInt
  admin_id Int    @db.MediumInt
  optlog   String @default("") @db.VarChar(255)
  reason   String @db.VarChar(255)
  dateline Int
}

model rcloud_admin_email {
  id        Int     @id @default(autoincrement()) @db.UnsignedInt
  emailtype String  @db.VarChar(16)
  usertype  Int     @db.UnsignedTinyInt
  email     String? @db.VarChar(128)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_email_template {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String  @default("") @db.VarChar(100)
  title          String  @default("") @db.VarChar(200)
  content        String? @db.Text
  create_time    Int     @default(0)
  create_user_id Int     @default(0)
  type           Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_exercise {
  id             Int    @id @default(autoincrement())
  title          String @default("") @db.VarChar(255)
  stop_time      Int    @default(0)
  create_time    Int    @default(0)
  create_user_id Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_forbidden_log {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int    @db.UnsignedInt
  admin_id   Int
  admin_user String @db.VarChar(30)
  action     String @db.VarChar(30)
  reason     String @db.Text
  dateline   Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_mail_template {
  id          Int    @id
  title       String @default("") @db.VarChar(200)
  name        String @default("") @db.VarChar(45)
  content     String @db.Text
  create_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_mailcron {
  id       Int     @id @default(autoincrement()) @db.MediumInt
  admin_id Int     @db.MediumInt
  email    String  @default("") @db.VarChar(40)
  subject  String  @default("") @db.VarChar(100)
  message  String  @db.Text
  addtime  Int
  sendtime Int
  status   Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_message_template {
  id          Int    @id
  title       String @default("") @db.VarChar(200)
  name        String @default("") @db.VarChar(45)
  content     String @db.Text
  create_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_optlog {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  admin_id Int     @db.UnsignedMediumInt
  action   String  @db.VarChar(30)
  url      String  @db.VarChar(255)
  oplog    String  @db.LongText
  ip       String  @default("127.0.0.1") @db.Char(15)
  dateline Int
  new_data String? @db.Text
  old_data String? @db.Text

  @@index([url], map: "index_url")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_permission {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  name         String @default("") @db.VarChar(100)
  operate_name String @default("") @db.VarChar(256)
  controller   String @default("") @db.VarChar(100)
  url          String @default("") @db.VarChar(100)
  acl          String @default("") @db.VarChar(100)
  pid          Int    @default(0)
  type         Int    @default(1) @db.TinyInt
  icon         String @default("") @db.VarChar(256)
  sort         Int    @default(1) @db.TinyInt
  status       Int    @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_review_log {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id     Int     @db.UnsignedMediumInt
  admin_id   Int     @db.MediumInt
  action     String  @default("") @db.VarChar(30)
  reason     String  @db.VarChar(255)
  dateline   Int
  old_status Boolean
  rid        Int     @default(0)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_role {
  id          Int      @id @default(autoincrement()) @db.UnsignedSmallInt
  role_name   String   @db.VarChar(20)
  acl         String?  @db.Text
  status      Boolean? @default(true)
  create_time Int      @db.UnsignedInt
  update_time Int      @db.UnsignedInt
  is_onduty   Boolean? @default(true)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_sdk {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  android_version String  @default("") @db.VarChar(30)
  ios_version     String  @default("") @db.VarChar(30)
  android_pack    String  @default("") @db.VarChar(200)
  ios_pack        String  @default("") @db.VarChar(200)
  create_user_id  Int     @default(0)
  create_time     Int     @default(0)
  status          Boolean @default(false)
  platform        Boolean @default(true)
  version         String  @default("") @db.VarChar(30)
  version_type    Int     @default(3) @db.TinyInt
  version_log_url String  @default("") @db.VarChar(200)
  release_time    Int     @default(0)
  demo_url        String  @default("") @db.VarChar(200)
  demo2_url       String? @default("") @db.VarChar(200)
  is_voip         Boolean @default(false)
  cordova_url     String  @default("") @db.VarChar(200)
  platformtype    String? @db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_sdk_demo {
  id             Int     @id @default(autoincrement())
  platform       Int     @default(0) @db.TinyInt
  name           String  @default("") @db.VarChar(64)
  url            String  @default("") @db.VarChar(255)
  demo_url       String  @default("") @db.VarChar(255)
  create_user_id Int     @default(0)
  status         Int     @default(0) @db.TinyInt
  ctime          Int     @default(0)
  utime          Int     @default(0)
  page           String? @default("") @db.VarChar(20)
  sort           Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_sdk_manage {
  id     Int     @id @default(autoincrement())
  name   String  @default("") @db.VarChar(64)
  alias  String  @default("") @db.VarChar(64)
  url    String  @default("") @db.VarChar(255)
  type   Boolean @default(true)
  status Boolean @default(false)
  ctime  Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_sdk_new {
  id               Int     @id @default(autoincrement())
  platform         Int     @default(0) @db.TinyInt
  version_type     Int     @default(0) @db.TinyInt
  charge           Int     @default(3) @db.TinyInt
  version          String  @default("") @db.VarChar(16)
  version_log_url  String  @default("") @db.VarChar(200)
  sdk_json         String  @default("") @db.VarChar(1000)
  source_code_json String  @default("") @db.VarChar(1000)
  doc_json         String  @default("") @db.VarChar(1000)
  remark_json      String  @default("") @db.VarChar(1000)
  create_user_id   Int     @default(0)
  status           Boolean @default(false)
  release_time     Int     @default(0)
  ctime            Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_sdk_online {
  id               Int      @id @default(autoincrement())
  platform         Int      @default(0) @db.TinyInt
  version_type     Int      @default(0) @db.TinyInt
  charge           Int      @default(3) @db.TinyInt
  version          String   @default("") @db.VarChar(16)
  version_log_url  String   @default("") @db.VarChar(200)
  sdk_json         String   @default("") @db.VarChar(1000)
  source_code_json String   @default("") @db.VarChar(1000)
  doc_json         String   @default("") @db.VarChar(1000)
  remark_json      String   @default("") @db.VarChar(1000)
  related_json     String   @db.VarChar(1000)
  create_user_id   Int      @default(0)
  status           Boolean  @default(false)
  release_time     Int      @default(0)
  ctime            Int      @default(0)
  sdk_new_id       Int?
  history_sdk_json String?  @db.VarChar(1000)
  site             Boolean? @default(false)
  product_category Boolean? @default(false)
  is_minprogram    Int      @default(0) @db.UnsignedTinyInt
  is_solution      Int      @default(0) @db.UnsignedTinyInt
  comment          String   @db.Text
  connection       String   @default("") @db.VarChar(255)
  description      String   @db.Text
  desc_image       String   @default("") @db.VarChar(255)
  three_level_json String   @db.Text
  maven_json       String?  @db.VarChar(5000)
  other_json       String?  @db.VarChar(5000)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_sdk_online_version {
  id               Int      @id @default(autoincrement())
  platform         Int      @default(0) @db.TinyInt
  version_type     Int      @default(0) @db.TinyInt
  charge           Int      @default(3) @db.TinyInt
  version          String   @default("") @db.VarChar(16)
  version_log_url  String   @default("") @db.VarChar(200)
  sdk_json         String   @default("") @db.VarChar(1000)
  source_code_json String   @default("") @db.VarChar(1000)
  doc_json         String   @default("") @db.VarChar(1000)
  remark_json      String   @default("") @db.VarChar(1000)
  related_json     String   @db.VarChar(1000)
  create_user_id   Int      @default(0)
  status           Boolean  @default(false)
  release_time     Int      @default(0)
  ctime            Int      @default(0)
  sdk_new_id       Int?
  history_sdk_json String?  @db.VarChar(1000)
  site             Boolean? @default(false)
  product_category Boolean? @default(false)
  is_minprogram    Int      @default(0) @db.UnsignedTinyInt
  is_solution      Int      @default(0) @db.UnsignedTinyInt
  comment          String   @db.Text
  connection       String   @default("") @db.VarChar(255)
  description      String   @db.Text
  desc_image       String   @default("") @db.VarChar(255)
  three_level_json String   @db.Text
  maven_json       String?  @db.VarChar(5000)
  other_json       String?  @db.VarChar(5000)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_ticket_role {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  date        Int     @default(0) @db.TinyInt
  hour        Int     @default(0) @db.TinyInt
  user_id     Int     @default(0)
  platform    Boolean @default(false)
  create_time Int     @default(0)
  is_del      Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_ticket_template {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  title          String  @default("") @db.VarChar(200)
  content        String? @db.Text
  create_time    Int     @default(0)
  create_user_id Int     @default(0)
  status         Boolean @default(false)
  platform       Int?    @default(100) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_user {
  id                     Int                      @id @default(autoincrement()) @db.UnsignedSmallInt
  username               String                   @unique(map: "username") @db.VarChar(30)
  name                   String?                  @db.VarChar(30)
  password               String                   @db.Char(32)
  role_id                String                   @db.VarChar(512)
  last_login_time        Int                      @db.UnsignedInt
  last_login_ip          String                   @db.VarChar(40)
  login_count            Int?                     @default(0) @db.UnsignedMediumInt
  email                  String                   @db.VarChar(50)
  mobile                 String                   @db.VarChar(11)
  create_time            Int                      @db.UnsignedInt
  update_time            Int                      @db.UnsignedInt
  status                 Boolean?                 @default(true)
  qq                     Int?
  google                 String?                  @db.VarChar(255)
  wechat_qrcode          String?                  @db.VarChar(255)
  rcloud_ticket_transfer rcloud_ticket_transfer[]

  @@index([email], map: "email")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_user_copy {
  id              Int      @id @default(autoincrement()) @db.UnsignedSmallInt
  username        String   @unique(map: "username") @db.VarChar(30)
  name            String?  @db.VarChar(30)
  password        String   @db.Char(32)
  role_id         String   @db.VarChar(512)
  last_login_time Int      @db.UnsignedInt
  last_login_ip   String   @db.VarChar(40)
  login_count     Int?     @default(0) @db.UnsignedMediumInt
  email           String   @db.VarChar(50)
  mobile          String   @db.VarChar(11)
  create_time     Int      @db.UnsignedInt
  update_time     Int      @db.UnsignedInt
  status          Boolean? @default(true)
  qq              Int?
  google          String?  @db.VarChar(255)
  wechat_qrcode   String?  @db.VarChar(255)

  @@index([email], map: "email")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_user_discount {
  id          Int     @id @default(autoincrement())
  admin_id    Int     @default(0) @db.UnsignedInt
  sales_id    Int     @default(0)
  is_del      Boolean @default(false)
  create_time Int     @default(0)
  update_time Int     @default(0)
  scheme_id   Int     @default(0)
  remark      String  @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_user_url {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  name        String @default("") @db.VarChar(30)
  url         String @default("") @db.VarChar(300)
  sort        Int    @default(0)
  color       String @default("") @db.VarChar(20)
  admin_id    Int    @default(0)
  update_time Int    @default(0)
  del         Int    @default(1) @db.TinyInt
  status      Int    @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_user_url_statistics {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  url         String @default("") @db.VarChar(300)
  admin_id    Int    @default(0)
  admin_name  String @default("") @db.VarChar(100)
  create_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_admin_voip {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int     @default(0) @db.UnsignedInt
  appkey         String  @unique(map: "appkey_UNIQUE") @default("0") @db.VarChar(45)
  test_appkey    String  @default("0") @db.VarChar(45)
  email          String  @default("0") @db.VarChar(128)
  ctime          Int     @default(0) @db.UnsignedInt
  is_out         Int     @default(0) @db.UnsignedTinyInt
  app_id         Int     @unique(map: "app_id") @default(0) @db.UnsignedInt
  status         Int     @default(0) @db.UnsignedTinyInt
  utime          Int     @default(0) @db.UnsignedInt
  signkey        String  @default("") @db.VarChar(64)
  save_server    Int     @default(0) @db.UnsignedTinyInt
  venderkey      String  @default("") @db.VarChar(64)
  price_audio    Decimal @default(0.0000) @db.Decimal(11, 4)
  price_sd       Decimal @default(0.0000) @db.Decimal(11, 4)
  price_hd       Decimal @default(0.0000) @db.Decimal(11, 4)
  price_hdd      Decimal @default(0.0000) @db.Decimal(11, 4)
  name           String  @default("") @db.VarChar(125)
  agora_id       String  @default("") @db.VarChar(64)
  blink_key      String  @default("") @db.VarChar(64)
  test_blink_key String  @default("") @db.VarChar(64)
  close_type     Boolean @default(false)
  audio_type     Boolean @default(false)

  @@index([appkey], map: "appkey")
  @@index([user_id, appkey], map: "idx_userId_appkey")
  @@index([user_id], map: "idx_user_id")
  @@index([test_appkey], map: "test_appkey")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_adv {
  id          Int      @id @default(autoincrement())
  name        String   @db.VarChar(255)
  content     String   @db.Text
  type        Int      @default(0) @db.TinyInt
  start_time  DateTime @db.DateTime(0)
  end_time    DateTime @db.DateTime(0)
  create_time DateTime @default(now()) @db.Timestamp(0)
  action      String   @db.VarChar(255)
  where       String   @db.VarChar(255)
  do          String   @db.VarChar(255)
  close       Boolean?
  count       Int?
  target      Int      @default(0) @db.TinyInt
  sort        Int      @default(1) @db.TinyInt
  color       String?  @db.VarChar(20)
}

model rcloud_agent_request_log {
  id            Int    @id @default(autoincrement())
  ip            String @default("") @db.VarChar(32)
  create_time   Int    @default(0)
  post_params   String @default("") @db.VarChar(1024)
  header_params String @default("") @db.VarChar(2048)
  request_url   String @default("") @db.VarChar(256)
  uid           Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ana {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int    @db.UnsignedInt
  app_id      Int    @db.UnsignedInt
  is_online   Int    @db.TinyInt
  func        String @db.VarChar(64)
  param       String @db.VarChar(256)
  times       Int    @db.UnsignedInt
  create_time Int    @db.UnsignedInt
  update_time Int    @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_api_ip_list {
  id            Int     @id @default(autoincrement())
  app_id        Int
  api_url       String? @db.VarChar(255)
  status        Int
  ctime         String  @db.VarChar(32)
  app_key       String  @db.VarChar(45)
  env           Int?
  type          Int?
  test_end_time String  @default("") @db.VarChar(32)
  manage_url    String? @db.VarChar(255)
  pay_status    Boolean @default(false)
  utime         String  @db.VarChar(32)
  oem_url       String  @default("") @db.VarChar(255)
  big_net       String  @default("") @db.VarChar(255)
  center_id     Int?    @default(0)

  @@index([test_end_time], map: "index_1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_api_ip_list_20210119 {
  id            Int     @id @default(autoincrement())
  app_id        Int
  api_url       String? @db.VarChar(255)
  status        Int
  ctime         String  @db.VarChar(32)
  app_key       String  @db.VarChar(45)
  env           Int?
  type          Int?
  test_end_time String  @default("") @db.VarChar(32)
  manage_url    String? @db.VarChar(255)
  pay_status    Boolean @default(false)
  utime         String  @db.VarChar(32)
  oem_url       String  @default("") @db.VarChar(255)
  big_net       String  @default("") @db.VarChar(255)

  @@index([test_end_time], map: "index_1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apicategory {
  id   Int    @id @default(autoincrement())
  name String @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apicategory_1230 {
  id   Int    @id @default(autoincrement())
  name String @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apicategory_bak {
  id   Int    @id @default(autoincrement())
  name String @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apitool {
  id        Int     @id @default(autoincrement())
  c_id      Int
  name      String  @db.VarChar(255)
  uri       String  @db.VarChar(255)
  params    String  @db.Text
  out       String  @db.VarChar(255)
  checkurl  String? @db.VarChar(255)
  docUrl    String  @db.VarChar(255)
  apihost   String? @default("api_url") @db.VarChar(20)
  apimehtod String? @default("post") @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apitool_20200602 {
  id        Int     @id @default(autoincrement())
  c_id      Int
  name      String  @db.VarChar(255)
  uri       String  @db.VarChar(255)
  params    String  @db.Text
  out       String  @db.VarChar(255)
  checkurl  String? @db.VarChar(255)
  docUrl    String  @db.VarChar(255)
  apihost   String? @default("api_url") @db.VarChar(20)
  apimehtod String? @default("post") @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apitool_20210928 {
  id        Int     @id @default(autoincrement())
  c_id      Int
  name      String  @db.VarChar(255)
  uri       String  @db.VarChar(255)
  params    String  @db.Text
  out       String  @db.VarChar(255)
  checkurl  String? @db.VarChar(255)
  docUrl    String  @db.VarChar(255)
  apihost   String? @default("api_url") @db.VarChar(20)
  apimehtod String? @default("post") @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apitool_20211214 {
  id        Int     @id @default(autoincrement())
  c_id      Int
  name      String  @db.VarChar(255)
  uri       String  @db.VarChar(255)
  params    String  @db.Text
  out       String  @db.VarChar(255)
  checkurl  String? @db.VarChar(255)
  docUrl    String  @db.VarChar(255)
  apihost   String? @default("api_url") @db.VarChar(20)
  apimehtod String? @default("post") @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_apitool_bak {
  id        Int     @id @default(autoincrement())
  c_id      Int
  name      String  @db.VarChar(255)
  uri       String  @db.VarChar(255)
  params    String  @db.Text
  out       String  @db.VarChar(255)
  checkurl  String? @db.VarChar(255)
  docUrl    String  @db.VarChar(255)
  apihost   String? @default("api_url") @db.VarChar(20)
  apimehtod String? @default("post") @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app {
  id                     Int       @id @default(autoincrement()) @db.UnsignedInt
  name                   String    @default("") @db.VarChar(125)
  category               Int       @default(0)
  app_id                 String    @default("") @db.VarChar(45)
  create_ip              String    @default("") @db.VarChar(16)
  user_id                Int       @default(0)
  create_time            Int       @default(0)
  last_modified_time     DateTime? @default(now()) @db.Timestamp(0)
  status                 Boolean   @default(false)
  version                Boolean   @default(false)
  description            String?   @db.Text
  app_secret             String    @default("") @db.VarChar(100)
  service_status         Boolean   @default(false)
  push_certificate       String    @default("") @db.VarChar(150)
  authorize_email        String    @default("") @db.VarChar(150)
  manage_url             String    @default("") @db.VarChar(150)
  update_time            Int       @default(0)
  customer_status        Boolean   @default(false)
  test_app_id            String    @default("") @db.VarChar(45)
  test_app_secret        String    @default("") @db.VarChar(100)
  test_push_certificate  String    @default("") @db.VarChar(150)
  target_id              String    @default("") @db.VarChar(32)
  test_target_id         String    @default("") @db.VarChar(32)
  cooperate_id           Int       @default(0)
  follow_status          Boolean   @default(false)
  dev_token_expire       String    @default("0") @db.Char(15)
  produce_token_expire   String    @default("0") @db.Char(15)
  arg_option             Boolean   @default(false)
  test_arg_option        Boolean   @default(false)
  ismarket               Boolean   @default(true)
  app_use                Boolean   @default(false)
  user_level             Int       @default(0) @db.TinyInt
  sms_pic                Int       @default(0) @db.TinyInt
  test_sms_pic           Int       @default(0) @db.TinyInt
  pay_version            Boolean   @default(false)
  business_level         Boolean   @default(true)
  business_update_time   Int       @default(0)
  delete_time            Int       @default(0)
  params                 String    @default("") @db.VarChar(500)
  equipment_number_count Int?      @default(0)

  @@index([app_id], map: "app_id")
  @@index([category], map: "category")
  @@index([user_id], map: "indexs")
  @@index([app_id], map: "ix_app_id")
  @@index([test_app_id], map: "ix_test_app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_api {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  callback_url  String  @default("") @db.VarChar(200)
  app_id        Int     @default(0)
  app_key       String  @default("") @db.VarChar(50)
  env           Boolean @default(true)
  user_id       Int     @default(0)
  create_time   Int     @default(0)
  status        Boolean @default(true)
  start_time    Int     @default(0)
  end_time      Int     @default(0)
  num           Int     @default(0)
  frequency     Boolean @default(true)
  admin_user_id Int     @default(0)
  type          Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_api_list_user {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0)
  app_id      Int     @default(0)
  create_time Int     @default(0)
  status      Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_approvement {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id          Int     @default(0)
  app_name        String  @default("") @db.VarChar(100)
  app_description String? @db.TinyText
  status          Boolean @default(false)
  create_time     Int     @default(0)
  user_id         Int     @default(0)
  is_again_audt   Boolean @default(false)

  @@index([app_id], map: "idx_app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_category {
  id   Int    @id
  name String @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_customer {
  id                   Int     @id @default(autoincrement()) @db.UnsignedInt
  domain               String  @default("") @db.VarChar(200)
  test_domain          String  @default("") @db.VarChar(200)
  authorize_email      String  @default("") @db.VarChar(100)
  test_authorize_email String  @default("") @db.VarChar(100)
  manage_url           String  @default("") @db.VarChar(200)
  test_manage_url      String  @default("") @db.VarChar(200)
  target_id            String  @default("") @db.VarChar(32)
  test_target_id       String  @default("") @db.VarChar(32)
  status               Boolean @default(false)
  test_status          Boolean @default(false)
  app_id               Int     @default(0)
  create_time          Int     @default(0)
  produce_time         Int     @default(0)
  test_time            Int     @default(0)
  is_del               Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_download_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  category    Boolean @default(false)
  version     String  @default("0") @db.VarChar(100)
  user_id     Int     @default(0)
  create_time Int     @default(0)
  ip          String  @default("'") @db.VarChar(20)
  site        Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_equipment_record {
  id                 Int      @id @default(autoincrement()) @db.UnsignedInt
  app_id             Int      @default(0) @db.UnsignedInt
  create_time        String   @default("") @db.VarChar(12)
  update_time        String   @default("") @db.VarChar(12)
  admin_id           Int      @default(0)
  number             Int      @default(0)
  accumulative_total Int      @default(0) @db.UnsignedInt
  batch              String   @default("0") @db.VarChar(30)
  is_del             Boolean  @default(false)
  remark             String?  @db.Text
  price              Decimal? @default(0.00) @db.Decimal(11, 2)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_expand {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  app_id      Int     @default(0) @db.UnsignedInt
  type        Int     @default(0) @db.UnsignedTinyInt
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Int     @default(0) @db.UnsignedTinyInt
  value1      String? @default("") @db.VarChar(255)
  value2      String? @default("") @db.VarChar(255)
  value3      String? @default("") @db.VarChar(255)
  value4      String? @default("") @db.VarChar(255)
  value5      String? @default("") @db.VarChar(255)
  remark      String? @default("") @db.VarChar(255)
  config      String? @db.Text

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_first {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int     @default(0)
  app_id         Int     @default(0)
  server_id      Int     @default(0)
  env            Boolean @default(false)
  dynamic_date   Int     @default(0)
  user_add_date  Int     @default(0)
  message_date   Int     @default(0)
  im_open_date   Int     @default(0)
  rtc_open_date  Int     @default(0)
  rtc_value_date Int     @default(0)
  create_time    Int     @default(0)
  update_time    Int     @default(0)
  remark         String? @default("") @db.VarChar(100)
  config         String? @default("") @db.VarChar(200)

  @@index([app_id], map: "app_id")
  @@index([dynamic_date], map: "dynamic_date")
  @@index([im_open_date], map: "im_open_date")
  @@index([message_date], map: "message_date")
  @@index([rtc_open_date], map: "rtc_open_date")
  @@index([rtc_open_date], map: "rtc_value_date")
  @@index([server_id], map: "server_id")
  @@index([user_add_date], map: "user_add_date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_first_copy {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int     @default(0)
  app_id         Int     @default(0)
  app_key        String  @default("") @db.VarChar(20)
  env            Boolean @default(false)
  dynamic_date   Int     @default(0)
  dynamic        BigInt  @default(0)
  user_add_date  Int     @default(0)
  user_add       Int     @default(0)
  message_date   Int     @default(0)
  message        BigInt? @default(0)
  im_open_date   Int     @default(0)
  rtc_open_date  Int     @default(0)
  rtc_value_date Int     @default(0)
  rtc_value      BigInt? @default(0)
  create_time    Int     @default(0)
  update_time    Int     @default(0)
  remark         String? @default("") @db.VarChar(100)
  config         String? @default("") @db.VarChar(200)

  @@index([app_id], map: "app_id")
  @@index([app_key], map: "app_key")
  @@index([dynamic_date], map: "dynamic_date")
  @@index([im_open_date], map: "im_open_date")
  @@index([message_date], map: "message_date")
  @@index([rtc_open_date], map: "rtc_open_date")
  @@index([rtc_open_date], map: "rtc_value_date")
  @@index([update_time], map: "update_time")
  @@index([user_add_date], map: "user_add_date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_follow {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id            Int     @default(0)
  follow_status     Boolean @default(false)
  operation_user_id Int     @default(0)
  create_time       Int     @default(0)
  create_user_id    Int     @default(0)
  description       String? @db.Text
  platform          String? @default("0") @db.VarChar(10)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_id {
  server_id Int @id @db.UnsignedInt
  app_id    Int @db.UnsignedInt
  env       Int @db.UnsignedTinyInt

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_log {
  id          Int    @id @default(autoincrement())
  user_id     Int    @default(0)
  app_id      Int    @default(0)
  oper        String @default("") @db.VarChar(45)
  content     String @db.Text
  create_time Int    @default(0)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_message_reminder {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  server_id    Int      @default(0) @db.UnsignedInt
  app_id       Int      @default(0) @db.UnsignedInt
  service      Int?     @default(0)
  type         Int      @default(0) @db.TinyInt
  status       Int      @default(0) @db.UnsignedTinyInt
  is_first     Int      @default(1) @db.UnsignedTinyInt
  env          Int      @default(1) @db.UnsignedTinyInt
  create_time  String   @default("") @db.VarChar(12)
  update_time  String   @default("") @db.VarChar(12)
  tally        String   @default("0") @db.VarChar(15)
  lev          Int?     @default(0) @db.TinyInt
  is_del       Int      @default(0) @db.UnsignedTinyInt
  send_status  Int?     @default(0) @db.TinyInt
  send_time    String?  @default("") @db.VarChar(12)
  limit_status Boolean? @default(false)
  loop_time    Int      @default(0) @db.UnsignedTinyInt
  remark       String?  @default("") @db.VarChar(200)
  config       String?  @default("") @db.VarChar(200)

  @@index([app_id], map: "index_app_id")
  @@index([create_time], map: "index_create_time")
  @@index([tally], map: "index_tally")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_message_reminder_copy {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  server_id    Int      @default(0) @db.UnsignedInt
  app_id       Int      @default(0) @db.UnsignedInt
  service      Int?     @default(0)
  type         Int      @default(0) @db.TinyInt
  status       Int      @default(0) @db.UnsignedTinyInt
  is_first     Int      @default(1) @db.UnsignedTinyInt
  env          Int      @default(1) @db.UnsignedTinyInt
  create_time  String   @default("") @db.VarChar(12)
  update_time  String   @default("") @db.VarChar(12)
  tally        String   @default("0") @db.VarChar(15)
  lev          Int?     @default(0) @db.TinyInt
  is_del       Int      @default(0) @db.UnsignedTinyInt
  send_status  Int?     @default(0) @db.TinyInt
  send_time    String?  @default("") @db.VarChar(12)
  limit_status Boolean? @default(false)
  loop_time    Int      @default(0) @db.UnsignedTinyInt
  remark       String?  @default("") @db.VarChar(200)
  config       String?  @default("") @db.VarChar(200)

  @@index([app_id], map: "index_app_id")
  @@index([create_time], map: "index_create_time")
  @@index([tally], map: "index_tally")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_message_reminder_copy1 {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  server_id    Int      @default(0) @db.UnsignedInt
  app_id       Int      @default(0) @db.UnsignedInt
  service      Int?     @default(0)
  type         Int      @default(0) @db.TinyInt
  status       Int      @default(0) @db.UnsignedTinyInt
  is_first     Int      @default(1) @db.UnsignedTinyInt
  env          Int      @default(1) @db.UnsignedTinyInt
  create_time  String   @default("") @db.VarChar(12)
  update_time  String   @default("") @db.VarChar(12)
  tally        String   @default("0") @db.VarChar(15)
  lev          Int?     @default(0) @db.TinyInt
  is_del       Int      @default(0) @db.UnsignedTinyInt
  send_status  Int?     @default(0) @db.TinyInt
  send_time    String?  @default("") @db.VarChar(12)
  limit_status Boolean? @default(false)
  loop_time    Int      @default(0) @db.UnsignedTinyInt
  remark       String?  @default("") @db.VarChar(200)
  config       String?  @default("") @db.VarChar(200)

  @@index([app_id], map: "index_app_id")
  @@index([create_time], map: "index_create_time")
  @@index([tally], map: "index_tally")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_message_trigger {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  app_id      Int?   @default(0) @db.UnsignedInt
  is_first    Int    @default(1) @db.UnsignedTinyInt
  env         Int    @default(1) @db.UnsignedTinyInt
  create_time String @default("") @db.VarChar(12)
  update_time String @default("") @db.VarChar(12)
  tally       String @default("0") @db.VarChar(15)
  lev         Int    @default(5) @db.UnsignedTinyInt
  is_del      Int    @default(0) @db.UnsignedTinyInt

  @@index([app_id], map: "index_app_id")
  @@index([create_time], map: "index_create_time")
  @@index([tally], map: "index_tally")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_message_trigger_copy {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  app_id      Int?   @default(0) @db.UnsignedInt
  is_first    Int    @default(1) @db.UnsignedTinyInt
  env         Int    @default(1) @db.UnsignedTinyInt
  create_time String @default("") @db.VarChar(12)
  update_time String @default("") @db.VarChar(12)
  tally       String @default("0") @db.VarChar(15)
  lev         Int    @default(5) @db.UnsignedTinyInt
  is_del      Int    @default(0) @db.UnsignedTinyInt

  @@index([app_id], map: "index_app_id")
  @@index([create_time], map: "index_create_time")
  @@index([tally], map: "index_tally")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_online {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id        Int     @default(0)
  app_key       String  @default("") @db.VarChar(50)
  admin_user_id Int     @default(0)
  user_id       Int     @default(0)
  create_time   Int     @default(0)
  oper          Boolean @default(false)
  channel       String  @default("developer") @db.Char(10)
  is_del        Boolean @default(false)
  is_offline    Int     @default(0) @db.UnsignedTinyInt

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_operating_record {
  id           Int    @id @default(autoincrement())
  user_id      Int    @default(0) @db.UnsignedInt
  app_id       String @default("") @db.VarChar(50)
  service_id   Int    @default(0) @db.UnsignedInt
  start_at     Int    @default(0) @db.UnsignedInt
  end_at       Int    @default(0) @db.UnsignedInt
  is_probation Int    @default(0) @db.UnsignedTinyInt
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int?   @db.UnsignedInt
  extra        String @default("") @db.VarChar(1000)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_options {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  app_id      Int    @db.UnsignedInt
  environment Int    @db.TinyInt
  key         String @db.VarChar(32)
  value       String @db.LongText
  status      Int    @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_send_relation {
  id           Int     @id @default(autoincrement())
  state        Int     @default(1) @db.TinyInt
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  admin_id     Int     @default(0)
  app_key      String  @default("") @db.VarChar(30)
  app_id       Int     @default(0)
  env          Int     @default(1) @db.TinyInt
  bind_app_key String  @default("") @db.VarChar(30)
  bind_app_id  Int     @default(0)
  bind_app_env Boolean @default(true)
  is_del       Boolean @default(false)

  @@index([app_id], map: "app_id")
  @@index([app_key], map: "app_key")
  @@index([bind_app_id], map: "bind_app_id")
  @@index([bind_app_key], map: "bind_app_key")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_test {
  id                     Int     @id @default(autoincrement()) @db.UnsignedInt
  name                   String  @default("") @db.VarChar(125)
  category               Int     @default(0)
  app_id                 String  @default("") @db.VarChar(45)
  create_ip              String  @default("") @db.VarChar(16)
  user_id                Int     @default(0)
  create_time            Int     @default(0)
  status                 Boolean @default(false)
  version                Boolean @default(false)
  description            String? @db.Text
  app_secret             String  @default("") @db.VarChar(100)
  service_status         Boolean @default(false)
  push_certificate       String  @default("") @db.VarChar(150)
  authorize_email        String  @default("") @db.VarChar(150)
  manage_url             String  @default("") @db.VarChar(150)
  update_time            Int     @default(0)
  customer_status        Boolean @default(false)
  test_app_id            String  @default("") @db.VarChar(45)
  test_app_secret        String  @default("") @db.VarChar(100)
  test_push_certificate  String  @default("") @db.VarChar(150)
  target_id              String  @default("") @db.VarChar(32)
  test_target_id         String  @default("") @db.VarChar(32)
  cooperate_id           Int     @default(0)
  follow_status          Boolean @default(false)
  dev_token_expire       String  @default("0") @db.Char(15)
  produce_token_expire   String  @default("0") @db.Char(15)
  arg_option             Boolean @default(false)
  test_arg_option        Boolean @default(false)
  ismarket               Boolean @default(true)
  app_use                Boolean @default(false)
  user_level             Int     @default(0) @db.TinyInt
  sms_pic                Int     @default(0) @db.TinyInt
  test_sms_pic           Int     @default(0) @db.TinyInt
  pay_version            Boolean @default(false)
  business_level         Boolean @default(true)
  business_update_time   Int     @default(0)
  delete_time            Int     @default(0)
  params                 String  @default("") @db.VarChar(500)
  equipment_number_count Int?    @default(0)

  @@index([app_id], map: "app_id")
  @@index([category], map: "category")
  @@index([user_id], map: "indexs")
  @@index([app_id], map: "ix_app_id")
  @@index([test_app_id], map: "ix_test_app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_app_will_close {
  id         Int     @id @default(autoincrement())
  type       Int     @default(0) @db.TinyInt
  user_id    Int     @default(0)
  app_id     Int     @default(0)
  count      Int     @default(0)
  sign_month String? @default("") @db.VarChar(255)
  deal       Int?    @default(0) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_application {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  username    String   @default("") @db.VarChar(100)
  mobile      String   @default("0") @db.Char(11)
  create_time Int      @default(0)
  ip          String   @default("") @db.Char(20)
  exercise_id Int      @default(0)
  type        Boolean? @default(false)
  address     String?  @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_appstat {
  id                      Int      @id @default(autoincrement()) @db.UnsignedInt
  create_user             Int?
  create_app_developer    Int?
  create_app_agent        Int?
  dynamic_app             Int?
  send_msg_test           BigInt?
  send_msg                BigInt?
  send_msg_app_test       Int?
  send_msg_app            Int?
  online_app_developer    Int?
  online_app_agent        Int?
  time                    String?  @unique(map: "time") @db.VarChar(64)
  createtime              DateTime @default(now()) @db.Timestamp(0)
  online_app_developer_50 Int?
  online_app_agent_50     Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_appstat_dynamic_time {
  ctime     Int  @default(0)
  server_id Int  @default(0)
  dynamic   Int  @default(0)
  app_id    Int  @id @default(0)
  channel   Int? @default(1) @db.SmallInt

  @@index([ctime, app_id, server_id], map: "app_time_index")
  @@index([server_id], map: "server_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_attachment {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  path            String  @default("") @db.VarChar(150)
  ticket_id       Int     @default(0)
  user_id         Int     @default(0)
  create_time     Int     @default(0)
  ticket_reply_id Int     @default(0)
  suffix          String  @default("'") @db.Char(10)
  status          Boolean @default(false)
  source_name     String  @default("") @db.VarChar(150)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_audio_account {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  total       Decimal @default(0.0000) @db.Decimal(11, 4)
  consume     Decimal @default(0.0000) @db.Decimal(11, 4)
  surplus     Decimal @default(0.0000) @db.Decimal(11, 4)
  ctime       Int     @default(0) @db.UnsignedInt
  utime       Int     @default(0) @db.UnsignedInt
  is_stop     Int     @default(0) @db.UnsignedTinyInt
  price_audio Decimal @default(0.0000) @db.Decimal(11, 4)
  price_sd    Decimal @default(0.0000) @db.Decimal(11, 4)
  price_hd    Decimal @default(0.0000) @db.Decimal(11, 4)
  price_hdd   Decimal @default(0.0000) @db.Decimal(11, 4)
  is_recharge Boolean @default(false)
  given       Decimal @default(0.0000) @db.Decimal(11, 4)

  @@index([user_id, surplus], map: "idx_userId_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_audio_consume_log {
  id               Int     @id @default(autoincrement())
  user_id          Int     @default(0) @db.UnsignedInt
  consume_cny      Decimal @default(0.0000) @db.Decimal(11, 4)
  ctime            Int     @default(0) @db.UnsignedInt
  audio_time       Int     @default(0) @db.UnsignedInt
  video_HDtime     BigInt  @default(0) @db.UnsignedBigInt
  consume_usd      Decimal @default(0.0000) @db.Decimal(11, 4)
  consume_time     Int     @default(0) @db.UnsignedInt
  exchange         Decimal @default(0.0000) @db.Decimal(11, 4)
  video_SDtime     BigInt  @default(0) @db.UnsignedBigInt
  video_HDDtime    BigInt  @default(0) @db.UnsignedBigInt
  appkey           String  @default("0") @db.VarChar(45)
  test_appkey      String  @default("0") @db.VarChar(45)
  real_consume_cny Decimal @default(0.0000) @db.Decimal(11, 4)

  @@index([user_id], map: "idx_use_id")
  @@index([user_id, appkey, consume_time], map: "idx_userId_appkey_consumeTime")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_audio_detail {
  id           Int     @id @default(autoincrement())
  voip_id      Int     @default(0)
  start_time   Int     @default(0)
  vuid         String  @default("0") @db.VarChar(32)
  seconds      Int     @default(0)
  call_id      String  @default("0") @db.VarChar(32)
  type         Boolean @default(true)
  channel_name String  @default("0") @db.VarChar(128)
  nat          String  @default("0") @db.VarChar(10)

  @@index([start_time], map: "start_time")
  @@index([voip_id, start_time], map: "voip_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_audio_month {
  id               Int     @id @default(autoincrement())
  user_id          Int     @default(0) @db.UnsignedInt
  appkey           String  @default("0") @db.VarChar(45)
  test_appkey      String  @default("0") @db.VarChar(45)
  audio_time       BigInt  @default(0) @db.UnsignedBigInt
  video_HDtime     BigInt  @default(0) @db.UnsignedBigInt
  video_SDtime     BigInt  @default(0) @db.UnsignedBigInt
  video_HDDtime    BigInt  @default(0) @db.UnsignedBigInt
  consume_cny      Decimal @default(0.0000) @db.Decimal(11, 4)
  month            String  @default("0") @db.Char(6)
  ctime            Int     @default(0) @db.UnsignedInt
  consume_usd      Decimal @default(0.0000) @db.Decimal(11, 4)
  exchange         Decimal @default(0.0000) @db.Decimal(11, 4)
  status           Boolean @default(false)
  is_send          Boolean @default(false)
  month_audio      BigInt  @default(0)
  month_sd         BigInt  @default(0)
  month_hd         BigInt  @default(0)
  month_hdd        BigInt  @default(0)
  log_audio        BigInt  @default(0)
  log_sd           BigInt  @default(0)
  log_hd           BigInt  @default(0)
  log_hdd          BigInt  @default(0)
  given            Decimal @default(0.0000) @db.Decimal(11, 4)
  name             String  @default("") @db.VarChar(125)
  real_consume_cny Decimal @default(0.0000) @db.Decimal(11, 4)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_audio_order {
  id         Int     @id @default(autoincrement())
  order_id   BigInt  @default(0)
  user_id    Int     @default(0)
  payment    Boolean @default(true)
  money      Decimal @default(0.0000) @db.Decimal(11, 4)
  status     Int     @default(2) @db.TinyInt
  ctime      Int     @default(0)
  utime      Int     @default(0)
  audio_type Boolean @default(false)
  app_id     Int     @default(0)

  @@index([audio_type], map: "audio_type")
  @@index([order_id], map: "order_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_audio_order_line {
  id           Int     @id @default(autoincrement())
  user_id      Int     @default(0)
  account_type Int     @default(0)
  money        Decimal @default(0.00) @db.Decimal(11, 2)
  line_num     String  @default("0") @db.VarChar(50)
  admin_id     Int
  audit_id     Int     @default(0)
  reason       String  @db.VarChar(255)
  remark       String  @default("") @db.VarChar(255)
  file         String  @default("") @db.VarChar(255)
  status       Int     @default(2) @db.TinyInt
  ctime        Int     @default(0)
  utime        Int?    @default(0)
  invoice      Int?    @default(0)
  cdate        String? @db.VarChar(32)
  order_id     String? @db.VarChar(32)
  audio_type   Boolean @default(false)
  app_id       Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_auth {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int    @db.UnsignedInt
  app_id      Int    @db.UnsignedInt
  source      String @db.VarChar(15)
  source_id   String @db.VarChar(255)
  token       String @db.VarChar(1024)
  create_time Int    @db.UnsignedInt

  @@index([user_id], map: "user_id")
}

model rcloud_authassignment {
  itemname        String          @db.VarChar(64)
  userid          String          @db.VarChar(64)
  bizrule         String?         @db.Text
  data            String?         @db.Text
  rcloud_authitem rcloud_authitem @relation(fields: [itemname], references: [name], onDelete: Cascade, map: "rcloud_AuthAssignment_ibfk_1")

  @@id([itemname, userid])
}

model rcloud_authitem {
  name                                                              String                  @id @db.VarChar(64)
  type                                                              Int
  description                                                       String?                 @db.Text
  bizrule                                                           String?                 @db.Text
  data                                                              String?                 @db.Text
  rcloud_authassignment                                             rcloud_authassignment[]
  rcloud_authitemchild_rcloud_authitemchild_parentTorcloud_authitem rcloud_authitemchild[]  @relation("rcloud_authitemchild_parentTorcloud_authitem")
  rcloud_authitemchild_rcloud_authitemchild_childTorcloud_authitem  rcloud_authitemchild[]  @relation("rcloud_authitemchild_childTorcloud_authitem")
}

model rcloud_authitemchild {
  parent                                                       String          @db.VarChar(64)
  child                                                        String          @db.VarChar(64)
  rcloud_authitem_rcloud_authitemchild_parentTorcloud_authitem rcloud_authitem @relation("rcloud_authitemchild_parentTorcloud_authitem", fields: [parent], references: [name], onDelete: Cascade, map: "rcloud_AuthItemChild_ibfk_1")
  rcloud_authitem_rcloud_authitemchild_childTorcloud_authitem  rcloud_authitem @relation("rcloud_authitemchild_childTorcloud_authitem", fields: [child], references: [name], onDelete: Cascade, map: "rcloud_AuthItemChild_ibfk_2")

  @@id([parent, child])
  @@index([child], map: "child")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_bad_loan {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  uid        Int    @default(0) @db.UnsignedInt
  content    String @db.VarChar(255)
  file       String @db.VarChar(255)
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_bad_loans {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  uid        Int    @default(0) @db.UnsignedInt
  content    String @db.VarChar(255)
  file       String @db.VarChar(255)
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_baidu_uv_statistic {
  id               Int      @id @default(autoincrement()) @db.UnsignedInt
  day              Int      @default(0)
  p_uv_count       Int      @default(0)
  p_new_uv_count   Int      @default(0)
  m_uv_count       Int      @default(0)
  m_new_uv_count   Int      @default(0)
  p_register_count Int      @default(0)
  m_register_count Int      @default(0)
  d_register_count Int      @default(0)
  status           Int      @default(1) @db.TinyInt
  create_by        Int      @default(0) @db.UnsignedInt
  update_by        Int      @default(0) @db.UnsignedInt
  create_time      DateTime @default(now()) @db.Timestamp(0)
  update_time      DateTime @default(now()) @db.Timestamp(0)

  @@index([day], map: "idx_day")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_blink_account {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int     @db.UnsignedInt
  status         Int     @default(0) @db.UnsignedTinyInt
  ctime          Int     @default(0) @db.UnsignedInt
  total          Decimal @default(0.0000) @db.Decimal(11, 4)
  gift_consume   Decimal @default(0.0000) @db.Decimal(11, 4)
  consume        Decimal @default(0.0000) @db.Decimal(11, 4)
  gift_surplus   Decimal @default(0.0000) @db.Decimal(11, 4)
  surplus        Decimal @default(0.0000) @db.Decimal(11, 4)
  time_total     Int     @default(0)
  time_consume   Int     @default(0)
  time_surplus   Int     @default(0)
  charging_time  Int     @default(0) @db.UnsignedInt
  app_id         Int     @default(0)
  notice         Boolean @default(false)
  service_white  Boolean @default(false)
  audio_type     Boolean @default(true)
  free_end       String? @default("") @db.VarChar(20)
  free_total     Int?    @default(0)
  free_consume   Int?    @default(0)
  free_surplus   Int?
  is_continue    Int?    @default(0) @db.TinyInt
  reminder_time  Int?    @default(0)
  reminder_money Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_blink_month {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int     @default(0) @db.UnsignedInt
  app_id     Int     @default(0) @db.UnsignedInt
  month      Int     @default(0) @db.UnsignedInt
  audio_time BigInt  @default(0) @db.UnsignedBigInt
  sd_time    BigInt  @default(0) @db.UnsignedBigInt
  hd_time    BigInt  @default(0) @db.UnsignedBigInt
  hdd_time   BigInt  @default(0) @db.UnsignedBigInt
  consume    Decimal @default(0.0000) @db.Decimal(11, 4)
  user_count Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_blink_month_2019011 {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int     @default(0) @db.UnsignedInt
  app_id     Int     @default(0) @db.UnsignedInt
  month      Int     @default(0) @db.UnsignedInt
  audio_time BigInt  @default(0) @db.UnsignedBigInt
  sd_time    BigInt  @default(0) @db.UnsignedBigInt
  hd_time    BigInt  @default(0) @db.UnsignedBigInt
  hdd_time   BigInt  @default(0) @db.UnsignedBigInt
  consume    Decimal @default(0.0000) @db.Decimal(11, 4)
  user_count Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_blink_statistics {
  sid             Int     @default(autoincrement()) @db.UnsignedInt
  id              String  @default("") @db.VarChar(40)
  inBoundBytes    Int     @default(0) @db.TinyInt
  resolutionType  Boolean @default(false)
  outBoundBytes   BigInt  @default(0)
  uid             String  @default("") @db.VarChar(40)
  appid           String  @default("") @db.VarChar(20)
  sessionDuration Int     @default(0)
  cid             String  @default("") @db.VarChar(100)
  sessionId       String  @default("") @db.VarChar(100)
  ctime           Int
  status          Boolean @default(false)
  startTime       BigInt  @default(0)
  endTime         BigInt  @default(0)
  money           Decimal @default(0.0000) @db.Decimal(11, 4)
  minute          Int     @default(0)
  app_id          Int     @default(0) @db.UnsignedInt
  user_id         Int     @default(0) @db.UnsignedInt

  @@id([sid, id])
  @@index([app_id], map: "app_id")
  @@index([appid], map: "appid")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_action {
  id              Int      @id @default(autoincrement())
  user_id         Int      @default(0)
  order_id        BigInt   @default(0)
  app_id          Int      @default(0)
  server          Int      @default(0)
  money           Decimal  @default(0.00) @db.Decimal(11, 2)
  cdate           String   @default("") @db.VarChar(16)
  invoice         Int      @default(1)
  source          Int      @default(0)
  file            String   @default("") @db.VarChar(256)
  reason          String   @default("") @db.VarChar(256)
  remark          String   @default("") @db.VarChar(256)
  status          Int      @default(1)
  admin_id        Int      @default(0)
  audit_id        Int      @default(0)
  origin          Int      @default(1)
  ctime           DateTime @default(now()) @db.Timestamp(0)
  utime           String   @default("") @db.VarChar(32)
  op_id           String   @default("0") @db.VarChar(32)
  invoice_back    Boolean  @default(false)
  account         Boolean  @default(false)
  out_order_id    String   @default("") @db.VarChar(32)
  server_order_id String   @default("0") @db.VarChar(32)
  discount_add    String   @default("") @db.VarChar(500)
  discount_del    String   @default("") @db.VarChar(500)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_adjust {
  id       Int      @id @default(autoincrement())
  type     Int      @default(1)
  user_id  Int      @default(0)
  server   Int      @default(0)
  app_id   Int      @default(0)
  order_id BigInt   @default(0)
  money    Decimal  @default(0.00) @db.Decimal(11, 2)
  file     String   @default("") @db.VarChar(256)
  reason   String   @default("") @db.VarChar(256)
  remark   String   @default("") @db.VarChar(256)
  status   Int      @default(1)
  admin_id Int      @default(0)
  audit_id Int      @default(0)
  ctime    DateTime @default(now()) @db.Timestamp(0)
  utime    Int      @default(0)
  cdate    String   @default("") @db.VarChar(32)
  cmonth   String   @default("") @db.VarChar(32)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_compliance {
  id                   Int     @id @default(autoincrement())
  uid                  Int
  mobile_check         Boolean @default(false)
  verify_name_all      String  @db.Text
  verify_name          String  @default("") @db.VarChar(255)
  verify_num           Int     @default(0) @db.TinyInt
  concat_name_all      String  @db.Text
  concat_name          String  @default("") @db.VarChar(255)
  concat_num           Int     @default(0) @db.TinyInt
  invoice_name_all     String? @db.Text
  invoice_name         String  @default("") @db.VarChar(255)
  invoice_num          Int     @db.TinyInt
  remark               String  @default("") @db.VarChar(5000)
  status               Boolean @default(false)
  remark_status        Boolean @default(false)
  config               String  @db.VarChar(5000)
  company_name_all     String? @db.Text
  company_name         String  @db.VarChar(255)
  company_num          Int     @db.TinyInt
  app_count            Int     @default(0)
  year                 Int     @default(2020)
  practical_use        String  @default("") @db.VarChar(255)
  remark_practical_use String  @default("") @db.VarChar(255)
  tags                 String  @db.VarChar(1000)
  data_mark            String  @default("") @db.VarChar(10)

  @@unique([uid, year], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_config {
  id            Int      @id @default(autoincrement())
  typeId        Int
  name          String   @db.VarChar(50)
  type          Int
  ctime         DateTime @default(now()) @db.Timestamp(0)
  finance       String   @default("0") @db.VarChar(32)
  developerName String?  @db.VarChar(50)
  is_show       Int?     @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_discount {
  id              Int    @id @default(autoincrement())
  order_id        Int    @default(0)
  server_order_id BigInt @default(0) @db.UnsignedBigInt
  discount        Int    @default(100) @db.UnsignedInt
  begin           Int    @default(20180101)
  end             Int    @default(20290101)
  status          Int    @default(0) @db.UnsignedTinyInt
  audit_status    Int    @default(0) @db.UnsignedTinyInt
  ctime           Int    @default(0)
  utime           Int    @default(0)
  category        Int    @default(4) @db.TinyInt
  exceed_discount Int    @default(100)
  order_type      Int    @default(0) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_discount_scheme {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  show_name   String  @default("") @db.VarChar(50)
  create_time Int     @default(0)
  update_time Int     @default(0)
  config      String  @default("") @db.VarChar(200)
  is_del      Boolean @default(false)
  remark      String  @default("") @db.VarChar(200)
  admin_id    Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_invoice {
  id              Int     @id @default(autoincrement())
  user_id         Int
  title           String  @default("") @db.VarChar(255)
  type            Int     @default(1) @db.TinyInt
  money           Decimal @db.Decimal(11, 2)
  name            String  @default("") @db.VarChar(30)
  phone           String  @default("") @db.VarChar(20)
  address         String  @default("") @db.VarChar(255)
  taxpayer        String? @default("") @db.VarChar(255)
  bank            String? @default("") @db.VarChar(255)
  sign_address    String? @default("") @db.VarChar(255)
  bank_num        String? @default("") @db.VarChar(255)
  sign_phone      String? @default("") @db.VarChar(20)
  admin_id        Int     @default(0)
  status          Int     @default(0) @db.TinyInt
  ctime           Int     @default(0)
  utime           Int     @default(0)
  records         String  @default("0") @db.VarChar(5000)
  attr            String? @default("") @db.VarChar(255)
  express         String? @default("") @db.VarChar(255)
  remark          String? @default("") @db.VarChar(255)
  last_title      String? @default("") @db.VarChar(255)
  read_time       Int     @default(0) @db.UnsignedInt
  open            String? @default("") @db.VarChar(10)
  backtime        Int     @default(0)
  backfile        String? @default("") @db.VarChar(255)
  email           String  @default("") @db.VarChar(128)
  oldtype         Int     @default(0) @db.TinyInt
  oldrecords      String  @default("0") @db.VarChar(5000)
  invoice_type    Boolean @default(false)
  recharge_money  Decimal @default(0.00) @db.Decimal(11, 2)
  recharge_finish Boolean @default(false)
  invoice_num     String  @default("") @db.VarChar(20)
  application_id  Int     @default(0)
  qiniu_attr      String  @default("") @db.VarChar(255)
  dcp_status      Int     @default(0) @db.TinyInt
  invoice_publish Boolean @default(true)
  invoice_remark  String? @db.VarChar(512)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_invoice_copy {
  id              Int     @id @default(autoincrement())
  user_id         Int
  title           String  @default("") @db.VarChar(255)
  type            Int     @default(1) @db.TinyInt
  money           Decimal @db.Decimal(11, 2)
  name            String  @default("") @db.VarChar(30)
  phone           String  @default("") @db.VarChar(20)
  address         String  @default("") @db.VarChar(255)
  taxpayer        String? @default("") @db.VarChar(255)
  bank            String? @default("") @db.VarChar(255)
  sign_address    String? @default("") @db.VarChar(255)
  bank_num        String? @default("") @db.VarChar(255)
  sign_phone      String? @default("") @db.VarChar(20)
  admin_id        Int     @default(0)
  status          Int     @default(0) @db.TinyInt
  ctime           Int     @default(0)
  utime           Int     @default(0)
  records         String  @default("0") @db.VarChar(5000)
  attr            String? @default("") @db.VarChar(255)
  express         String? @default("") @db.VarChar(255)
  remark          String? @default("") @db.VarChar(255)
  last_title      String? @default("") @db.VarChar(255)
  read_time       Int     @default(0) @db.UnsignedInt
  open            String? @default("") @db.VarChar(10)
  backtime        Int     @default(0)
  backfile        String? @default("") @db.VarChar(255)
  email           String  @default("") @db.VarChar(128)
  oldtype         Int     @default(0) @db.TinyInt
  oldrecords      String  @default("0") @db.VarChar(5000)
  invoice_type    Boolean @default(false)
  recharge_money  Decimal @default(0.00) @db.Decimal(11, 2)
  recharge_finish Boolean @default(false)
  invoice_num     String  @default("") @db.VarChar(20)
  application_id  Int     @default(0)
  qiniu_attr      String  @default("") @db.VarChar(255)
  dcp_status      Int     @default(0) @db.TinyInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_monitor {
  id                 Int      @id @default(autoincrement())
  user_id            Int
  service            String   @db.VarChar(45)
  service_status     Int      @default(0) @db.TinyInt
  charging_status    Int      @default(0) @db.TinyInt
  charge             String?  @db.VarChar(45)
  app_id             Int?     @default(0)
  app_status         Int?     @default(0) @db.TinyInt
  order_no           String?  @db.VarChar(45)
  last_month_consume Decimal? @default(0.00) @db.Decimal(11, 2)
  this_month_expect  Decimal? @default(0.00) @db.Decimal(11, 2)
  status             Int?     @default(0) @db.TinyInt

  @@index([user_id], map: "rcloud_boss_monitor_user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_order {
  id                Int     @id @default(autoincrement())
  user_id           Int     @default(0)
  order_id          BigInt  @default(0)
  out_order_id      String  @default("0") @db.VarChar(32)
  account           Int?    @default(1)
  app_id            Int     @default(0)
  money             Decimal @default(0.00) @db.Decimal(11, 2)
  recharge_money    Decimal @default(0.00) @db.Decimal(11, 2)
  start             String  @default("0") @db.VarChar(16)
  end               String  @default("0") @db.VarChar(16)
  line_num          String  @default("0") @db.VarChar(50)
  server            Int     @default(0)
  admin_id          Int     @default(0)
  audit_id          Int     @default(0)
  reason            String  @default("") @db.VarChar(255)
  remark            String  @default("") @db.VarChar(255)
  file              String  @default("") @db.VarChar(255)
  status            Int     @default(1) @db.TinyInt
  ctime             Int     @default(0)
  utime             Int     @default(0)
  closetime         Int     @default(0)
  type              Int     @default(1)
  cdate             String  @default("") @db.VarChar(32)
  source            Int     @default(0)
  charge            Int     @default(0)
  price             Int     @default(0)
  day_count         String  @default("0") @db.VarChar(16)
  second_count      String  @default("0") @db.VarChar(16)
  message_size      String  @default("0") @db.VarChar(16)
  premonthmoney     String  @default("0") @db.VarChar(16)
  monthmoney        String  @default("0") @db.VarChar(16)
  online_count      String  @default("0") @db.VarChar(16)
  month_count       String  @default("0") @db.VarChar(16)
  packet_discount   String  @default("0") @db.VarChar(16)
  smsprice          String  @default("0") @db.VarChar(16)
  smsnum            String  @default("0") @db.VarChar(16)
  audioprice        String  @default("0") @db.VarChar(16)
  tags              String  @default("0") @db.VarChar(32)
  toolCode          Int     @default(0)
  policyId          Int     @default(0)
  cate_type         Int     @default(0)
  answernum         Int     @default(0)
  mcnum             Int     @default(0)
  mclen             Int     @default(0)
  out_email         String  @default("") @db.VarChar(32)
  discount          String  @default("") @db.VarChar(11)
  appkeynum         Int     @default(0)
  made              Int     @default(0)
  relevance_service String? @db.Text
  config            String? @default("") @db.VarChar(500)
  expand_config     String? @db.Text
  hand_order        Int?    @default(0) @db.TinyInt

  @@index([order_id], map: "idx_order_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_recharge {
  id                 Int       @id @default(autoincrement())
  user_id            Int       @default(0)
  order_id           BigInt    @default(0)
  app_id             Int       @default(0)
  server             Int       @default(0)
  money              Decimal   @default(0.00) @db.Decimal(11, 2)
  cdate              String    @default("") @db.VarChar(16)
  invoice            Int       @default(1)
  source             Int       @default(0)
  file               String    @default("") @db.VarChar(256)
  reason             String    @default("") @db.VarChar(256)
  remark             String    @default("") @db.VarChar(256)
  status             Int       @default(1)
  admin_id           Int       @default(0)
  audit_id           Int       @default(0)
  origin             Int       @default(1)
  ctime              DateTime? @default(now()) @db.Timestamp(0)
  utime              String    @default("") @db.VarChar(32)
  op_id              String    @default("0") @db.VarChar(32)
  invoice_back       Boolean   @default(false)
  refund             Decimal?  @default(0.00) @db.Decimal(11, 2)
  manage_id          Int       @default(0)
  account            Boolean   @default(false)
  out_order_id       String    @default("") @db.VarChar(32)
  server_order_id    String    @default("0") @db.VarChar(32)
  company_pay_id     Int       @default(0)
  remitter           String    @default("默认单位") @db.VarChar(255)
  public_cloud       Boolean?  @default(false)
  config             String?   @db.VarChar(500)
  discount           Int       @default(10) @db.UnsignedTinyInt
  time_length        Int       @default(0) @db.UnsignedTinyInt
  invoice_money      Decimal   @default(0.00) @db.Decimal(11, 2)
  currency           Boolean?  @default(true)
  payee              Boolean?  @default(true)
  tally              Boolean   @default(true)
  apply_pay_code     String    @default("") @db.VarChar(64)
  apply_pay_date     String    @default("") @db.VarChar(64)
  transfer_out_money Decimal?  @default(0.00) @db.Decimal(11, 2)
  recharge_subject   Boolean   @default(true)
  recharge_status    Boolean   @default(true)

  @@index([app_id], map: "app_id")
  @@index([company_pay_id], map: "company_pay_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_recharge_confirm {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int
  pay_account String  @db.VarChar(255)
  pay_info    String  @db.VarChar(255)
  relation    String? @db.VarChar(255)
  date        String? @db.VarChar(20)
  name        String? @db.VarChar(255)
  status      Int     @default(0) @db.TinyInt
  from        String  @db.VarChar(50)
  remark      String? @db.VarChar(255)
  ctime       Int     @default(0)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_refund {
  id             Int      @id @default(autoincrement())
  user_id        Int      @default(0)
  money          Decimal  @default(0.00) @db.Decimal(11, 2)
  file           String   @default("") @db.VarChar(256)
  reason         String   @default("") @db.VarChar(512)
  remark         String   @default("") @db.VarChar(256)
  status         Int      @default(1)
  admin_id       Int      @default(0)
  audit_id       Int      @default(0)
  ctime          DateTime @default(now()) @db.Timestamp(0)
  utime          Int      @default(0)
  cdate          String   @default("") @db.VarChar(32)
  type           Int      @default(1)
  app_id         Int?     @default(0)
  money1         Decimal  @default(0.00) @db.Decimal(11, 2)
  money2         Decimal  @default(0.00) @db.Decimal(11, 2)
  application_id Int      @default(0)
  dcp_status     Int      @default(0) @db.TinyInt
  account_name   String   @default("") @db.VarChar(30)
  bank           String?  @default("") @db.VarChar(255)
  bank_num       String?  @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_boss_special {
  id           Int     @id @default(autoincrement())
  user_id      Int
  app_id       Int
  service      Int
  end_time     Int     @default(0)
  remark       String  @default("") @db.VarChar(500)
  file_path    String  @default("") @db.VarChar(255)
  month_status Int     @default(0)
  status       Boolean @default(false)
  admin_id     Int     @default(0)
  create_time  Int
  update_time  Int

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_cases_link {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String  @default("") @db.VarChar(50)
  link_url       String  @default("") @db.VarChar(200)
  logo_url       String  @default("") @db.VarChar(200)
  create_time    Int     @default(0)
  release_time   Int     @default(0)
  admin_user_id  Int     @default(0)
  status         Boolean @default(true)
  handel_user_id Int     @default(0)
  sort           Int     @default(0) @db.TinyInt
}

model rcloud_cases_sort {
  id      Int    @id @default(autoincrement())
  content String @db.Text
  status  Int    @default(0) @db.TinyInt
  ctime   Int
  uid     Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ccmp_info {
  id               Int      @id @default(autoincrement())
  projectId        Int      @default(0)
  projectName      String   @db.VarChar(255)
  deliveryFileName String?  @db.VarChar(255)
  totalNum         Int?
  date             String?  @db.VarChar(20)
  type             Boolean? @default(false)
  json             String?  @db.LongText
  data             String?  @db.LongText
  create_time      Int?     @default(0)

  @@index([projectId], map: "projectId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_cdn_transcoding_stat {
  id          Int      @id @default(autoincrement())
  vendor      Int      @default(0) @db.TinyInt
  user_id     Int      @default(0)
  app_id      Int      @default(0)
  env         Boolean  @default(false)
  domains     String   @default("") @db.VarChar(5000)
  date        Int      @default(0)
  data        Json
  create_time DateTime @default(now()) @db.DateTime(0)
  update_time DateTime @default(now()) @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_certificate {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  title               String  @default("") @db.VarChar(50)
  bundle_id           String  @default("") @db.VarChar(200)
  file_test_name      String  @default("") @db.VarChar(255)
  file_test           String  @default("") @db.VarChar(200)
  voip_file_name      String  @default("") @db.VarChar(255)
  voip_file           String  @default("") @db.VarChar(200)
  create_time         Int     @default(0)
  app_id              Int     @default(0)
  user_id             Int     @default(0)
  file_product_name   String  @default("") @db.VarChar(255)
  file_product        String  @default("") @db.VarChar(200)
  package_name        String  @default("") @db.VarChar(200)
  oh_package_name     String  @default("") @db.VarChar(200)
  status              Boolean @default(false)
  production_password String  @default("") @db.VarChar(32)
  voip_password       String  @default("") @db.VarChar(32)
  dev_password        String  @default("") @db.VarChar(32)
  production_voice    String  @default("") @db.VarChar(50)
  dev_voice           String  @default("") @db.VarChar(50)
  dev_psuh_ret        Boolean @default(false)
  production_psuh_ret Boolean @default(false)
  push_user_id        String  @default("") @db.VarChar(50)
  push_set            String  @db.LongText
  push_dev_set        String  @db.LongText
  file_test_time      Int     @default(0)
  file_product_time   Int     @default(0)
  voip_time           Int     @default(0)
  last_updatetime     Int?    @default(0)
  ios_push_set        String  @default("") @db.VarChar(1000)
  ios_push_dev_set    String  @default("") @db.VarChar(1000)
  cer_env             String  @default("") @db.VarChar(2)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_certificate_bak {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  title               String  @default("") @db.VarChar(50)
  bundle_id           String  @default("") @db.VarChar(200)
  file_test_name      String  @default("") @db.VarChar(255)
  file_test           String  @default("") @db.VarChar(200)
  voip_file_name      String  @default("") @db.VarChar(255)
  voip_file           String  @default("") @db.VarChar(200)
  create_time         Int     @default(0)
  app_id              Int     @default(0)
  user_id             Int     @default(0)
  file_product_name   String  @default("") @db.VarChar(255)
  file_product        String  @default("") @db.VarChar(200)
  package_name        String  @default("") @db.VarChar(200)
  status              Boolean @default(false)
  production_password String  @default("") @db.VarChar(32)
  voip_password       String  @default("") @db.VarChar(32)
  dev_password        String  @default("") @db.VarChar(32)
  production_voice    String  @default("") @db.VarChar(50)
  dev_voice           String  @default("") @db.VarChar(50)
  dev_psuh_ret        Boolean @default(false)
  production_psuh_ret Boolean @default(false)
  push_user_id        String  @default("") @db.VarChar(50)
  push_set            String  @default("") @db.VarChar(200)
  push_dev_set        String  @default("") @db.VarChar(200)
  file_test_time      Int     @default(0)
  file_product_time   Int     @default(0)
  voip_time           Int     @default(0)
  last_updatetime     Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_certificate_bell {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int     @default(0) @db.UnsignedInt
  app_id       Int     @default(0) @db.UnsignedInt
  env          Int     @default(1) @db.UnsignedTinyInt
  status       Int     @default(1) @db.UnsignedTinyInt
  type         Int     @default(0) @db.UnsignedTinyInt
  create_time  String  @default("") @db.VarChar(15)
  update_time  String  @default("") @db.VarChar(15)
  config       String? @db.Text
  last_config  String? @db.Text
  channel_id   String? @default("") @db.VarChar(255)
  channel_name String? @default("") @db.VarChar(50)
  is_del       Int     @default(0) @db.UnsignedTinyInt

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_certificate_name_list {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int     @default(0)
  name          String  @default("") @db.VarChar(30)
  create_time   Int     @default(0)
  admin_user_id Int     @default(0)
  status        Boolean @default(false)
  num           Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_certificate_name_list_common {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int     @default(0)
  name          String  @default("") @db.VarChar(30)
  create_time   Int     @default(0)
  admin_user_id Int     @default(0)
  status        Boolean @default(false)
  num           Int     @default(0)
  app_id        Int?    @default(0)
  update_time   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_charge_package_switch_log {
  id               Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id          Int    @db.UnsignedInt
  app_id           Int    @db.UnsignedInt
  env              Int    @db.UnsignedTinyInt
  operation_log_id Int    @default(0) @db.UnsignedInt
  old_service_id   Int    @default(0) @db.UnsignedInt
  service_id       Int    @db.UnsignedInt
  status           Int    @default(1) @db.UnsignedTinyInt
  old_config       Json?
  new_config       Json?
  package_item     String @default("") @db.VarChar(500)
  switch_desc      String @default("") @db.VarChar(500)
  switch_result    String @default("") @db.VarChar(5000)
  created_at       Int    @db.UnsignedInt
  updated_at       Int    @db.UnsignedInt

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_charging_log_billing {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  type       String   @db.VarChar(45)
  orderNo    String?  @db.VarChar(45)
  userId     String   @default("") @db.VarChar(45)
  appId      Int?
  itemId     Int?
  service    String   @default("") @db.VarChar(45)
  money      Decimal  @default(0.00) @db.Decimal(11, 2)
  createTime DateTime @default(now()) @db.Timestamp(0)
  chargeDate Int?
  updateTime String   @default("") @db.VarChar(64)
  status     Int      @default(1) @db.TinyInt
  reason     String   @default("") @db.VarChar(64)

  @@index([userId], map: "IDX_userId")
  @@index([chargeDate], map: "chargeDate")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_charging_log_billing_record {
  id       Int      @id @default(autoincrement())
  bid      Int
  admin_id Int      @default(0)
  oldmoney Decimal  @default(0.00) @db.Decimal(11, 2)
  money    Decimal  @default(0.00) @db.Decimal(11, 2)
  ctime    DateTime @default(now()) @db.Timestamp(0)
  action   String   @default("") @db.VarChar(32)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rcloud_city {
  id        Float?
  name      String? @db.VarChar(384)
  ename     String? @db.VarChar(384)
  serial    String? @db.VarChar(384)
  sort      Int?
  parent_id Float?
  namecode  String? @db.VarChar(96)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_click_count {
  id      Int @id @default(autoincrement())
  type    Int @default(1)
  user_id Int
  ctime   Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_collection_url {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  url        String  @default("") @db.VarChar(255)
  sha1       String  @default("") @db.VarChar(255)
  remark     String? @db.VarChar(255)
  created_at Int     @default(0)
  updated_at Int     @default(0)
  deleted_at Int?

  @@index([sha1], map: "sha1")
  @@index([url], map: "url")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_com_app {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id       Int
  user_id      Int
  com_name     String  @default("") @db.VarChar(255)
  operation    Boolean @default(false)
  user_level   Boolean @default(false)
  apk_name     String  @default("") @db.VarChar(255)
  ios_name     String  @default("") @db.VarChar(255)
  web_site     String  @default("") @db.VarChar(255)
  android_site String  @default("") @db.VarChar(255)
  ios_site     String  @default("") @db.VarChar(255)
  status       Boolean @default(false)
  update_time  Int     @default(0)
  remark       String  @db.Text
  icon         String  @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_com_app_label {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  com_app_id Int
  label_id   Int
  status     Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_com_attachment {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  relevance_id Int     @default(0)
  user_id      Int     @default(0)
  type         Int     @default(0) @db.TinyInt
  path         String  @default("") @db.VarChar(150)
  create_time  Int     @default(0)
  update_time  Int     @default(0) @db.UnsignedInt
  suffix       String  @default("'") @db.Char(10)
  source_name  String  @default("") @db.VarChar(150)
  is_del       Boolean @default(false)
  remark       String  @default("") @db.VarChar(200)

  @@index([relevance_id], map: "relevance_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_com_crawler {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id       Int
  user_id      Int
  icon         String? @db.VarChar(255)
  package_name String  @default("") @db.VarChar(255)
  app_name     String  @default("") @db.VarChar(255)
  download_num BigInt  @default(0)
  size         Float   @default(0)
  app_type     String  @db.VarChar(255)
  version      String  @default("") @db.VarChar(255)
  version_time String  @default("") @db.VarChar(255)
  developer    String  @default("") @db.VarChar(255)
  info         String  @db.Text
  status       Int     @default(0) @db.TinyInt
  plat         Int     @default(0) @db.TinyInt
  other        String  @default("") @db.VarChar(2000)

  @@index([app_id], map: "app_id")
  @@index([package_name], map: "index_package_name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_com_info_links {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  com_app_id Int
  type       Boolean @default(false)
  title      String  @default("") @db.VarChar(255)
  link       String  @default("") @db.VarChar(255)
  status     Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_com_label_list {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  name        String  @db.VarChar(255)
  status      Boolean @default(false)
  admin_id    Int
  update_time Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_commands_log {
  id     Int    @id @default(autoincrement())
  info   String @db.VarChar(255)
  type   Int
  status Int    @db.TinyInt
  ctime  Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_company_info {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  uid             Int     @db.UnsignedInt
  name            String  @db.VarChar(45)
  address         String  @db.VarChar(45)
  business_no     String  @db.VarChar(45)
  organization_no String  @db.VarChar(45)
  tax_no          String  @db.VarChar(45)
  legal           String  @db.VarChar(45)
  tel             String  @db.VarChar(20)
  url             String  @db.VarChar(45)
  createtime      Int
  updatetime      Int
  status          Int     @db.TinyInt
  use_sms         Boolean @default(true)
  certify_type    String  @default("") @db.VarChar(200)
  file_idre       String? @default("") @db.VarChar(255)
  deny_reason     String  @default("") @db.VarChar(255)

  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_company_pay_info {
  id                           Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id                      Int
  app_id                       Int     @default(0)
  server                       Int     @default(0)
  remitter                     String  @default("默认账户") @db.VarChar(255)
  money                        Decimal @default(0.00) @db.Decimal(11, 2)
  file                         String  @default("") @db.VarChar(255)
  status                       Boolean @default(true)
  create_time                  Int     @default(0)
  recharge_time                String  @default("") @db.VarChar(255)
  audit_reason                 String  @default("") @db.VarChar(255)
  audit_admin_id               Int     @default(0)
  audit_time                   Int     @default(0)
  receive_account              Boolean @default(false)
  add_receive_account_admin_id Int     @default(0)
  add_receive_account_time     Int     @default(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_config {
  id     Int       @id @default(autoincrement())
  name   String    @db.VarChar(255)
  config String    @db.LongText
  time   DateTime? @db.Timestamp(0)
  remark String    @db.VarChar(255)

  @@index([name], map: "name")
}

model rcloud_contract {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int?
  money      Decimal? @db.Decimal(11, 2)
  start_time Int?
  end_time   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_cooperate_config {
  id          Int    @id @default(autoincrement())
  user_id     Int    @default(0)
  status      Int    @default(1)
  conf        String @default("") @db.VarChar(1024)
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_cooperate_name_list {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  ip                String  @default("") @db.Char(20)
  is_del            Boolean @default(false)
  admin_user_id     Int     @default(0)
  create_time       Int     @default(0)
  cooperate_user_id Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_cooperate_user {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  name              String  @default("") @db.VarChar(30)
  email             String  @default("") @db.Char(50)
  mobile            String  @default("") @db.Char(15)
  secret            String  @default("") @db.VarChar(32)
  create_time       Int     @default(0)
  create_user_id    Int     @default(0)
  developer_user_id Int     @default(0)
  app_key           String  @default("") @db.VarChar(20)
  is_third          Boolean @default(true)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_cooperater_customer {
  id             Int    @id @default(autoincrement())
  appKey         String @db.VarChar(32)
  customerUserId String @db.VarChar(64)
  user_type      Int    @db.TinyInt
  create_time    Int    @db.UnsignedInt
  status         Int    @db.TinyInt
}

model rcloud_cooperater_customer_page {
  id                     Int    @id @default(autoincrement()) @db.UnsignedInt
  cooperater_customer_id Int    @db.UnsignedInt
  page_id                String @db.VarChar(225)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_click {
  id          Int    @id @default(autoincrement())
  name        String @default("") @db.VarChar(255)
  count_click Int    @default(1) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type {
  id     Int     @id @default(autoincrement()) @db.UnsignedInt
  name   String? @default("") @db.VarChar(255)
  remark String? @default("") @db.VarChar(255)
  type   Boolean @default(true)
  title  String  @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type_dynamic {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  app_id        Int    @default(0)
  max_dynamic   BigInt
  avg_dynamic   BigInt
  month         Int
  create_time   Int
  month_dynamic BigInt @default(0)
  max_message   BigInt @default(0)
  avg_message   BigInt @default(0)
  five_dynamic  BigInt @default(0)

  @@index([app_id], map: "app_id")
  @@index([month], map: "month")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type_list {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  type_id     Int     @default(0)
  dimension   Boolean @default(true)
  total1      Int     @default(0)
  total2      Int     @default(0)
  total3      Float   @default(0.0000) @db.Float
  start       Int     @default(0)
  end         Int     @default(0)
  create_time Int     @default(0)

  @@index([dimension], map: "dimension")
  @@index([end], map: "end")
  @@index([start], map: "start")
  @@index([type_id], map: "type_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type_pay_list {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int     @default(0)
  app_id        Int     @default(0)
  sum_money     Float
  money         Float   @default(0.00)
  month         Int     @default(0)
  max_dynamic   Int     @default(0)
  avg_dynamic   BigInt  @default(0)
  count_message BigInt  @default(0)
  app_from      String  @default("") @db.VarChar(10)
  create_time   String  @default("") @db.VarChar(11)
  status        Boolean @default(false)
  month_dynamic BigInt  @default(0)

  @@index([app_id], map: "app_id")
  @@index([month], map: "month")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type_server {
  id      Int    @id @default(autoincrement())
  user_id Int    @default(0)
  app_id  Int    @default(0)
  server  Int    @default(0)
  month   Int    @default(0)
  var1    String @default("") @db.VarChar(255)
  var2    String @default("") @db.VarChar(255)
  var3    String @default("") @db.VarChar(255)
  var4    String @default("") @db.VarChar(255)
  var5    String @default("") @db.VarChar(255)
  var6    String @default("") @db.VarChar(255)
  var7    String @default("") @db.VarChar(255)
  var8    String @default("") @db.VarChar(255)

  @@index([app_id], map: "app_id")
  @@index([month], map: "month")
  @@index([server], map: "server")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type_server_detail {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int    @default(0)
  app_id      Int    @default(0)
  server      Int    @default(0)
  week_max    Int    @default(0)
  week_avg    Int    @default(0)
  week_sum    BigInt @default(0)
  month_max   Int    @default(0)
  month_avg   Int    @default(0)
  month_sum   BigInt @default(0)
  create_time Int    @default(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_count_type_top {
  id     Int    @id @default(autoincrement())
  app_id Int
  type   Int    @default(0) @db.TinyInt
  start  Int    @default(0)
  end    Int    @default(0)
  rank   Int    @default(0) @db.TinyInt
  total  BigInt @default(0)
  avg    BigInt @default(0)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_country_code {
  id   Int    @id @default(autoincrement())
  code Int    @default(0) @db.UnsignedInt
  name String @default("") @db.VarChar(50)
}

model rcloud_course {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  parent_id  Int    @default(0) @db.UnsignedInt
  name       String @db.VarChar(255)
  describe   String @db.VarChar(255)
  url        String @db.VarChar(255)
  length     Int    @default(0) @db.UnsignedInt
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

model rcloud_course_relation {
  id         Int  @id @default(autoincrement()) @db.UnsignedInt
  course_id  Int  @default(0) @db.UnsignedInt
  user_id    Int  @default(0) @db.UnsignedInt
  status     Int  @default(0) @db.UnsignedTinyInt
  created_at Int  @default(0) @db.UnsignedInt
  updated_at Int  @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_activity_import_user {
  id               Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id          Int    @default(0)
  import_content   String @default("") @db.VarChar(256)
  follow_content   String @default("") @db.VarChar(256)
  follow_file_url  String @default("") @db.VarChar(256)
  next_follow_time String @default("") @db.VarChar(256)
  state            Int    @default(0) @db.TinyInt
  is_del           Int    @default(0) @db.TinyInt
  create_by        Int    @default(0)
  update_by        Int    @default(0)
  create_time      Int    @default(0)
  update_time      Int    @default(0)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_city {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  city_name   String @default("") @db.VarChar(50)
  province_id Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue {
  id                     Int     @id @default(autoincrement()) @db.UnsignedInt
  uid                    Int     @default(0)
  p_uid                  Int     @default(0)
  customer_type          Int     @default(0) @db.TinyInt
  customer               String  @default("") @db.VarChar(200)
  certify_type           Int     @default(0) @db.TinyInt
  customer_trades        Int     @default(0) @db.TinyInt
  area_id                Int     @default(0)
  pool_province          Int     @default(0)
  customer_cityid        Int     @default(0)
  customer_city          String  @default("") @db.VarChar(20)
  contacts               String  @default("") @db.VarChar(20)
  contact_phone          String  @default("") @db.VarChar(16)
  phone                  String  @default("") @db.Char(11)
  wechat                 String  @default("") @db.VarChar(50)
  email                  String  @default("") @db.VarChar(100)
  clue_source            Int     @default(0) @db.TinyInt
  search_keyword         String  @default("") @db.VarChar(20)
  intentio_product       String  @default("") @db.VarChar(10)
  is_business_move       Int     @default(0) @db.TinyInt
  im_business            String  @default("") @db.VarChar(50)
  rce_business           String  @default("") @db.VarChar(50)
  use_scene              String  @default("") @db.VarChar(255)
  project_name           String  @default("") @db.VarChar(50)
  remarks                String  @default("") @db.VarChar(255)
  expand_status          Int     @default(0) @db.TinyInt
  allot_time             Int     @default(0)
  expire_endtime         Int     @default(0)
  end_record_time        Int     @default(0)
  limit_record_time      Int     @default(0)
  bind_time              Int     @default(0)
  ignore_by              Int     @default(0)
  ignore_date            Int     @default(0)
  sales_id               Int     @default(0)
  sales_name             String  @default("") @db.VarChar(50)
  status                 Int     @default(0) @db.TinyInt
  create_id              Int     @default(0)
  create_time            Int     @default(0)
  give_up_time           Int     @default(0)
  update_id              Int     @default(0)
  update_time            Int     @default(0)
  belong_to              Boolean @default(false)
  confirm_time           Int     @default(0)
  online_time            Int     @default(0)
  next_follow_time       String  @default("") @db.VarChar(32)
  app_expect_online_time Int     @default(0)
  display_date           String  @default("") @db.VarChar(64)
  display_num            Int     @default(0) @db.UnsignedInt
  is_long_time           Int     @default(0) @db.TinyInt

  @@index([phone], map: "phone")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_call_statistic {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  date        String  @default("") @db.VarChar(15)
  admin_id    Int     @default(0) @db.UnsignedInt
  source      Int     @default(0) @db.TinyInt
  config      String  @db.LongText
  is_del      Boolean @default(false)
  remark      String  @default("") @db.VarChar(200)
  create_time Int     @default(0)
  update_time Int     @default(0)

  @@index([date], map: "date_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_check {
  id               Int      @id @default(autoincrement()) @db.UnsignedInt
  clue_id          Int      @default(0)
  user_id          Int      @default(0)
  clue_expire_date DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  delay_date       DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  delay_reason     String   @default("") @db.VarChar(512)
  status           Int      @default(1) @db.TinyInt
  check_status     Int      @default(0) @db.TinyInt
  check_by         Int      @default(0)
  check_date       DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  b_check_status   Int      @default(0) @db.TinyInt
  b_check_by       Int      @default(0)
  b_check_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  create_by        Int      @default(0) @db.UnsignedInt
  update_by        Int      @default(0) @db.UnsignedInt
  create_time      DateTime @default(now()) @db.Timestamp(0)
  update_time      DateTime @default(now()) @db.Timestamp(0)

  @@index([clue_id], map: "idx_clue_id")
  @@index([create_by], map: "idx_create_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_contacts {
  id              Int    @id @default(autoincrement()) @db.UnsignedInt
  clue_id         Int    @default(0)
  contacts_name   String @default("") @db.VarChar(20)
  contacts_phone  String @default("") @db.Char(11)
  contacts_wechat String @default("") @db.VarChar(50)
  create_id       Int    @default(0)
  create_time     Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_extend {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  type             Int     @default(0) @db.TinyInt
  clue_id          Int     @default(0)
  expand_channel   Int     @default(0)
  value            String? @default("") @db.VarChar(2000)
  value1           String  @default("") @db.VarChar(255)
  status           Boolean @default(false)
  create_id        Int     @default(0)
  create_time      Int     @default(0)
  update_id        Int     @default(0)
  update_time      Int     @default(0)
  proof_time       Int     @default(0)
  next_follow_time String  @default("") @db.VarChar(32)
  proof_userid     Int     @default(0)
  auth_time        Int     @default(0)
  auth_userid      Int     @default(0)
  is_del           Int     @default(0) @db.TinyInt
  value_2          String  @default("0") @db.VarChar(255)
  value_3          String  @default("0") @db.VarChar(255)
  value_4          String  @default("0") @db.VarChar(255)

  @@index([clue_id], map: "index_clue_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_extend_copy {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  type             Int     @default(0) @db.TinyInt
  clue_id          Int     @default(0)
  expand_channel   Int     @default(0)
  value            String  @default("") @db.VarChar(255)
  value1           String  @default("") @db.VarChar(255)
  status           Boolean @default(false)
  create_id        Int     @default(0)
  create_time      Int     @default(0)
  update_id        Int     @default(0)
  update_time      Int     @default(0)
  proof_time       Int     @default(0)
  next_follow_time String  @default("") @db.VarChar(32)
  proof_userid     Int     @default(0)
  auth_time        Int     @default(0)
  auth_userid      Int     @default(0)
  is_del           Int     @default(0) @db.TinyInt
  value_2          String  @default("0") @db.VarChar(255)
  value_3          String  @default("0") @db.VarChar(255)
  value_4          String  @default("0") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_extend_pic {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  extend_id Int    @default(0)
  url       String @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_follow_log {
  id              Int    @id @default(autoincrement()) @db.UnsignedInt
  clue_id         Int
  admin_user_id   Int
  admin_user_name String @default("") @db.VarChar(50)
  source          Int    @db.TinyInt
  type            Int    @db.TinyInt
  create_time     Int
  create_id       Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_follow_statistics {
  id                   Int    @id @default(autoincrement()) @db.UnsignedInt
  admin_user_id        Int    @default(0)
  admin_user_name      String @default("") @db.VarChar(50)
  date                 String @default("") @db.VarChar(20)
  total                Int    @default(0)
  confirm_total        Int    @default(0)
  follow_total         Int    @default(0)
  give_up_total        Int    @default(0)
  public_total         Int    @default(0)
  public_confirm_total Int    @default(0)
  public_follow_total  Int    @default(0)
  public_give_up_total Int    @default(0)
  online_total         Int    @default(0)
  create_time          Int    @default(0)

  @@index([admin_user_id], map: "admin_user_id")
  @@index([date], map: "date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_import_task {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  name        String   @default("") @db.VarChar(256)
  status      Int      @default(1) @db.TinyInt
  create_by   Int      @default(0) @db.UnsignedInt
  update_by   Int      @default(0) @db.UnsignedInt
  create_time DateTime @default(now()) @db.Timestamp(0)
  update_time DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_import_task_config {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  task_id     Int      @default(0)
  column      String   @default("") @db.VarChar(256)
  column_val  String   @default("") @db.VarChar(256)
  status      Int      @default(1) @db.TinyInt
  create_by   Int      @default(0) @db.UnsignedInt
  update_by   Int      @default(0) @db.UnsignedInt
  create_time DateTime @default(now()) @db.Timestamp(0)
  update_time DateTime @default(now()) @db.Timestamp(0)

  @@index([task_id], map: "idx_task_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_import_task_item {
  id                     Int      @id @default(autoincrement()) @db.UnsignedInt
  task_id                Int      @default(0)
  user_id                Int      @default(0)
  sale_id                Int      @default(0)
  app_key                String   @default("") @db.VarChar(256)
  item_config            String   @default("") @db.VarChar(512)
  customer_type          Int      @default(0) @db.TinyInt
  is_connect             Int      @default(0) @db.TinyInt
  is_effective           Int      @default(0) @db.TinyInt
  is_sale                Int      @default(0) @db.TinyInt
  is_business_follow     Int      @default(0) @db.TinyInt
  is_business_ok         Int      @default(0) @db.TinyInt
  belong_to              Int      @default(0) @db.TinyInt
  success_connect_result String   @default("") @db.VarChar(256)
  fail_connect_reason    String   @default("") @db.VarChar(256)
  follow_content         String   @default("") @db.VarChar(256)
  remark                 String   @default("") @db.VarChar(256)
  task_config            String   @default("") @db.VarChar(512)
  status                 Int      @default(1) @db.TinyInt
  visit_status           Int      @default(1) @db.TinyInt
  visit_time             DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  visit_by               Int      @default(0)
  create_by              Int      @default(0) @db.UnsignedInt
  update_by              Int      @default(0) @db.UnsignedInt
  create_time            DateTime @default(now()) @db.Timestamp(0)
  update_time            DateTime @default(now()) @db.Timestamp(0)

  @@index([sale_id], map: "idx_sale_id")
  @@index([task_id], map: "idx_task_id")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_log {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  operate     String @default("") @db.VarChar(30)
  old_data    String @db.Text
  new_data    String @db.Text
  create_id   Int    @default(0)
  create_time Int    @default(0)
  remark      String @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_map_tag {
  id          Int @id @default(autoincrement()) @db.UnsignedInt
  clue_id     Int @default(0)
  user_tag_id Int @default(0)
  is_del      Int @default(0) @db.TinyInt
  create_by   Int @default(0) @db.UnsignedInt
  update_by   Int @default(0) @db.UnsignedInt
  create_time Int @default(0)
  update_time Int @default(0)

  @@index([clue_id], map: "idx_clue_id")
  @@index([user_tag_id], map: "idx_user_tag_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_permission {
  id                   Int    @id @default(autoincrement()) @db.UnsignedInt
  admin_user_id        Int    @default(0)
  sales_name           String @default("") @db.VarChar(50)
  sale_group_id        Int    @default(0)
  area_id              Int    @default(0)
  area_name            String @default("") @db.VarChar(30)
  position             Int    @default(0) @db.TinyInt
  business_line        Int    @default(0) @db.TinyInt
  clue_count           Int    @default(0)
  public_clue_count    Int    @default(0)
  status               Int    @default(0) @db.TinyInt
  create_id            Int    @default(0)
  create_time          Int    @default(0)
  update_id            Int
  update_time          Int
  clue_surplus         Int    @default(0)
  allot_area_time      Int    @default(0)
  holiday_clue_surplus Int    @default(0)
  consult_clue_surplus Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_pool {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id             Int     @default(0) @db.UnsignedInt
  sales_id            Int     @default(0) @db.UnsignedInt
  source              Int     @default(0) @db.UnsignedTinyInt
  email               String  @default("") @db.VarChar(60)
  mobile              String  @default("") @db.VarChar(50)
  belong_to           Int     @default(0) @db.TinyInt
  customer_type       Int     @default(0) @db.TinyInt
  province            Int     @default(0)
  area_id             Int     @default(0)
  city_id             Int     @default(0)
  contact_name        String  @default("") @db.VarChar(50)
  contact_phone       String  @default("") @db.VarChar(16)
  customer_name       String  @default("") @db.VarChar(100)
  position            String  @default("") @db.VarChar(30)
  search_link         String  @default("") @db.VarChar(255)
  search_keyword      String  @default("") @db.VarChar(100)
  register_method     Boolean @default(false)
  create_time         Int     @default(0)
  update_time         Int     @default(0)
  status              Boolean @default(true)
  contact_count       Int     @default(0) @db.TinyInt
  sale_contact_count  Int     @default(0) @db.TinyInt
  is_intention        Boolean @default(false)
  no_intention_reason Boolean @default(false)
  remark              String  @default("") @db.VarChar(255)
  clue_id             Int     @default(0)
  activity_name       String  @default("") @db.VarChar(100)
  activity_time       String  @default("") @db.VarChar(30)
  admin_id            Int     @default(0)
  admin_time          Int     @default(0)
  register_url        String  @default("") @db.VarChar(255)
  start_url           String  @default("") @db.VarChar(255)
  is_del              Boolean @default(false)
  config              String? @db.LongText
  no_contact_reason   Boolean @default(false)
  allot_id            Int?    @default(0)
  is_holiday          Int     @default(0) @db.TinyInt

  @@index([email], map: "index_email")
  @@index([mobile], map: "index_mobile")
  @@index([user_id], map: "index_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_pool_import {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  clue_pool_id Int @default(0)
  create_time  Int @default(0)

  @@index([clue_pool_id], map: "idx_clue_pool_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_pool_manager {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  allot_id     Int     @default(0)
  admin_id     Int     @default(0)
  clue_count   Int     @default(0) @db.UnsignedInt
  clue_surplus Int     @default(0)
  create_time  Int?    @default(0)
  update_time  Int?    @default(0)
  allot_time   Int     @default(0)
  is_del       Boolean @default(false)
  remark       String? @default("") @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_clue_receive_record {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  clue_id      Int      @default(0) @db.UnsignedInt
  receive_time DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  receive_by   Int      @default(0) @db.UnsignedInt
  status       Int      @default(1) @db.TinyInt
  create_time  DateTime @default(now()) @db.Timestamp(0)
  update_time  DateTime @default(now()) @db.Timestamp(0)

  @@index([clue_id], map: "idx_clue_id")
  @@index([receive_by], map: "idx_receive_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_dashboard {
  id                           Int      @id @default(autoincrement()) @db.UnsignedInt
  day                          Int      @default(0)
  clue_pool_total_count        Int      @default(0)
  clue_pool_intention_count    Int      @default(0)
  clue_pool_no_intention_count Int      @default(0)
  clue_pool_wait_handle_count  Int      @default(0)
  clue_total_count             Int      @default(0)
  clue_im_count                Int      @default(0)
  clue_rtc_count               Int      @default(0)
  clue_im_rtc_count            Int      @default(0)
  clue_other_count             Int      @default(0)
  clue_recharge_count          Int      @default(0)
  type                         Int      @default(0) @db.TinyInt
  status                       Int      @default(1) @db.TinyInt
  create_by                    Int      @default(0) @db.UnsignedInt
  update_by                    Int      @default(0) @db.UnsignedInt
  create_time                  DateTime @default(now()) @db.Timestamp(0)
  update_time                  DateTime @default(now()) @db.Timestamp(0)

  @@index([day], map: "idx_day")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_province {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  province_name String @default("") @db.VarChar(50)
  pid           Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_group {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  pid         Int    @default(0)
  code        String @unique(map: "uniq_code") @default("") @db.VarChar(32)
  name        String @default("") @db.VarChar(32)
  sort        Int    @default(0) @db.TinyInt
  is_del      Int    @default(2) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_import_task {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  name        String @default("") @db.VarChar(256)
  is_del      Int    @default(0) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_import_task_item {
  id                      Int    @id @default(autoincrement()) @db.UnsignedInt
  crm_sale_import_task_id Int    @default(0)
  user_id                 Int    @default(0)
  import_content          String @default("") @db.VarChar(512)
  follow_content          String @default("") @db.VarChar(256)
  follow_file_url         String @default("") @db.VarChar(256)
  next_follow_time        Int    @default(0)
  state                   Int    @default(0) @db.TinyInt
  is_del                  Int    @default(0) @db.TinyInt
  create_by               Int    @default(0)
  update_by               Int    @default(0)
  create_time             Int    @default(0)
  update_time             Int    @default(0)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_pre_support_record {
  id                 Int    @id @default(autoincrement()) @db.UnsignedInt
  type               Int    @default(0) @db.TinyInt
  user_id            Int    @default(0)
  app_id             Int    @default(0)
  assign_sale_pre_by Int    @default(0)
  use_scene          String @default("") @db.VarChar(32)
  follow_content     String @default("") @db.VarChar(256)
  follow_time        Int    @default(0)
  state              Int    @default(0) @db.TinyInt
  is_del             Int    @default(2) @db.TinyInt
  create_by          Int    @default(0)
  update_by          Int    @default(0)
  create_time        Int    @default(0)
  update_time        Int    @default(0)

  @@index([app_id], map: "idx_app_id")
  @@index([assign_sale_pre_by], map: "idx_assign_sale_pre_by")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_pre_support_record_file {
  id                             Int    @id @default(autoincrement()) @db.UnsignedInt
  crm_sale_pre_support_record_id Int    @default(0)
  file_url                       String @default("") @db.VarChar(256)
  is_del                         Int    @default(2) @db.TinyInt
  create_by                      Int    @default(0)
  update_by                      Int    @default(0)
  create_time                    Int    @default(0)
  update_time                    Int    @default(0)

  @@index([crm_sale_pre_support_record_id], map: "idx_crm_sale_pre_support_record_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_score_record {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  sale_id     Int      @default(0)
  user_id     Int      @default(0)
  score       Int      @default(0)
  handle_date Int      @default(0)
  type        Int      @default(0) @db.TinyInt
  status      Int      @default(1) @db.TinyInt
  create_by   Int      @default(0) @db.UnsignedInt
  update_by   Int      @default(0) @db.UnsignedInt
  create_time DateTime @default(now()) @db.Timestamp(0)
  update_time DateTime @default(now()) @db.Timestamp(0)

  @@index([handle_date], map: "idx_handle_date")
  @@index([sale_id], map: "idx_sale_id")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_target {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  sale_id      Int      @default(0)
  month        Int      @default(0)
  target_money Int      @default(0)
  status       Int      @default(1) @db.TinyInt
  create_by    Int      @default(0) @db.UnsignedInt
  update_by    Int      @default(0) @db.UnsignedInt
  create_time  DateTime @default(now()) @db.Timestamp(0)
  update_time  DateTime @default(now()) @db.Timestamp(0)

  @@index([month], map: "idx_month")
  @@index([sale_id], map: "idx_sale_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_sale_visit_item {
  id                Int @id @default(autoincrement()) @db.UnsignedInt
  type              Int @default(0) @db.TinyInt
  user_id           Int @default(0)
  open_service_time Int @default(0)
  open_im_time      Int @default(0)
  is_del            Int @default(2) @db.TinyInt
  create_by         Int @default(0)
  update_by         Int @default(0)
  create_time       Int @default(0)
  update_time       Int @default(0)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_user_change_record {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int    @default(0)
  clue_id      Int    @default(0)
  clue_pool_id Int    @default(0)
  op_name      String @default("") @db.VarChar(256)
  op_content   String @default("") @db.VarChar(256)
  create_by    Int    @default(0) @db.UnsignedInt
  create_time  Int    @default(0)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_crm_user_tag {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  name        String @default("") @db.VarChar(256)
  color       String @default("") @db.VarChar(256)
  is_del      Int    @default(0) @db.TinyInt
  create_by   Int    @default(0) @db.UnsignedInt
  update_by   Int    @default(0) @db.UnsignedInt
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_clue {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  name        String @default("") @db.VarChar(200)
  user_id     Int    @default(0) @db.UnsignedInt
  sale_id     Int    @default(0) @db.UnsignedInt
  admin_id    Int    @default(0) @db.UnsignedInt
  create_time Int    @db.UnsignedInt
  update_time Int    @db.UnsignedInt
  expire_time String @default("") @db.VarChar(15)
  config      String @db.Text
  is_del      Int    @default(0) @db.UnsignedTinyInt
  remark      String @default("") @db.VarChar(255)
  dcp_id      Int    @default(0) @db.UnsignedInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_expect_recharge {
  id                    Int      @id @default(autoincrement()) @db.UnsignedInt
  sale_id               Int      @default(0)
  expect_recharge_money Int      @default(0)
  target_money          Int      @default(0)
  execute_date          Int      @default(0)
  status                Int      @default(1) @db.TinyInt
  create_by             Int      @default(0) @db.UnsignedInt
  update_by             Int      @default(0) @db.UnsignedInt
  create_time           DateTime @default(now()) @db.Timestamp(0)
  update_time           DateTime @default(now()) @db.Timestamp(0)

  @@index([execute_date], map: "idx_execute_date")
  @@index([sale_id], map: "idx_sale_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_follow {
  id            Int      @id @default(autoincrement()) @db.UnsignedInt
  customerid    Int?
  adminid       Int?
  appid         Int?
  appname       String?  @db.VarChar(128)
  appservice    Int?     @db.TinyInt
  contracttype  Boolean?
  contractpay   String?  @db.VarChar(32)
  contractterm  Int?     @db.TinyInt
  contractstime String?  @db.VarChar(32)
  contractetime String?  @db.VarChar(32)
  industry      String?  @db.VarChar(128)
  phone         String?  @db.VarChar(32)
  function      String?  @db.VarChar(128)
  other         String?  @db.VarChar(128)
  contracurl    String?  @db.VarChar(128)
  createtime    Int?

  @@index([customerid], map: "customer_follow")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_identify {
  id          Int     @id @default(autoincrement())
  user_id     Int     @default(0)
  flow_id     String  @default("") @db.VarChar(60)
  link        String  @default("") @db.VarChar(1000)
  name        String  @default("") @db.VarChar(200)
  type        Boolean @default(true)
  id_number   String  @default("") @db.VarChar(60)
  legal       String  @default("") @db.VarChar(40)
  status      Int     @default(1) @db.TinyInt
  create_time Int     @default(0)
  update_time Int     @default(0)
  admin_id    Int     @default(0)
  admin_time  Int     @default(0)
  is_del      Boolean @default(false)
  reason      String  @default("") @db.VarChar(200)
  remark      String  @default("") @db.VarChar(200)
  config      String? @db.Text

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_management {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  email             String? @db.VarChar(128)
  userId            Int?
  mark              String? @db.VarChar(128)
  remark            String? @db.VarChar(255)
  adminId           Int?
  admin_proportion  Decimal @default(0.00) @db.Decimal(8, 2)
  status            Int?
  createtime        Int?
  updatetime        Int?
  secondary_id      Int?    @default(0)
  second_proportion Decimal @default(0.00) @db.Decimal(8, 2)
  secondary_time    String? @default("") @db.VarChar(15)

  @@index([adminId], map: "adminId")
  @@index([userId], map: "customer_management")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_management_log {
  id                     Int     @id @default(autoincrement()) @db.UnsignedInt
  customer_management_id Int     @default(0)
  user_id                Int     @default(0)
  manager_id             Int     @default(0)
  admin_proportion       Decimal @default(0.00) @db.Decimal(8, 2)
  remark                 String  @default("") @db.VarChar(255)
  ctime                  Int     @default(0)
  secondary_id           Int?    @default(0)
  second_proportion      Decimal @default(0.00) @db.Decimal(8, 2)
  secondary_time         String? @default("") @db.VarChar(15)
  main_time              String? @default("") @db.VarChar(15)
  operate_id             Int     @default(0)
}

model rcloud_customer_tags {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  name       String @default("") @db.VarChar(10)
  user_id    Int    @default(0) @db.UnsignedInt
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_url {
  id       Int    @id @default(autoincrement())
  app_key  String @default("") @db.VarChar(255)
  app_id   Int    @default(0)
  env      Int    @default(0)
  user_id  Int    @default(0)
  admin_id Int    @default(0)
  csType   String @default("") @db.VarChar(255)
  csUrl    String @default("") @db.VarChar(255)
  ctime    Int    @default(0)
  status   Int    @default(1)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_customer_vfour {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  uid                 Int     @db.UnsignedInt
  user_id             String  @db.VarChar(64)
  email               String  @db.VarChar(64)
  email_validated     Int     @db.TinyInt
  third_part          Int     @db.TinyInt
  third_part_registed Int     @default(0) @db.TinyInt
  app_id              Int     @db.UnsignedInt
  app_key             String  @db.VarChar(32)
  environment         Int     @db.TinyInt
  kefu_key            String  @db.VarChar(20)
  name                String  @db.VarChar(64)
  img                 String  @db.VarChar(128)
  status              Int     @db.TinyInt
  mobile              String  @db.VarChar(11)
  create_time         Int     @db.UnsignedInt
  change_email        String  @default("") @db.VarChar(128)
  success             Boolean @default(false)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_data_center_config {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  name        String  @default("") @db.VarChar(50)
  show_name   String  @default("") @db.VarChar(50)
  overseas    Boolean @default(true)
  show        Boolean @default(true)
  is_replace  Boolean @default(false)
  admin_id    Int     @default(0)
  remark      String  @default("") @db.VarChar(100)
  idc         String  @default("") @db.VarChar(100)
  env_id      String? @db.VarChar(100)
  create_time Int     @default(0)
  update_time Int     @default(0)
  is_del      Boolean @default(false)
  config      String? @db.Text
  expand      String  @default("") @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_dcp_info {
  id            Int      @id @default(autoincrement())
  type          Int      @default(0)
  user_id       Int
  app_id        Int
  app_key       String   @db.VarChar(255)
  create_time   Int
  update_time   Int
  status        Int      @default(2) @db.TinyInt
  request       String   @db.Text
  result        String   @db.Text
  other_config  String   @db.Text
  approval_type Int      @default(1) @db.UnsignedTinyInt
  dcp_id        Int
  is_del        Boolean  @default(false)
  run_status    Boolean? @default(false)
  run_result    String   @db.Text
  original_data String   @db.Text

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_demo_user_experience {
  id              Int    @id @default(autoincrement()) @db.UnsignedInt
  demo_type       Int    @default(0) @db.TinyInt
  mobile          String @db.VarChar(11)
  channel         String @default("") @db.VarChar(32)
  client_type     String @default("") @db.VarChar(32)
  version         String @default("") @db.VarChar(32)
  is_verify       Int    @default(0) @db.UnsignedTinyInt
  is_developer    Int    @default(0) @db.UnsignedTinyInt
  register_time   String @default("") @db.VarChar(12)
  experience_time String @default("") @db.VarChar(12)
  create_time     String @default("") @db.VarChar(12)
  update_time     String @default("") @db.VarChar(12)
  is_del          Int    @default(0) @db.UnsignedTinyInt
  country         String @default("中国") @db.VarChar(30)

  @@index([experience_time], map: "experience_time")
  @@index([mobile], map: "index_1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_demos {
  id            Int    @id @default(autoincrement())
  site          Int    @default(1) @db.UnsignedTinyInt
  type          Int    @default(1) @db.UnsignedTinyInt
  name          String @default("") @db.VarChar(128)
  version       String @default("") @db.VarChar(32)
  description   String @db.Text
  comment       String @db.Text
  is_new        Int    @default(0) @db.UnsignedTinyInt
  is_mobile     Int    @default(0) @db.UnsignedTinyInt
  client        String @db.Text
  download_list String @db.Text
  qrcode_list   String @db.Text
  source_list   String @db.Text
  business_type Int    @default(0) @db.TinyInt
  image         String @default("") @db.VarChar(500)
  tags          String @default("") @db.VarChar(500)
  status        Int    @default(0) @db.UnsignedTinyInt
  sort          Int    @default(0) @db.UnsignedTinyInt
  admin_id      Int    @default(0) @db.UnsignedInt
  created_at    Int    @default(0)
  updated_at    Int    @default(0)
  deleted_at    Int?
  icon_list     String @db.Text
  img_list      String @db.Text

  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_demos_copy {
  id            Int    @id @default(autoincrement())
  site          Int    @default(1) @db.UnsignedTinyInt
  type          Int    @default(1) @db.UnsignedTinyInt
  name          String @default("") @db.VarChar(128)
  version       String @default("") @db.VarChar(32)
  description   String @db.Text
  comment       String @db.Text
  is_new        Int    @default(0) @db.UnsignedTinyInt
  is_mobile     Int    @default(0) @db.UnsignedTinyInt
  client        String @db.Text
  download_list String @db.Text
  qrcode_list   String @db.Text
  source_list   String @db.Text
  business_type Int    @default(0) @db.TinyInt
  image         String @default("") @db.VarChar(500)
  tags          String @default("") @db.VarChar(500)
  status        Int    @default(0) @db.UnsignedTinyInt
  sort          Int    @default(0) @db.UnsignedTinyInt
  admin_id      Int    @default(0) @db.UnsignedInt
  created_at    Int    @default(0)
  updated_at    Int    @default(0)
  deleted_at    Int?
  icon_list     String @db.Text
  img_list      String @db.Text

  @@index([name], map: "name")
}

model rcloud_developer_email {
  id        Int     @id @default(autoincrement()) @db.UnsignedInt
  email     String? @db.VarChar(128)
  emailtype Int?    @db.TinyInt
  time      Int?

  @@index([emailtype], map: "ix_emailtype")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_developer_exercise {
  id                Int    @id @default(autoincrement()) @db.UnsignedInt
  team_name         String @default("") @db.VarChar(50)
  mobile            String @default("0") @db.Char(15)
  email             String @default("") @db.VarChar(50)
  qq                String @default("0") @db.Char(15)
  qingcloud_account String @default("") @db.VarChar(50)
  coding_account    String @default("") @db.VarChar(50)
  create_time       Int    @default(0)
  exercise_id       Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_discount_code {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  code     String  @unique(map: "code") @db.VarChar(32)
  user_id  Int     @default(0)
  discount Float   @default(9) @db.Float
  amount   Int     @default(0) @db.UnsignedInt
  start    String  @default("20170901") @db.VarChar(32)
  end      String  @default("20180831") @db.VarChar(32)
  type     Int     @default(1)
  status   Int     @default(1)
  day      Int     @default(0)
  config   String? @default("") @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_discount_user {
  id         Int      @id @default(autoincrement())
  cate       Int      @default(1)
  user_id    Int
  app_id     Int      @default(0)
  status     Int      @default(1)
  start      String   @default("20170901") @db.VarChar(16)
  end        String   @default("20180831") @db.VarChar(16)
  discount   Float?   @default(1) @db.Float
  createtime DateTime @default(now()) @db.Timestamp(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rcloud_dm_mobile {
  ID           Int?
  MobileNumber Int?
  MobileArea   String? @db.VarChar(150)
  MobileType   String? @db.VarChar(150)
  AreaCode     String? @db.VarChar(30)
  PostCode     String? @db.VarChar(150)

  @@index([MobileNumber], map: "rcloud_dm_mobile_mobilenumber")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_email_queue {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  title             String  @default("") @db.VarChar(200)
  content           String? @db.MediumText
  email             String  @default("") @db.VarChar(200)
  status            Boolean @default(false)
  create_time       Int     @default(0)
  send_time         Int     @default(0)
  attachment        String  @default("") @db.VarChar(255)
  email_template_id Int     @default(0)
  type              String  @default("") @db.VarChar(10)

  @@index([create_time], map: "create_time")
  @@index([email], map: "index_email")
  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_email_queue_2023 {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  title             String  @default("") @db.VarChar(200)
  content           String? @db.MediumText
  email             String  @default("") @db.VarChar(200)
  status            Boolean @default(false)
  create_time       Int     @default(0)
  send_time         Int     @default(0)
  attachment        String  @default("") @db.VarChar(255)
  email_template_id Int     @default(0)
  type              String  @default("") @db.VarChar(10)

  @@index([create_time], map: "create_time")
  @@index([email], map: "index_email")
  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_enterprise_wechat_department {
  id            Int      @id @default(autoincrement())
  department_id Int      @default(0)
  name          String   @default("") @db.VarChar(255)
  parentid      Int      @default(0)
  order         String   @default("") @db.VarChar(50)
  status        Boolean  @default(true)
  create_time   DateTime @default(now()) @db.DateTime(0)
  update_time   DateTime @default(now()) @db.DateTime(0)

  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_enterprise_wechat_department_user {
  id              Int      @id @default(autoincrement())
  department_name String   @default("") @db.VarChar(255)
  userid          String   @default("") @db.VarChar(255)
  name            String   @default("") @db.VarChar(255)
  mobile          Int      @default(0)
  nick_name       String   @default("") @db.VarChar(255)
  status          Boolean  @default(true)
  create_time     DateTime @default(now()) @db.DateTime(0)
  update_time     DateTime @default(now()) @db.DateTime(0)

  @@index([status], map: "status")
  @@index([userid(length: 191)], map: "userid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_enterprise_wechat_group_chat_list {
  id                Int      @id @default(autoincrement())
  chat_id           String   @default("") @db.VarChar(255)
  follow_up_status  Int      @default(0) @db.UnsignedTinyInt
  name              String   @default("") @db.VarChar(255)
  owner             String   @default("") @db.VarChar(255)
  group_create_time Int      @default(0)
  notice            String   @default("") @db.VarChar(1000)
  type              Boolean  @default(true)
  status            Boolean  @default(true)
  create_time       DateTime @default(now()) @db.DateTime(0)
  update_time       DateTime @default(now()) @db.DateTime(0)

  @@index([chat_id(length: 191)], map: "chat_id")
  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_enterprise_wechat_group_chat_user_list {
  id             Int      @id @default(autoincrement())
  chat_id        String   @default("") @db.VarChar(255)
  userid         String   @default("") @db.VarChar(255)
  type           Int      @default(1)
  join_time      Int      @default(1)
  join_scene     Int      @default(1)
  group_nickname String   @default("") @db.VarChar(255)
  status         Boolean  @default(true)
  create_time    DateTime @default(now()) @db.DateTime(0)
  update_time    DateTime @default(now()) @db.DateTime(0)

  @@index([chat_id(length: 191)], map: "chat_id")
  @@index([status], map: "status")
  @@index([userid(length: 191)], map: "userid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_enterprise_wechat_group_chat_user_news_202109 {
  id               Int      @id @default(autoincrement())
  chat_id          String   @default("") @db.VarChar(255)
  userid           String   @default("") @db.VarChar(255)
  news_create_time Int      @default(1)
  msg_type         Boolean  @default(true)
  content          String?  @db.Text
  media_id         String   @default("") @db.VarChar(255)
  value            String   @default("") @db.VarChar(255)
  msg_id           String   @default("") @db.VarChar(255)
  agent_id         Int      @default(1)
  create_date      Int      @default(1)
  create_time      DateTime @default(now()) @db.DateTime(0)
  update_time      DateTime @default(now()) @db.DateTime(0)

  @@index([chat_id(length: 191)], map: "chat_id")
  @@index([create_date], map: "create_date")
  @@index([userid(length: 191)], map: "userid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_exclusive {
  id            Int     @id @default(autoincrement())
  app_id        Int     @default(0)
  user_id       Int     @default(0)
  remind        Int     @default(0)
  close         Int     @default(0)
  utime         Int     @default(0)
  ctime         Int     @default(0)
  start         Int     @default(0)
  remark        String  @default("") @db.VarChar(255)
  status        Int     @default(0)
  service       Int?    @default(0)
  charging_type Boolean @default(true)
  remind_config String? @default("") @db.VarChar(200)
  admin_id      Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_exclusive_copy {
  id            Int     @id @default(autoincrement())
  app_id        Int     @default(0)
  user_id       Int     @default(0)
  remind        Int     @default(0)
  close         Int     @default(0)
  utime         Int     @default(0)
  ctime         Int     @default(0)
  start         Int     @default(0)
  remark        String  @default("") @db.VarChar(255)
  status        Int     @default(0)
  service       Int?    @default(0)
  remind_config String? @default("") @db.VarChar(200)
  admin_id      Int?    @default(0)
}

model rcloud_exclusive_info {
  id         Int    @id @default(autoincrement())
  app_id     Int
  app_key    String @db.VarChar(255)
  user_id    Int
  start      Int
  app        String @db.VarChar(255)
  num        String @db.VarChar(255)
  config     String @db.VarChar(255)
  ctime      Int
  admin_id   Int
  close_time Int    @default(0)
  close_user Int?   @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_exclusive_protected {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id          Int     @default(0) @db.UnsignedInt
  app_id           Int     @default(0) @db.UnsignedInt
  server           Int     @default(0)
  app_key          String  @default("") @db.VarChar(50)
  admin_id         Int     @default(0) @db.UnsignedInt
  is_del           Boolean @default(false)
  type             Int     @default(0) @db.UnsignedTinyInt
  operation_status Boolean @default(false)
  operation        Int     @default(0) @db.UnsignedTinyInt
  charging_type    Boolean @default(true)
  message_count    BigInt  @default(0)
  end_date         Int     @default(0) @db.UnsignedInt
  send_remark      String  @default("") @db.VarChar(100)
  remark           String  @default("") @db.VarChar(200)
  config           String? @db.Text
  create_time      Int     @default(0)
  update_time      Int     @default(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_exclusive_protected_order {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  p_id          Int     @default(0) @db.UnsignedInt
  user_id       Int     @default(0)
  app_id        Int     @default(0)
  server        Int     @default(0)
  operation     Boolean @default(false)
  charge_type   Boolean @default(false)
  charging_type Boolean @default(true)
  discount      String  @default("100") @db.VarChar(10)
  should_money  Decimal @default(0.00) @db.Decimal(11, 2)
  money         Decimal @default(0.00) @db.Decimal(11, 2)
  charging_date String  @default("") @db.VarChar(15)
  remark        String  @default("") @db.VarChar(255)
  order_id      String  @default("") @db.VarChar(50)
  status        Boolean @default(true)
  config        String? @db.Text
  create_time   Int     @default(0)
  update_time   Int     @default(0)
  admin_id      Int     @default(0)

  @@index([app_id], map: "app_id")
  @@index([p_id], map: "index_p_id")
  @@index([user_id], map: "user_id")
}

model rcloud_exclusive_service {
  id          Int       @id @default(autoincrement())
  appId       String    @db.VarChar(45)
  enableTime  DateTime  @default(now()) @db.Timestamp(0)
  status      Boolean   @default(true)
  disableTime DateTime? @db.Timestamp(0)

  @@unique([appId, status], map: "appId_status")
}

model rcloud_exclusives {
  Id           Int     @id @default(autoincrement())
  PartitionKey String  @db.VarChar(128)
  Roles        String? @db.VarChar(255)
  ClusterName  String? @db.VarChar(128)
  Protocol     String? @db.VarChar(32)
  Status       Int     @db.SmallInt
  Host         String? @db.VarChar(128)
  Port         Int?
  ProjectName  String? @db.VarChar(128)
  SetName      String? @db.VarChar(128)
  Args         String? @db.VarChar(128)
  InstanceId   String? @db.VarChar(45)
  Eip          String? @db.VarChar(45)
  Owner        String? @db.VarChar(45)
  Disabled     String? @default("0") @db.Char(1)
  Domain       String? @db.VarChar(128)
  SetID        Int?    @default(0)

  @@index([PartitionKey], map: "PartitionKey_Idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_feishu_approval {
  id            Int       @id @default(autoincrement())
  name          String?   @default("") @db.VarChar(255)
  dcp_query_id  String?   @default("") @db.VarChar(255)
  approval_name String?   @default("") @db.VarChar(255)
  approval_code String?   @default("") @db.VarChar(255)
  approval_form String?   @db.Text
  callback_url  String?   @db.VarChar(512)
  update_time   DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_file {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  module      Int    @db.UnsignedTinyInt
  module_id   Int    @db.UnsignedInt
  raw_name    String @db.VarChar(45)
  save_medium Int    @db.UnsignedTinyInt
  file_id     String @db.VarChar(45)
  add_on      String @db.VarChar(45)
  copy_type   Int    @default(1) @db.TinyInt
  create_time Int    @db.UnsignedInt
  update_time Int    @db.UnsignedInt
  status      Int    @db.TinyInt
  uid         Int    @db.UnsignedInt
}

model rcloud_file_data {
  id      Int    @id @default(autoincrement())
  hash_id String @unique(map: "hash_id") @db.VarChar(32)
  data    Bytes
}

model rcloud_file_data_back {
  id      Int    @id @default(autoincrement())
  hash_id String @unique(map: "hash_id") @db.VarChar(32)
  data    Bytes
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_finance_white {
  id       Int     @id @default(autoincrement())
  user_id  Int?    @default(0)
  content  String? @default("") @db.VarChar(64)
  file     String? @default("") @db.VarChar(64)
  ctime    Int     @default(0)
  admin_id Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_find_user_password {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  email       String  @default("") @db.VarChar(100)
  mobile      String  @db.VarChar(50)
  secret      String? @db.VarChar(100)
  create_time Int     @default(0)
  status      Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_friends_link {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  name           String  @default("") @db.VarChar(50)
  link_url       String  @default("") @db.VarChar(200)
  logo_url       String  @default("") @db.VarChar(200)
  create_time    Int     @default(0)
  release_time   Int     @default(0)
  admin_user_id  Int     @default(0)
  status         Boolean @default(true)
  handel_user_id Int     @default(0)
  type           Int     @db.TinyInt
  nofollow       Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_google_project {
  id                        Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id                   Int    @default(0) @db.UnsignedInt
  app_key                   String @default("") @db.VarChar(50)
  project_id                String @default("") @db.VarChar(100)
  project_name              String @default("") @db.VarChar(100)
  project_no                String @default("") @db.VarChar(100)
  service_account_email     String @default("") @db.VarChar(100)
  service_account_unique_id String @default("") @db.VarChar(100)
  service_account_key_id    String @default("") @db.VarChar(100)
  service_account_key_data  Json
  open_services             Json
  config                    Json
  created_at                Int    @default(0) @db.UnsignedInt
  updated_at                Int    @default(0) @db.UnsignedInt
  deleted_at                Int    @default(0)

  @@index([app_key], map: "rcloud_google_project_app_key_index")
  @@index([user_id], map: "rcloud_google_project_user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_google_statistic {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id             Int     @default(0) @db.UnsignedInt
  app_id              Int     @default(0)
  env                 Int     @default(0) @db.TinyInt
  google_project_id   String  @default("") @db.VarChar(100)
  google_service_id   String  @default("") @db.VarChar(100)
  google_service_name String? @default("") @db.VarChar(100)
  google_sku_id       String  @default("") @db.VarChar(100)
  google_sku_name     String  @default("") @db.VarChar(100)
  amount              Decimal @default(0.0000) @db.Decimal(12, 4)
  money               Decimal @default(0.0000) @db.Decimal(12, 4)
  date                Int     @default(0)
  is_abroad           Boolean @default(false)
  created_at          Int     @default(0) @db.UnsignedInt
  updated_at          Int     @default(0) @db.UnsignedInt
  deleted_at          Int     @default(0)

  @@index([app_id], map: "rcloud_google_statistic_app_id_index")
  @@index([date], map: "rcloud_google_statistic_date_index")
  @@index([user_id], map: "rcloud_google_statistic_user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_hot_words {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  page_url         String  @default("") @db.VarChar(255)
  page_name        String  @default("") @db.VarChar(255)
  site             Boolean @default(false)
  sha1             String  @default("") @db.VarChar(255)
  update_frequency Int     @default(0) @db.UnsignedInt
  hot_words        String  @db.Text
  created_at       Int     @default(0) @db.UnsignedInt
  updated_at       Int     @default(0) @db.UnsignedInt
  deleted_at       Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_huawei_cloud_license {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  customer_id   String @default("") @db.VarChar(128)
  customer_name String @default("") @db.VarChar(80)
  business_id   String @default("") @db.VarChar(80)
  order_id      String @default("") @db.VarChar(80)
  sku_code      String @default("") @db.VarChar(80)
  product_id    String @default("") @db.VarChar(80)
  request_data  Json?
  license       String @default("") @db.VarChar(255)
  created_at    Int    @db.UnsignedInt
  updated_at    Int    @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_identity_certification_enterprise {
  id               Int     @id @default(autoincrement())
  user_id          Int     @default(0)
  attestation_mode Boolean @default(false)
  company_name     String  @default("") @db.VarChar(100)
  region           String  @default("") @db.VarChar(255)
  credential_no    String  @default("") @db.VarChar(60)
  contact_mode     String  @default("") @db.VarChar(50)
  attestation_type Boolean @default(false)
  status           Boolean @default(false)
  application_url  String  @default("") @db.VarChar(255)
  create_date      Int     @default(0)
  create_time      Int     @default(0)
  update_time      Int     @default(0)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_identity_certification_personal {
  id               Int     @id @default(autoincrement())
  user_id          Int     @default(0)
  attestation_mode Boolean @default(false)
  user_name        String  @default("") @db.VarChar(100)
  region           String  @default("") @db.VarChar(255)
  certificate_type Boolean @default(false)
  credential_no    String  @default("") @db.VarChar(60)
  contact_mode     String  @default("") @db.VarChar(50)
  bank_card_no     String  @default("") @db.VarChar(30)
  passport         String  @default("") @db.VarChar(255)
  attestation_type Boolean @default(false)
  status           Boolean @default(false)
  flow_id          String  @default("") @db.VarChar(100)
  application_url  String  @default("") @db.VarChar(255)
  create_date      Int     @default(0)
  create_time      Int     @default(0)
  update_time      Int     @default(0)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_individual_info {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  uid          Int     @db.UnsignedInt
  name         String  @db.VarChar(10)
  license_type Int     @db.UnsignedTinyInt
  license_no   String  @db.VarChar(45)
  create_time  Int     @db.UnsignedInt
  update_time  Int     @db.UnsignedInt
  status       Int     @db.TinyInt
  re           String? @default("") @db.VarChar(200)
  deny_reason  String  @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_information {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  status      Boolean @default(false)
  type        String  @default("") @db.VarChar(255)
  info        String  @db.Text
  create_time Int     @default(0)
  date        String  @default("") @db.VarChar(25)
  mobile      String  @default("") @db.Char(11)
  agent       String  @default("") @db.VarChar(1000)
  ip          String  @default("") @db.VarChar(255)
  sha1        String  @default("") @db.VarChar(255)
  varchar1    String  @default("") @db.VarChar(1000)
  varchar2    String  @default("") @db.VarChar(1000)
  varchar3    String  @default("") @db.VarChar(1000)
  varchar4    String  @default("") @db.VarChar(1000)
  varchar5    String  @default("") @db.VarChar(1000)
  varchar6    String  @default("") @db.VarChar(1000)
  varchar7    String  @default("") @db.VarChar(1000)
  varchar8    String  @default("") @db.VarChar(1000)
  varchar9    String  @default("") @db.VarChar(1000)
  varchar10   String  @db.Text

  @@index([type], map: "type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_information_type {
  id     Int     @id @default(autoincrement())
  type   String  @default("") @db.VarChar(512)
  status Boolean @default(false)
  name   String  @default("") @db.VarChar(255)
  info   String  @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ip_location {
  id    Int     @id @default(autoincrement()) @db.UnsignedInt
  ip    String? @db.VarChar(255)
  where String? @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_is_holiday {
  id          Int     @id @default(autoincrement())
  date        Int
  isholiday   Boolean @default(false)
  create_time Int     @default(0)
  update_time Int     @default(0)
  week        Int?    @default(0) @db.TinyInt
}

model rcloud_jop {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  position    String? @db.VarChar(96)
  title       String? @db.VarChar(192)
  description String? @db.VarChar(1536)
  require     String? @db.VarChar(1536)
  creattime   Int?
  status      Int?    @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_keywords {
  id     Int    @id @default(autoincrement())
  userId Int
  value  String @db.VarChar(255)
  env    Int    @db.TinyInt
  appId  String @db.VarChar(30)
  sId    Int
  ctime  Int
  utime  Int    @default(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rcloud_language {
  en String? @db.VarChar(255)
  ch String? @db.VarChar(255)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_limit {
  id           Int     @id @default(autoincrement())
  app_id       Int     @default(0)
  server       Int     @default(0)
  user_id      Int     @default(0)
  admin_id     Int     @default(0)
  min_type     Int     @default(0)
  min_cate     Int     @default(0)
  max_type     Int     @default(0)
  max_cate     Int     @default(0)
  start        Int     @default(0)
  end          Int     @default(0)
  status       Int     @default(0)
  remark       String? @default("") @db.VarChar(255)
  audit_id     Int?    @default(0)
  audit_time   Int?    @default(0)
  audit_reason String? @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_live_cdn_domain {
  id                     Int       @id @default(autoincrement())
  vendor                 Int       @default(0) @db.TinyInt
  user_id                Int       @default(0)
  app_id                 Int       @default(0)
  env                    Boolean   @default(false)
  status                 Boolean   @default(false)
  domain                 String    @default("") @db.VarChar(500)
  domain_cname           String    @default("") @db.VarChar(500)
  domain_id              String    @default("") @db.VarChar(500)
  flow                   Boolean   @default(false)
  config                 String    @default("") @db.VarChar(5000)
  region                 String    @default("") @db.VarChar(100)
  automatic_cdn          Boolean   @default(false)
  secret_key             String    @default("") @db.VarChar(256)
  time_domain            Int       @default(0)
  cdn_push_id            Int       @default(0)
  ssl_certificate_id     Int       @default(0)
  ssl_certificate_status Boolean   @default(false)
  ssl_certificate_time   Int       @default(0)
  create_time            DateTime  @default(now()) @db.DateTime(0)
  update_time            DateTime  @default(now()) @db.DateTime(0)
  delete_time            DateTime? @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([vendor, user_id, app_id, env, flow], map: "idx_app_id_user_id_env_flow_vendor")
  @@index([vendor, domain(length: 191)], map: "idx_domain_vendor")
  @@index([ssl_certificate_id, ssl_certificate_status], map: "idx_ssl_certificate")
  @@index([vendor, cdn_push_id], map: "idx_vendor_cdn_push_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_live_cdn_ssl_certificate {
  id                     Int      @id @default(autoincrement())
  user_id                Int      @default(0)
  certificate_name       String   @default("") @db.VarChar(256)
  empower_domain         String   @default("") @db.VarChar(256)
  be_overdue_time        Int      @default(0)
  certificate_content    String?  @db.Text
  secret_key             String?  @db.Text
  remarks                String   @default("") @db.VarChar(256)
  wang_su_certificate_id String   @default("") @db.VarChar(50)
  status                 Boolean  @default(true)
  config                 String   @default("") @db.VarChar(5000)
  create_time            DateTime @default(now()) @db.DateTime(0)
  update_time            DateTime @default(now()) @db.DateTime(0)

  @@index([status], map: "status")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_live_cdn_statistic {
  id          Int       @id @default(autoincrement())
  vendor      Int       @default(0) @db.TinyInt
  user_id     Int       @default(0)
  app_id      Int       @default(0)
  env         Boolean   @default(false)
  region      String    @default("") @db.VarChar(100)
  bandwidth   Decimal   @default(0.00) @db.Decimal(10, 2)
  domains     String    @default("") @db.VarChar(5000)
  date        Int       @default(0)
  time        DateTime  @db.DateTime(0)
  create_time DateTime  @default(now()) @db.DateTime(0)
  update_time DateTime  @default(now()) @db.DateTime(0)
  delete_time DateTime? @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_live_cdn_transcoding_statistic {
  id          Int       @id @default(autoincrement())
  vendor      Int       @default(0) @db.TinyInt
  user_id     Int       @default(0)
  app_id      Int       @default(0)
  env         Boolean   @default(false)
  domains     String    @default("") @db.VarChar(5000)
  date        Int       @default(0)
  value       Decimal   @default(0.00) @db.Decimal(10, 2)
  data        String    @default("") @db.VarChar(5000)
  create_time DateTime  @default(now()) @db.DateTime(0)
  update_time DateTime  @default(now()) @db.DateTime(0)
  delete_time DateTime? @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_log {
  id          Int    @id @default(autoincrement())
  user_id     Int    @default(0)
  action      String @db.VarChar(16)
  content     String @default("") @db.VarChar(500)
  create_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_login_err_log {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  username   String @default("") @db.VarChar(100)
  login_ip   String @default("") @db.VarChar(16)
  login_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mc {
  id           Int     @id @default(autoincrement())
  app_id       Int
  mcId         String  @db.VarChar(32)
  mcName       String  @db.VarChar(16)
  mcLogoImg    String  @db.VarChar(2083)
  mcUi         Boolean @default(false)
  isGlobal     Boolean @default(false)
  mcRouteUrl   String? @db.VarChar(255)
  mcEnviroment Boolean
  createTime   Int
  isRouteOpen  Boolean @default(false)
  category     Boolean
  status       Int     @default(0) @db.TinyInt
  is_customer  Boolean
  menuId       String  @default("0") @db.VarChar(36)
  bindingtime  String? @db.VarChar(32)

  @@unique([app_id, mcId, mcEnviroment, category], map: "app_mcId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mc_menu {
  id     Int    @id @default(autoincrement())
  mcId   String @default("0") @db.VarChar(32)
  menuId String @default("0") @db.VarChar(36)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mcmp_active {
  id             Int     @id @default(autoincrement())
  matter_id      Int     @default(0)
  name           String  @default("") @db.VarChar(64)
  status         Boolean @default(false)
  send_time      Int     @default(0)
  push_way       Boolean @default(false)
  system_user_id Int     @default(0)
  update_time    Int     @default(0)
  create_time    Int     @default(0)
  tags           String? @db.Text
  method         Boolean @default(false)
  platform       Boolean @default(false)
  app_id         Int     @default(0)
  remark         String  @default("") @db.VarChar(256)
  from           String  @default("") @db.VarChar(32)
  audience       Boolean @default(false)
  push           String  @default("") @db.VarChar(512)
  push_id        String  @default("") @db.VarChar(64)
}

model rcloud_mcmp_demo_count {
  id          Int    @id @default(autoincrement())
  ip          String @default("") @db.VarChar(32)
  create_time String @default("") @db.VarChar(32)
  count       Int    @default(0)
}

model rcloud_mcmp_log {
  id          Int    @id @default(autoincrement())
  content     String @default("") @db.VarChar(2048)
  create_time Float  @default(0)
  app_id      Int    @default(0)
  header      String @default("") @db.VarChar(1024)
  result      String @default("") @db.VarChar(1024)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mcmp_matter {
  id           Int     @id @default(autoincrement())
  name         String  @default("") @db.VarChar(128)
  template_id  Int     @default(0)
  update_time  Int     @default(0)
  create_time  Int     @default(0)
  send_time    Int     @default(0)
  status       Boolean @default(false)
  app_id       Int     @default(0)
  content      String  @default("") @db.VarChar(2048)
  jump_type    Boolean @default(false)
  url          String  @default("") @db.VarChar(256)
  ios_url      String  @default("") @db.VarChar(256)
  android_url  String  @default("") @db.VarChar(256)
  notice       String  @default("") @db.VarChar(256)
  push_content String  @default("") @db.VarChar(256)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mcmp_pushcount {
  id          Int     @id @default(autoincrement())
  app_id      Int     @default(0)
  times       Int     @default(0)
  create_time Int
  update_time Int
  status      Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mcmp_template {
  id           Int     @id @default(autoincrement())
  name         String  @default("") @db.VarChar(32)
  class        String  @default("") @db.VarChar(32)
  data         String  @default("") @db.VarChar(1024)
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  img_url      String  @default("") @db.VarChar(256)
  status       Boolean @default(true)
  msg_type     String  @default("") @db.VarChar(64)
  formlist     String  @default("") @db.VarChar(1024)
  push_content String  @db.VarChar(256)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mcmp_user {
  id          Int    @id @default(autoincrement())
  name        String @default("") @db.VarChar(128)
  user_id     String @default("") @db.VarChar(128)
  img_url     String @default("") @db.VarChar(256)
  app_id      Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)
  status      Int    @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_message_box {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  title        String  @default("") @db.VarChar(200)
  content      String? @db.Text
  from_user_id Int     @default(0)
  to_user_id   Int     @default(0)
  is_read      Boolean @default(false)
  type         Boolean @default(false)
  status       Boolean @default(false)
  create_time  Int     @default(0)
  dest         String  @default("'") @db.VarChar(50)

  @@index([to_user_id], map: "ix_userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_message_email_template {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  title      String  @default("") @db.VarChar(200)
  name       String  @default("") @db.VarChar(100)
  content    String? @db.Text
  attachment String  @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_message_template {
  id          Int     @id
  title       String  @default("") @db.VarChar(200)
  name        String? @default("") @db.VarChar(50)
  content     String? @db.Text
  create_time Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mf {
  id      Int     @id @default(autoincrement())
  RC_KEY  String  @db.VarChar(255)
  name    String  @db.VarChar(255)
  intro   String  @db.VarChar(255)
  img     String  @db.VarChar(255)
  web_url String  @db.VarChar(255)
  is_app  Boolean
  is_auth Boolean
  owner   String  @db.VarChar(255)
  ctime   Int
  uptime  Int
  secret  String  @db.VarChar(255)
  status  Int     @default(2) @db.TinyInt
  kb_url  String  @db.VarChar(255)
}

model rcloud_mini_experience {
  id          Int     @id @default(autoincrement())
  comName     String  @default("") @db.VarChar(256)
  userName    String  @default("") @db.VarChar(256)
  mobile      Float   @default(0)
  notice      String  @default("") @db.VarChar(1024)
  create_time Int     @default(0)
  status      Boolean @default(false)
}

model rcloud_mobile_ip_log {
  id          Int    @id @default(autoincrement())
  ip          String @default("") @db.VarChar(16)
  create_time Int    @default(0)

  @@index([create_time], map: "idx_create_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mobile_log {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  code        String @default("") @db.VarChar(45)
  mobile      String @default("") @db.Char(11)
  create_time Int    @default(0)
  status      Int    @default(0) @db.TinyInt
  email       String @default("") @db.VarChar(100)

  @@index([mobile], map: "mobile")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mobile_sdk {
  id           Int       @id @default(autoincrement())
  name         String    @db.VarChar(255)
  os           String    @db.VarChar(255)
  product      String?   @db.VarChar(255)
  type         Int
  version      String    @db.VarChar(255)
  version_int  Int
  version_type String    @db.VarChar(255)
  download_url String    @db.VarChar(512)
  package_time DateTime? @db.Timestamp(0)
  package_url  String?   @db.VarChar(512)
  description  String?   @db.VarChar(512)
  status       Int       @default(1) @db.TinyInt
  create_by    Int       @default(0) @db.UnsignedInt
  update_by    Int       @default(0) @db.UnsignedInt
  create_time  DateTime  @default(now()) @db.Timestamp(0)
  update_time  DateTime  @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mobile_sdk_dependency {
  id       Int @id @default(autoincrement())
  basic_id Int
  sub_id   Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_mp_verify_log {
  Id            Int       @id @default(autoincrement()) @db.UnsignedInt
  ctype         String    @default("1") @db.VarChar(6)
  reason        String?   @db.VarChar(255)
  admin_uid     String?   @db.VarChar(255)
  mp_id         Int?
  create_time   DateTime? @default(now()) @db.Timestamp(0)
  other_reasons String?   @db.VarChar(255)
  mp_nick_name  String?   @db.VarChar(255)
  ctype_num     String?   @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_multiport {
  id                Int     @id @default(autoincrement()) @db.UnsignedInt
  app_key           String  @default("") @db.VarChar(50)
  developer_user_id Int     @default(0)
  app_id            Int     @default(0)
  env               Boolean @default(false)
  create_time       Int     @default(0)
  close_time        Int?    @default(0)
  admin_user_id     Int     @default(0)
  release_time      Int     @default(0)
  status            Int     @default(2) @db.TinyInt
  is_del            Boolean @default(false)
  terminal          Int?    @default(2) @db.TinyInt
  terminal_start    Int?    @default(0)
  terminal_close    Int?    @default(0)
  terminal_admin    Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_news_roam {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  app_key     String    @db.VarChar(45)
  roam_status Int       @default(2) @db.UnsignedTinyInt
  ok_time     Int?      @db.UnsignedInt
  create_time Int?      @db.UnsignedInt
  update_time DateTime? @db.Timestamp(0)

  @@index([app_key], map: "news_roam_app_key")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_notice {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  title       String  @default("") @db.VarChar(100)
  content     String? @db.Text
  remark      String? @db.Text
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Boolean @default(false)
  type        Int     @default(0) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_notification {
  id          Int     @id @default(autoincrement())
  title       String  @default("") @db.VarChar(255)
  inner_sms   String  @default("") @db.VarChar(255)
  outer_sms   String  @default("") @db.VarChar(255)
  inner_email String  @db.Text
  outer_email String  @db.Text
  message     String  @db.Text
  rce         String  @db.Text
  comment     String  @default("") @db.VarChar(255)
  created_at  Int     @default(0) @db.UnsignedInt
  updated_at  Int     @default(0) @db.UnsignedInt
  deleted_at  Int?    @db.UnsignedInt
  remark      String? @default("") @db.VarChar(255)
  code        BigInt  @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_notification_records {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  type            Boolean
  app_id          Int     @db.UnsignedInt
  user_id         Int     @db.UnsignedInt
  service         Int     @db.UnsignedInt
  operation       Boolean @default(false)
  n               Boolean @default(false)
  except_close_at String  @db.VarChar(50)
  status          Int     @db.UnsignedTinyInt
  has_delay       Boolean
  config          String  @default("{}") @db.VarChar(5000)
  created_at      Int
  updated_at      Int
  deleted_at      Int?

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_numberarea {
  id           Int    @id @default(autoincrement())
  MobileNumber String @db.VarChar(20)
  MobileArea   String @db.VarChar(50)
  type         Int

  @@index([MobileNumber], map: "MobileNumber")
  @@index([type], map: "type")
}

model rcloud_online_broadcast {
  id      Int    @id @default(autoincrement())
  app_id  Int
  env     Int?   @db.SmallInt
  app_key String @db.VarChar(255)
  user_id Int
  status  Int    @db.SmallInt
  ctime   Int
  utime   Int
  ok_time Int

  @@index([app_id], map: "idx_app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_online_question {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int     @default(0) @db.UnsignedInt
  question_count Int     @default(0) @db.UnsignedInt
  token          String  @default("") @db.VarChar(200)
  report_url     String? @db.Text
  start_time     String  @default("") @db.VarChar(15)
  end_time       String  @default("") @db.VarChar(15)
  status         Int     @default(0) @db.UnsignedTinyInt
  create_time    String  @default("") @db.VarChar(15)
  update_time    String  @default("") @db.VarChar(15)
  is_del         Int     @default(0) @db.UnsignedTinyInt
  remark         String? @default("") @db.VarChar(200)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_online_question_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  question_id Int     @default(0) @db.UnsignedInt
  action      String  @default("") @db.VarChar(50)
  param       String? @db.Text
  result      String? @db.Text
  create_time String  @default("") @db.VarChar(15)
  update_time String  @default("") @db.VarChar(15)
  is_del      Int     @default(0) @db.UnsignedTinyInt
  remark      String? @default("") @db.VarChar(200)

  @@index([question_id], map: "question_id")
}

model rcloud_online_status {
  id          Int      @id @default(autoincrement())
  appkey      String   @default("") @db.VarChar(50)
  url         String   @default("") @db.VarChar(255)
  create_time DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_online_subscribe {
  id      Int    @id @default(autoincrement())
  app_id  Int
  app_key String @db.VarChar(255)
  url     String @db.VarChar(255)
  env     Int    @db.SmallInt
  user_id Int
  ctime   Int
  utime   Int
  status  Int    @db.SmallInt
  ok_time Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_op_log {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  user_id     Int    @default(0)
  event_type  String @default("") @db.VarChar(32)
  from_page   String @default("") @db.VarChar(32)
  from_module String @default("") @db.VarChar(32)
  op_date     Int    @default(0)
  create_by   Int    @default(0)
  create_time Int    @default(0)

  @@index([track_id], map: "idx_track_id")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_operation {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  source_table  String  @default("") @db.VarChar(50)
  source_id     Int     @default(0) @db.UnsignedInt
  app_id        Int     @default(0) @db.UnsignedInt
  app_key       String  @default("") @db.VarChar(50)
  user_id       Int     @default(0) @db.UnsignedInt
  admin_user_id Int     @default(0) @db.UnsignedInt
  operator_type Int     @default(0) @db.UnsignedTinyInt
  type          String  @default("") @db.VarChar(50)
  operated_at   Int     @default(0) @db.UnsignedInt
  remark        String  @default("") @db.VarChar(255)
  order_id      String  @default("0") @db.VarChar(255)
  ip            String  @default("") @db.VarChar(32)
  env           Boolean @default(false)
  service       Int     @default(0) @db.UnsignedInt
  account       Int     @default(0) @db.UnsignedTinyInt
  created_at    Int     @default(0) @db.UnsignedInt
  updated_at    Int     @default(0) @db.UnsignedInt
  deleted_at    Int?

  @@index([app_id], map: "app_id")
  @@index([app_key], map: "app_key")
  @@index([user_id], map: "user_id")
}

model rcloud_operation_log {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  name       String @db.VarChar(32)
  operation  String @db.VarChar(32)
  ip         String @db.VarChar(32)
  content    String @db.VarChar(512)
  createtime Int
  env        Int    @db.TinyInt
  appid      Int
  time       String @db.VarChar(32)

  @@index([appid], map: "rcloud_operation_appid")
  @@index([operation, createtime], map: "rcloud_operation_operation_createtime")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_operation_white_list {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int    @db.UnsignedInt
  app_id     Int    @db.UnsignedInt
  type       Int    @db.UnsignedTinyInt
  service    Int    @db.UnsignedInt
  category   Int    @db.UnsignedTinyInt
  operation  Int    @db.UnsignedTinyInt
  n          Int    @db.UnsignedTinyInt
  start_at   String @db.VarChar(50)
  end_at     String @db.VarChar(50)
  remark     String @db.Text
  file       String @db.VarChar(500)
  status     Int    @db.UnsignedTinyInt
  admin_id   Int    @db.UnsignedInt
  created_at Int    @db.UnsignedInt
  updated_at Int
  deleted_at Int?   @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_order {
  order_id      BigInt  @id @default(autoincrement())
  user_id       Int
  type          Int     @default(1)
  money         Decimal @db.Decimal(11, 2)
  out_no        String  @default("0") @db.VarChar(50)
  payment       String  @default("ALIPAY") @db.VarChar(20)
  ctime         Int     @default(0)
  utime         Int     @default(0)
  status        Boolean
  app_id        Int?
  service_id    Int     @default(0) @db.UnsignedInt
  params        String  @default("") @db.VarChar(500)
  alipay_params String? @db.Text
  discount      Int     @default(100) @db.UnsignedTinyInt
  time_length   Int     @default(0) @db.UnsignedTinyInt
  config        String  @db.Text

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_order_will_pay {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  type               Int     @default(0) @db.UnsignedTinyInt
  user_id            Int     @default(0) @db.UnsignedInt
  app_id             Int     @default(0) @db.UnsignedInt
  create_time        Int     @default(0) @db.UnsignedInt
  update_time        Int     @default(0) @db.UnsignedInt
  status             Int     @default(0) @db.UnsignedTinyInt
  money              Decimal @default(0.00) @db.Decimal(11, 2)
  order_id           String  @default("") @db.VarChar(255)
  money_distribution String  @default("") @db.VarChar(500)
  service_type       Int     @default(0) @db.UnsignedTinyInt
  service_config     String  @default("") @db.VarChar(500)
  discount_config    String  @default("") @db.VarChar(5000)
  gift_config        String? @default("") @db.VarChar(500)
  activity           String  @default("") @db.VarChar(50)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_owe_month {
  id                      Int      @id @default(autoincrement()) @db.UnsignedInt
  userId                  Int      @db.UnsignedInt
  sum                     String?  @db.VarChar(64)
  orderBalanceNegativeSet String?  @db.Text
  orderTotalAmountSet     String?  @db.Text
  date                    String?  @db.VarChar(16)
  createTime              DateTime @default(now()) @db.Timestamp(0)
  status                  Int      @default(1) @db.TinyInt
}

model rcloud_pha_task_log {
  id          Int    @id @default(autoincrement())
  date        Int    @default(0)
  content     String @default("") @db.VarChar(1024)
  mobile      Float  @default(0)
  create_time Int    @default(0)
  code        Int?   @default(0)
  msg         String @default("") @db.VarChar(64)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_pha_user {
  id          Int
  type        String @default("") @db.VarChar(16)
  phid        String @default("") @db.VarChar(64)
  username    String @default("") @db.VarChar(64)
  realname    String @default("") @db.VarChar(64)
  roles       String @default("") @db.VarChar(64)
  create_time Int    @default(0)
  update_time Int    @default(0)
  insert_time Int    @default(0)
  status      Int    @default(1) @db.TinyInt
  mobile      Float  @default(0)
  user_id     String @default("") @db.VarChar(64)
  member_id   Int    @default(0)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_picture {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  path                String  @default("") @db.VarChar(100)
  name                String  @default("") @db.VarChar(50)
  create_time         Int     @default(0)
  is_del              Boolean @default(false)
  admin_user_id       Int     @default(0)
  picture_category_id Int     @default(0)
  status              Boolean @default(false)
  handel_user_id      Int     @default(0)
  release_time        Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_picture_category {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  name          String @default("") @db.VarChar(30)
  pid           Int    @default(0)
  create_time   Int    @default(0)
  admin_user_id Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_press_edition_push_rule_list {
  id          Int    @id @default(autoincrement())
  app_key     String @default("") @db.VarChar(45)
  server_id   String @default("") @db.VarChar(45)
  rule_name   String @default("") @db.VarChar(100)
  object_name String @default("") @db.VarChar(100)
  plat        Int    @default(1)
  content     String @default("") @db.VarChar(512)
  versions    String @default("") @db.VarChar(512)
  create_time Int    @default(0)
  update_time Int    @default(0)

  @@index([app_key], map: "app_key")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_program_online {
  id     Int @id @default(autoincrement())
  app_id Int @default(0)
  ctime  Int @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_public_recharge {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id            Int     @default(0) @db.UnsignedInt
  remitter           String  @default("") @db.VarChar(200)
  money              Decimal @default(0.00) @db.Decimal(11, 2)
  create_time        String  @default("") @db.VarChar(12)
  update_time        String  @default("") @db.VarChar(12)
  reason             String  @default("") @db.VarChar(200)
  source             Int     @default(0) @db.UnsignedTinyInt
  file               String  @default("") @db.VarChar(255)
  status             Boolean @default(true)
  enter_account_time String  @default("") @db.VarChar(20)
  admin_id           Int     @default(0) @db.UnsignedInt
  audit_id           Int     @default(0) @db.UnsignedInt
  config             String? @db.Text
  response           String? @db.Text
  remark             String  @default("") @db.VarChar(200)
  audit_reason       String  @default("") @db.VarChar(200)
  is_del             Int     @default(0) @db.UnsignedTinyInt

  @@index([user_id], map: "user_id")
}

model rcloud_push_history {
  id          Int      @id @default(autoincrement())
  push_id     String   @db.VarChar(100)
  app_key     String   @db.VarChar(100)
  create_time DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_push_record {
  id          Int     @id @default(autoincrement())
  app_id      Int     @default(0)
  env         Boolean @default(true)
  user_id     Int     @default(0)
  push_id     Int     @default(0)
  rule_name   String  @default("") @db.VarChar(100)
  name        String  @default("") @db.VarChar(50)
  platform    String  @default("") @db.VarChar(255)
  versions    String  @default("") @db.VarChar(255)
  content     String  @default("") @db.VarChar(50)
  admin_id    Int     @default(0)
  create_time Int     @default(0)
  update_time Int     @default(0)
  is_del      Int     @default(0) @db.TinyInt

  @@index([app_id], map: "app_Id")
  @@index([user_id], map: "user_id")
}

model rcloud_push_template {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id        Int
  env           Boolean
  app_key       String  @db.VarChar(255)
  template_id   String  @db.VarChar(255)
  template_name String  @db.VarChar(255)
  template_type String  @db.VarChar(255)
  content       String  @db.Text
  created_at    Int
  updated_at    Int
  deleted_at    Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_qiniu_upload {
  id            Int      @id @default(autoincrement())
  bucket_name   String   @default("") @db.VarChar(255)
  path          String   @default("") @db.VarChar(255)
  file_name     String   @default("") @db.VarChar(255)
  file_url      String   @default("") @db.VarChar(255)
  upload_return String   @default("") @db.VarChar(255)
  remark        String   @default("") @db.VarChar(255)
  create_time   DateTime @default(now()) @db.Timestamp(0)
  admin_id      Int      @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_question_article_old {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  c_id           Int     @default(0)
  title          String  @default("") @db.VarChar(200)
  description    String  @db.Text
  solve_plan     String  @db.Text
  tag            String  @default("") @db.VarChar(500)
  good_num       Int     @default(0)
  bad_num        Int     @default(0)
  create_time    Int     @default(0)
  create_user_id Int     @default(0)
  is_del         Boolean @default(false)
  update_time    Int     @default(0)
  chase_num      Int     @default(0)
  status         Int     @default(1)
  visit_count    Int     @default(0)

  @@index([c_id, status, is_del], map: "ix_cid_del_status")
  @@index([visit_count], map: "visit_count")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_question_category_old {
  id             Int    @id @default(autoincrement()) @db.UnsignedInt
  name           String @default("") @db.VarChar(50)
  create_user_id Int    @default(0)
  create_time    Int    @default(0)
  sort           Int    @default(0)
  is_del         Int    @default(0) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_recharge_list {
  id                       Int     @id @default(autoincrement()) @db.UnsignedInt
  service_id               Int     @default(0)
  img_balance              String  @default("") @db.VarChar(255)
  img_arrears              String  @default("") @db.VarChar(255)
  active_url               String  @default("") @db.VarChar(255)
  active_begin             Int     @default(0)
  active_end               Int     @default(0)
  recharge_url             String  @default("") @db.VarChar(255)
  sort                     Int     @default(0)
  utime                    Int     @default(0)
  charge_explain           String  @db.Text
  always_show              Boolean @default(false)
  is_new                   Boolean @default(false)
  service_describe         String  @db.Text
  charge_dimension         Int     @default(0) @db.UnsignedTinyInt
  charge_type              Int     @default(0) @db.UnsignedTinyInt
  price                    Decimal @default(0.00) @db.Decimal(10, 2)
  min_price                Decimal @default(0.00) @db.Decimal(10, 2)
  discount                 String  @db.Text
  package_config           String  @db.Text
  charge_describe          String  @db.Text
  regular_switch           Int?    @default(0) @db.TinyInt
  regular_discount         String  @db.Text
  regular_price            Decimal @default(0.00) @db.Decimal(10, 2)
  internal_charge_describe String  @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_recharge_list_copy {
  id                       Int     @id @default(autoincrement()) @db.UnsignedInt
  service_id               Int     @default(0)
  img_balance              String  @default("") @db.VarChar(255)
  img_arrears              String  @default("") @db.VarChar(255)
  active_url               String  @default("") @db.VarChar(255)
  active_begin             Int     @default(0)
  active_end               Int     @default(0)
  recharge_url             String  @default("") @db.VarChar(255)
  sort                     Int     @default(0)
  utime                    Int     @default(0)
  charge_explain           String  @db.Text
  always_show              Boolean @default(false)
  is_new                   Boolean @default(false)
  service_describe         String  @db.Text
  charge_dimension         Int     @default(0) @db.UnsignedTinyInt
  charge_type              Int     @default(0) @db.UnsignedTinyInt
  price                    Decimal @default(0.00) @db.Decimal(10, 2)
  min_price                Decimal @default(0.00) @db.Decimal(10, 2)
  discount                 String  @db.Text
  package_config           String  @db.Text
  charge_describe          String  @db.Text
  regular_switch           Int?    @default(0) @db.TinyInt
  regular_discount         String  @db.Text
  regular_price            Decimal @default(0.00) @db.Decimal(10, 2)
  internal_charge_describe String  @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_red_account {
  id              Int      @id @default(autoincrement())
  user_id         Int      @default(0)
  app_id          Int      @default(0)
  money           Int      @default(0)
  packet_money    Int      @default(0)
  order_id        String   @default("0") @db.VarChar(64)
  packet_order_id String   @default("0") @db.VarChar(64)
  ctime           DateTime @default(now()) @db.Timestamp(0)
  utime           String   @default("") @db.VarChar(64)
  partnerId       String   @default("") @db.VarChar(64)
  status          Int      @default(2)
  charging_time   String   @default("") @db.VarChar(16)
  // old_or_new      rcloud_red_account_old_or_new @default(dbgenerated("新用户"))
  charging_money  Int      @default(0)
  packet_sum      Int      @default(0)
  packet_use      Int      @default(0)
  packet_alert0   Int      @default(0) @db.TinyInt
  packet_alert1   Int      @default(0) @db.TinyInt
  packet_alert2   Int      @default(0) @db.TinyInt
  packet_alert3   Int      @default(0) @db.TinyInt
  flow_alert      Int      @default(0) @db.TinyInt
  tpye            Int      @default(0) @db.TinyInt

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_red_month {
  id             Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id        Int     @default(0) @db.UnsignedInt
  app_id         Int     @default(0) @db.UnsignedInt
  month          Int     @default(0) @db.UnsignedInt
  send           Int     @default(0) @db.UnsignedInt
  send_money     Decimal @default(0.00) @db.Decimal(11, 2)
  recharge       Int     @default(0) @db.UnsignedInt
  recharge_money Decimal @default(0.00) @db.Decimal(11, 2)
  transfer       Int     @default(0) @db.UnsignedInt
  transfer_money Decimal @default(0.00) @db.Decimal(11, 2)
  withdraw       Int     @default(0) @db.UnsignedInt
  withdraw_money Decimal @default(0.00) @db.Decimal(11, 2)
  partnerId      String  @default("") @db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_red_order {
  id           Int     @id @default(autoincrement())
  order_id     String  @default("0") @db.VarChar(30)
  user_id      Int     @default(0)
  app_id       Int     @default(0)
  payment      Int     @default(0) @db.TinyInt
  money        Decimal @default(0) @db.Decimal(11, 0)
  packet_money Decimal @default(0) @db.Decimal(11, 0)
  status       Boolean @default(true)
  ctime        Int     @default(0)
  utime        Int     @default(0)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_red_statistics {
  id        Int     @id @default(autoincrement()) @db.UnsignedInt
  order_id  String  @default("0") @db.VarChar(64)
  dataDate  String  @db.VarChar(64)
  userid    String  @default("") @db.VarChar(46)
  customkey String  @default("") @db.VarChar(32)
  payStatus Int     @default(0) @db.TinyInt
  payType   Int     @default(0) @db.TinyInt
  money     Decimal @default(0.00) @db.Decimal(11, 2)
  ctime     String  @db.VarChar(32)
  app_id    Int     @default(0) @db.UnsignedInt
  user_id   Int     @default(0) @db.UnsignedInt
  type      Int     @default(1) @db.TinyInt
  env       Int     @default(1) @db.TinyInt

  @@index([app_id], map: "app_id")
  @@index([app_id, dataDate], map: "app_id_dataDate")
  @@index([id], map: "sid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_redirect_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  url_md5     String  @default("") @db.VarChar(255)
  url         String  @default("") @db.VarChar(500)
  create_time Int     @default(0)
  uv          Boolean @default(false)
  channel     String  @default("") @db.VarChar(20)
  user_agent  String  @default("") @db.VarChar(500)
  client      Boolean @default(false)
  log_id      String  @default("") @db.VarChar(50)

  @@index([log_id], map: "log_id")
  @@index([channel], map: "type")
  @@index([url_md5], map: "url_md5")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_reminder_sales {
  id           Int    @id @default(autoincrement())
  type         Int    @default(0) @db.TinyInt
  date         Int    @default(0)
  user_id      Int
  app_id       Int
  service_id   Int?   @default(0)
  info         String @default("") @db.VarChar(2000)
  admin_id     Int    @default(0)
  secondary_id Int    @default(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_reply_log {
  id              Int      @id @default(autoincrement()) @db.UnsignedInt
  userid          Int
  ticketid        Int
  username        String   @db.VarChar(64)
  replyid         Int
  replytime       Int
  workstatus      Boolean?
  replydelay      Int?
  userplatform    Int?
  replyfloor      Int?     @db.TinyInt
  tickettime      Int?
  ticketclosetime Int?

  @@index([ticketid], map: "ix_ticketid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rongyun_transfer_record {
  id                       Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id                  Int      @default(0) @db.UnsignedInt
  operation_type           Int      @default(0)
  operation_name           String   @default("") @db.VarChar(100)
  service_id               Int      @default(0)
  app_id                   Int      @default(0)
  rongyun_books_balance    Decimal  @db.Decimal(11, 2)
  rongyun_no_invoice_money Decimal  @db.Decimal(11, 2)
  transfer_money           Decimal  @db.Decimal(11, 2)
  remarks                  String   @db.Text
  enclosure_url            String   @default("") @db.VarChar(255)
  launch_admin_id          Int      @default(0)
  approval_status          Boolean  @default(true)
  check_admin_id           Int      @default(0)
  approval_reason          String   @default("") @db.VarChar(255)
  approval_at              Int      @default(0)
  recharge_transfer_info   String   @db.Text
  chargeing_order_id       BigInt   @default(0)
  collect_money_order_id   BigInt   @default(0)
  create_date              DateTime @default(now()) @db.DateTime(0)
  update_date              DateTime @default(now()) @db.DateTime(0)

  @@index([app_id], map: "rcloud_app_id")
  @@index([approval_status], map: "rcloud_approval_status")
  @@index([user_id], map: "rcloud_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_check_stat {
  id             Int     @id @default(autoincrement())
  user_id        Int
  app_id         Int
  server_id      Int
  env            Boolean @default(false)
  product        String  @db.VarChar(50)
  type           String  @default("") @db.VarChar(50)
  value          Int     @default(0)
  pay_value      Int     @default(0)
  money          Decimal @default(0.0000) @db.Decimal(10, 4)
  date           Int     @default(0)
  is_abroad      Int     @default(0) @db.UnsignedTinyInt
  is_charge_item Int     @default(1) @db.UnsignedTinyInt
  cluster        String  @default("") @db.VarChar(100)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_check_stat_20221025 {
  id        Int     @id @default(autoincrement())
  user_id   Int
  app_id    Int
  server_id Int
  env       Boolean @default(false)
  product   String  @db.VarChar(50)
  type      String  @default("") @db.VarChar(50)
  value     Int     @default(0)
  pay_value Int     @default(0)
  money     Decimal @default(0.0000) @db.Decimal(10, 4)
  date      Int     @default(0)
  is_abroad Int     @default(0) @db.UnsignedTinyInt
  cluster   String  @default("") @db.VarChar(100)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_check_stat_20230406 {
  id             Int     @id @default(autoincrement())
  user_id        Int
  app_id         Int
  server_id      Int
  env            Boolean @default(false)
  product        String  @db.VarChar(50)
  type           String  @default("") @db.VarChar(50)
  value          Int     @default(0)
  pay_value      Int     @default(0)
  money          Decimal @default(0.0000) @db.Decimal(10, 4)
  date           Int     @default(0)
  is_abroad      Int     @default(0) @db.UnsignedTinyInt
  is_charge_item Int     @default(1) @db.UnsignedTinyInt
  cluster        String  @default("") @db.VarChar(100)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_cloud_record_config_statistics {
  id        Int    @id @default(autoincrement())
  user_id   Int
  app_id    Int
  server_id Int
  date      Int
  config    String @default("0") @db.VarChar(1000)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_cloud_record_statistics {
  id              Int     @id @default(autoincrement())
  user_id         Int
  app_id          Int
  server_id       Int
  date            Int
  rtc_id          String  @db.VarChar(100)
  room            String  @db.VarChar(500)
  record_id       String  @db.VarChar(100)
  uuid            String  @default("") @db.VarChar(100)
  code            String  @db.VarChar(6)
  stream_tag      String? @default("RongCloudRTC") @db.VarChar(255)
  model           Boolean @default(false)
  file_path       String  @default("") @db.VarChar(1024)
  url             String  @default("") @db.VarChar(1024)
  type0_times     Int
  type1_times     Int
  type2_times     Int
  type3_times     Int
  type0_pay_times Int
  type1_pay_times Int
  type2_pay_times Int
  type3_pay_times Int
  money           Decimal @default(0.0000) @db.Decimal(8, 4)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([server_id], map: "server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_live_statistics {
  id        Int @id @default(autoincrement()) @db.UnsignedInt
  user_id   Int
  app_id    Int
  date      Int @default(0)
  room_num  Int @default(0)
  bandwidth Int @default(0)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_stat {
  id            Int     @id @default(autoincrement())
  user_id       Int
  app_id        Int     @default(0)
  server_id     Int
  env           Boolean @default(false)
  business_type Boolean @default(false)
  money_type    Boolean @default(true)
  flow_type     Int     @default(0) @db.TinyInt
  value         Int     @default(0)
  pay_value     Int     @default(0)
  type          Boolean @default(false)
  date          Int     @default(0)
  ctime         Int?    @default(0)
  money         Float   @default(0.0000) @db.Float
  virtual_money Float?  @default(0.0000) @db.Float
  is_use        Boolean @default(false)

  @@index([app_id], map: "app_id")
  @@index([date, server_id], map: "date_server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_stat_20200818 {
  id            Int     @id @default(autoincrement())
  user_id       Int
  app_id        Int     @default(0)
  server_id     Int
  env           Boolean @default(false)
  business_type Boolean @default(false)
  money_type    Boolean @default(true)
  flow_type     Int     @default(0) @db.TinyInt
  value         Int     @default(0)
  pay_value     Int     @default(0)
  type          Boolean @default(false)
  date          Int     @default(0)
  ctime         Int?    @default(0)
  money         Float   @default(0.0000) @db.Float
  virtual_money Float?  @default(0.0000) @db.Float
  is_use        Boolean @default(false)

  @@index([app_id], map: "app_id")
  @@index([date, server_id], map: "date_server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_stat_20221205 {
  id            Int     @id @default(autoincrement())
  user_id       Int
  app_id        Int     @default(0)
  server_id     Int
  env           Boolean @default(false)
  business_type Boolean @default(false)
  money_type    Boolean @default(true)
  flow_type     Int     @default(0) @db.TinyInt
  value         Int     @default(0)
  pay_value     Int     @default(0)
  type          Boolean @default(false)
  date          Int     @default(0)
  ctime         Int?    @default(0)
  money         Float   @default(0.0000) @db.Float
  virtual_money Float?  @default(0.0000) @db.Float
  is_use        Boolean @default(false)

  @@index([app_id], map: "app_id")
  @@index([date, server_id], map: "date_server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_stat_expect {
  id            Int     @id @default(autoincrement())
  user_id       Int
  app_id        Int     @default(0)
  app_key       String  @default("") @db.VarChar(100)
  server_id     Int
  env           Boolean @default(false)
  business_type Boolean @default(false)
  money_type    Boolean @default(true)
  flow_type     Int     @default(0) @db.TinyInt
  value         Int     @default(0)
  pay_value     Int     @default(0)
  type          Boolean @default(false)
  month         Int     @default(0)
  date          Int     @default(0)
  ctime         Int?    @default(0)
  money         Float   @default(0.0000) @db.Float
  virtual_money Float?  @default(0.0000) @db.Float
  is_use        Boolean @default(false)
  tag           String  @default("") @db.VarChar(500)
  discount      Decimal @default(100.00) @db.Decimal(8, 2)

  @@index([app_id], map: "app_id")
  @@index([month, server_id], map: "date_server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_statistics {
  id              Int     @id @default(autoincrement())
  user_id         Int     @default(0)
  app_id          Int     @default(0)
  server_id       Int
  create_time     Int
  rtc_id          String  @default("") @db.VarChar(100)
  room            String  @default("") @db.VarChar(255)
  uuid            String  @db.VarChar(100)
  type0_times     Int     @default(0)
  type1_times     Int     @default(0)
  type2_times     Int     @default(0)
  type3_times     Int     @default(0)
  type4_times     Int     @default(0)
  type5_times     Int     @default(0)
  type6_times     Int     @default(0)
  type7_times     Int     @default(0)
  type0_pay_times Int     @default(0)
  type1_pay_times Int     @default(0)
  type2_pay_times Int     @default(0)
  type3_pay_times Int     @default(0)
  type4_pay_times Int     @default(0)
  type5_pay_times Int     @default(0)
  type6_pay_times Int     @default(0)
  type7_pay_times Int     @default(0)
  money           Decimal @default(0.0000) @db.Decimal(8, 4)

  @@index([app_id], map: "app_id")
  @@index([create_time], map: "create_time")
  @@index([rtc_id], map: "rtc_id")
  @@index([server_id], map: "server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_rtc_statistics_daily {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id         Int     @default(0) @db.UnsignedInt
  app_id          Int     @default(0) @db.UnsignedInt
  server_id       Int     @default(0) @db.UnsignedInt
  date            Int     @default(0) @db.UnsignedInt
  type0_times     Int     @default(0) @db.UnsignedInt
  type1_times     Int     @default(0) @db.UnsignedInt
  type2_times     Int     @default(0) @db.UnsignedInt
  type3_times     Int     @default(0) @db.UnsignedInt
  type4_times     Int     @default(0) @db.UnsignedInt
  type5_times     Int     @default(0) @db.UnsignedInt
  type6_times     Int     @default(0) @db.UnsignedInt
  type7_times     Int     @default(0) @db.UnsignedInt
  type0_pay_times Int     @default(0) @db.UnsignedInt
  type1_pay_times Int     @default(0) @db.UnsignedInt
  type2_pay_times Int     @default(0) @db.UnsignedInt
  type3_pay_times Int     @default(0) @db.UnsignedInt
  type4_pay_times Int     @default(0) @db.UnsignedInt
  type5_pay_times Int     @default(0) @db.UnsignedInt
  type6_pay_times Int     @default(0) @db.UnsignedInt
  type7_pay_times Int     @default(0) @db.UnsignedInt
  money           Decimal @default(0.0000) @db.Decimal(10, 4)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([server_id], map: "server_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sale_relation {
  id            Int     @id @default(autoincrement())
  admin_user_id Int
  parent_email  String  @db.VarChar(50)
  status        Boolean @default(false)
  create_time   Int
  update_time   Int?

  @@index([admin_user_id], map: "admin_user_id")
  @@index([parent_email], map: "parent_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sale_sign_record {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  customer_name String @default("") @db.VarChar(64)
  content       String @default("") @db.VarChar(256)
  address       String @default("") @db.VarChar(64)
  create_by     Int    @default(0)
  create_time   Int    @default(0)

  @@index([create_by], map: "idx_create_by")
}

model rcloud_sdk_zip {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  sdk_id      Int    @db.UnsignedInt
  sdk_url     String @db.VarChar(255)
  tags_url    String @db.VarChar(1000)
  status      Int    @db.TinyInt
  create_time Int    @db.UnsignedInt
}

model rcloud_sdk_zip_tags {
  id       Int    @id @default(autoincrement())
  zip_id   Int    @db.UnsignedInt
  urls     String @db.VarChar(1000)
  urls_key String @db.Char(32)
  status   Int    @db.TinyInt
}

model rcloud_sdk_zip_tags_copy {
  id       Int    @id @default(autoincrement())
  zip_id   Int    @db.UnsignedInt
  urls     String @db.VarChar(1000)
  urls_key String @db.Char(32)
  status   Int    @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_interface {
  id            Int    @id @default(autoincrement())
  server_id     Int?
  app_id        Int
  env           Int?   @db.SmallInt
  app_key       String @db.VarChar(255)
  admin_user_id Int
  status        Int    @default(2) @db.SmallInt
  ctime         Int
  utime         Int
  ok_time       Int
  user_id       Int

  @@index([app_id, server_id], map: "app_server_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_router {
  id                  Int     @id @default(autoincrement()) @db.UnsignedInt
  app_key             String  @default("") @db.VarChar(50)
  url                 String  @default("") @db.VarChar(200)
  developer_user_id   Int     @default(0)
  developer_user_name String  @default("") @db.VarChar(50)
  app_name            String  @default("") @db.VarChar(50)
  app_id              Int     @default(0)
  env                 Boolean @default(false)
  create_time         Int     @default(0)
  admin_user_id       Int     @default(0)
  release_time        Int     @default(0)
  status              Boolean @default(false)
  is_del              Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_status_center {
  id               Int      @id @default(autoincrement()) @db.MediumInt
  name             String   @default("") @db.VarChar(20)
  scope            String?  @db.VarChar(20)
  service_provider Int      @default(0) @db.UnsignedTinyInt
  data_center      Int      @default(0) @db.UnsignedTinyInt
  server_status    Int?     @default(1) @db.TinyInt
  show_status      Int      @default(1) @db.UnsignedTinyInt
  is_show          Int?     @default(0) @db.TinyInt
  auto_refresh     Int?     @default(0) @db.TinyInt
  sort             Int?     @default(0) @db.TinyInt
  is_del           Boolean? @default(false)
  create_time      String   @default("") @db.VarChar(12)
  update_time      String   @default("") @db.VarChar(12)
  delay_time       String   @db.VarChar(20)
  show_delay_time  String   @default("0") @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_status_display {
  id            Int      @id @default(autoincrement()) @db.MediumInt
  scope         String?  @db.VarChar(20)
  server_status Int?     @default(1) @db.TinyInt
  is_show       Int?     @default(0) @db.TinyInt
  auto_refresh  Int?     @default(0) @db.TinyInt
  sort          Int?     @default(0) @db.TinyInt
  is_del        Boolean? @default(false)
  name          String   @default("") @db.VarChar(20)
  explain       String?  @default("") @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_status_event {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  title        String  @db.VarChar(150)
  description  String? @db.Text
  display_type Int     @default(0) @db.UnsignedTinyInt
  event_type   Int     @default(1) @db.UnsignedTinyInt
  schedule     Int     @default(1) @db.UnsignedTinyInt
  is_del       Int     @default(0) @db.UnsignedTinyInt
  create_time  String  @db.VarChar(20)
  start_time   String? @default("") @db.VarChar(20)
  end_time     String? @default("") @db.VarChar(20)

  @@index([create_time], map: "index_1")
  @@index([display_type], map: "index_2")
  @@index([end_time], map: "index_3")
  @@index([start_time], map: "index_4")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_status_event_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  event_id    Int     @default(0) @db.UnsignedInt
  content     String? @db.Text
  is_del      Int     @default(0) @db.UnsignedTinyInt
  create_time String  @default("") @db.VarChar(20)
  update_time String  @default("") @db.VarChar(20)

  @@index([event_id], map: "index_1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_server_status_notice {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  title       String? @default("") @db.VarChar(100)
  content     String  @default("") @db.VarChar(255)
  status      Boolean @default(false)
  start_time  String  @default("") @db.VarChar(20)
  end_time    String  @default("") @db.VarChar(20)
  create_time String  @default("") @db.VarChar(20)
  is_del      Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_config {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  category     Int     @default(1)
  parent       Int     @default(0)
  need_opening Int     @default(0) @db.TinyInt
  sync         Int     @default(0) @db.TinyInt
  is_common    Int     @default(1) @db.TinyInt
  name         String  @default("") @db.VarChar(100)
  call_back    String  @default("") @db.VarChar(200)
  set_func     String  @default("") @db.VarChar(100)
  service      Int     @default(0)
  show         String  @default("") @db.VarChar(20)
  desc         String  @default("") @db.VarChar(255)
  tips         String  @default("") @db.VarChar(300)
  sort         Int     @default(0)
  admin_id     Int     @default(0)
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  config       String? @db.Text
  remark       String  @default("") @db.VarChar(100)
  is_del       Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_config_0712 {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  category     Int     @default(1)
  parent       Int     @default(0)
  need_opening Int     @default(0) @db.TinyInt
  sync         Int     @default(0) @db.TinyInt
  is_common    Int     @default(1) @db.TinyInt
  name         String  @default("") @db.VarChar(100)
  call_back    String  @default("") @db.VarChar(200)
  set_func     String  @default("") @db.VarChar(100)
  service      Int     @default(0)
  show         String  @default("") @db.VarChar(20)
  desc         String  @default("") @db.VarChar(255)
  tips         String  @default("") @db.VarChar(300)
  sort         Int     @default(0)
  admin_id     Int     @default(0)
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  config       String? @db.Text
  remark       String  @default("") @db.VarChar(100)
  is_del       Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_config_0713 {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  category     Int     @default(1)
  parent       Int     @default(0)
  need_opening Int     @default(0) @db.TinyInt
  sync         Int     @default(0) @db.TinyInt
  is_common    Int     @default(1) @db.TinyInt
  name         String  @default("") @db.VarChar(100)
  call_back    String  @default("") @db.VarChar(200)
  set_func     String  @default("") @db.VarChar(100)
  service      Int     @default(0)
  show         String  @default("") @db.VarChar(20)
  desc         String  @default("") @db.VarChar(255)
  tips         String  @default("") @db.VarChar(300)
  sort         Int     @default(0)
  admin_id     Int     @default(0)
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  config       String? @db.Text
  remark       String  @default("") @db.VarChar(100)
  is_del       Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_config_0718 {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  category     Int     @default(1)
  parent       Int     @default(0)
  need_opening Int     @default(0) @db.TinyInt
  sync         Int     @default(0) @db.TinyInt
  is_common    Int     @default(1) @db.TinyInt
  name         String  @default("") @db.VarChar(100)
  call_back    String  @default("") @db.VarChar(200)
  set_func     String  @default("") @db.VarChar(100)
  service      Int     @default(0)
  show         String  @default("") @db.VarChar(20)
  desc         String  @default("") @db.VarChar(255)
  tips         String  @default("") @db.VarChar(300)
  sort         Int     @default(0)
  admin_id     Int     @default(0)
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  config       String? @db.Text
  remark       String  @default("") @db.VarChar(100)
  is_del       Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_config_0718_1 {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  category     Int     @default(1)
  parent       Int     @default(0)
  need_opening Int     @default(0) @db.TinyInt
  sync         Int     @default(0) @db.TinyInt
  is_common    Int     @default(1) @db.TinyInt
  name         String  @default("") @db.VarChar(100)
  call_back    String  @default("") @db.VarChar(200)
  set_func     String  @default("") @db.VarChar(100)
  service      Int     @default(0)
  show         String  @default("") @db.VarChar(20)
  desc         String  @default("") @db.VarChar(255)
  tips         String  @default("") @db.VarChar(300)
  sort         Int     @default(0)
  admin_id     Int     @default(0)
  create_time  Int     @default(0)
  update_time  Int     @default(0)
  config       String? @db.Text
  remark       String  @default("") @db.VarChar(100)
  is_del       Boolean @default(false)

  @@map("rcloud_service_config_0718-1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_give_config {
  id          Int      @id @default(autoincrement())
  service_id  Int      @default(0)
  env         Boolean  @default(false)
  config      Json
  create_time DateTime @default(now()) @db.DateTime(0)
  update_time DateTime @default(now()) @db.DateTime(0)

  @@index([service_id], map: "service_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_limit_config {
  id             Int     @id @default(autoincrement())
  service_type   Int     @default(0) @db.UnsignedTinyInt
  service_id     Int     @default(0)
  pre_service_id Int     @default(0)
  default_config Int     @default(0)
  unit_config    Int     @default(0)
  unit_price     Int     @default(0)
  unit_name      String  @default("") @db.VarChar(50)
  params         String  @db.Text
  is_overseas    Boolean @default(false)
  status         Boolean @default(false)
  server_host    String  @default("") @db.VarChar(255)
  server_url     String  @default("") @db.VarChar(255)
  server_params  String  @default("") @db.VarChar(255)
  boss_policyId  Int     @default(0)
  order_by       Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_online {
  id      Int @id @default(autoincrement())
  app_id  Int @default(0)
  service Int @default(0)
  ctime   Int @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_recharge_config {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  service_id Int     @default(0) @db.UnsignedInt
  type       Int     @default(1) @db.UnsignedTinyInt
  unit_price Decimal @default(0.00) @db.Decimal(11, 2)
  policy_id  Int     @default(0) @db.UnsignedInt
  charge     Int     @default(0) @db.UnsignedInt
  price      Int     @default(0) @db.UnsignedInt
  tool_code  Int     @default(0) @db.UnsignedInt
  created_at Int     @db.UnsignedInt
  updated_at Int     @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_send_message {
  id             Int @id @default(autoincrement())
  app_id         Int @db.UnsignedInt
  service_id     Int @default(0) @db.UnsignedInt
  ctime          Int @db.UnsignedInt
  status         Int @default(0)
  send_time      Int @db.UnsignedInt
  real_send_time Int @default(0) @db.UnsignedInt
  type           Int @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_service_user {
  id              Int      @id @default(autoincrement()) @db.UnsignedSmallInt
  username        String   @unique(map: "username") @db.VarChar(30)
  password        String   @db.Char(32)
  role_id         String   @default("23") @db.VarChar(30)
  last_login_time Int      @db.UnsignedInt
  last_login_ip   String   @db.VarChar(40)
  login_count     Int?     @default(0) @db.UnsignedMediumInt
  email           String   @db.VarChar(50)
  create_time     Int      @db.UnsignedInt
  status          Boolean? @default(true)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_settings {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  team        String @db.VarChar(64)
  s_name      String @db.VarChar(64)
  s_value     String @db.VarChar(1000)
  status      Int
  create_time Int
  update_time Int

  @@index([s_name], map: "s_name")
  @@index([team], map: "team")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_shenji {
  id              Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id         Int
  name            String   @default("") @db.VarChar(60)
  email           String   @default("") @db.VarChar(100)
  create_time     Int      @default(0)
  contract_line   Int      @default(2) @db.TinyInt
  contract        Decimal? @default(0.00) @db.Decimal(11, 2)
  bank_money      Decimal? @default(0.00) @db.Decimal(11, 2)
  online_money    Decimal? @default(0.00) @db.Decimal(11, 2)
  consum_money    Decimal? @default(0.00) @db.Decimal(11, 2)
  invoice_money   Decimal? @default(0.00) @db.Decimal(11, 2)
  uninvoice_money Decimal? @default(0.00) @db.Decimal(11, 2)
  service_type    String?  @default("0") @db.VarChar(128)
  test            Int?     @default(0) @db.TinyInt
  date            String   @default("") @db.VarChar(16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_shenji_month {
  id              Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id         Int?
  name            String?  @default("") @db.VarChar(60)
  email           String?  @default("") @db.VarChar(100)
  create_time     Int?     @default(0)
  contract_line   Int?     @default(2) @db.TinyInt
  contract        Decimal? @default(0.00) @db.Decimal(11, 2)
  bank_money      Decimal? @default(0.00) @db.Decimal(11, 2)
  online_money    Decimal? @default(0.00) @db.Decimal(11, 2)
  consum_money    Decimal? @default(0.00) @db.Decimal(11, 2)
  invoice_money   Decimal? @default(0.00) @db.Decimal(11, 2)
  uninvoice_money Decimal? @default(0.00) @db.Decimal(11, 2)
  service_type    String?  @default("0") @db.VarChar(128)
  test            Int?     @default(0) @db.TinyInt
  date            String?  @default("") @db.VarChar(16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_shenji_month_2018 {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int?
  name         String?  @default("") @db.VarChar(60)
  email        String?  @default("") @db.VarChar(100)
  create_time  Int?     @default(0)
  consum_money Decimal? @default(0.00) @db.Decimal(11, 2)
  date         String?  @default("") @db.VarChar(16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_shumei_account {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int    @default(0) @db.UnsignedInt
  app_key      String @default("") @db.VarChar(255)
  access_key   String @default("") @db.VarChar(255)
  organization String @default("") @db.VarChar(255)
  name         String @default("") @db.VarChar(100)
  phone        String @default("") @db.VarChar(30)
  email        String @default("") @db.VarChar(127)
  status       Int    @default(1) @db.UnsignedTinyInt
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_shumei_check_statistic {
  id             Int      @id @default(autoincrement())
  user_id        Int
  app_id         Int
  server_id      Int
  env            Boolean  @default(false)
  product        String   @db.VarChar(50)
  date           Int      @default(0)
  total_value    Int      @default(0)
  review_value   Int      @default(0)
  pass_value     Int      @default(0)
  reject_value   Int      @default(0)
  review_details String   @default("") @db.VarChar(5000)
  reject_details String   @default("") @db.VarChar(5000)
  is_abroad      Int      @default(0) @db.UnsignedTinyInt
  is_activity    Boolean  @default(false)
  create_time    DateTime @default(now()) @db.Timestamp(0)
  update_time    DateTime @default(now()) @db.Timestamp(0)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sight_account {
  id      Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id  Int     @default(0) @db.UnsignedInt
  user_id Int     @default(0) @db.UnsignedInt
  ctime   Int     @default(0) @db.UnsignedInt
  total   Int     @default(0)
  consume Int     @default(0)
  surplus Int     @default(0)
  finance Int     @default(0) @db.UnsignedInt
  notice  Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sight_day {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  app_id       Int      @default(0) @db.UnsignedInt
  server_id    Int      @default(0) @db.UnsignedInt
  day          Int      @default(0) @db.UnsignedInt
  tally        Int      @default(0)
  status       Int      @default(1) @db.UnsignedTinyInt
  error_msg    String   @default("") @db.VarChar(255)
  sight_switch Boolean? @default(false)
  amount       Decimal? @default(0.0000) @db.Decimal(12, 4)

  @@index([app_id], map: "app_id")
  @@index([server_id], map: "server_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sight_order {
  id       Int     @id @default(autoincrement())
  order_id String  @default("0") @db.VarChar(30)
  user_id  Int     @default(0)
  app_id   Int     @default(0)
  payment  Int     @default(0) @db.TinyInt
  money    Decimal @default(0) @db.Decimal(11, 0)
  total    Int     @default(0)
  status   Boolean @default(true)
  ctime    Int     @default(0)
  utime    Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sight_reminder {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  app_id      Int     @default(0) @db.UnsignedInt
  create_time String  @default("") @db.VarChar(12)
  send_time   String  @default("") @db.VarChar(12)
  send_number Int     @default(0) @db.UnsignedTinyInt
  stop_time   String  @default("") @db.VarChar(12)
  is_del      Boolean @default(false)
  remark      String? @default("") @db.VarChar(255)

  @@index([send_time], map: "send_time")
  @@index([stop_time], map: "stop_time")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_account {
  id               Int  @id @default(autoincrement())
  user_id          Int
  total            Int  @default(0)
  consumer         Int  @default(0)
  surplus          Int  @default(0)
  send_surplus     Int  @default(0)
  stime            Int  @default(0)
  ctime            Int
  notice           Int? @default(0) @db.SmallInt
  notice_threshold Int  @default(100)

  @@index([user_id], map: "user_id")
}

model rcloud_sms_account_receive {
  id      Int @id @default(autoincrement())
  user_id Int @unique(map: "user_id_UNIQUE")
  cate    Int
  num     Int
  ctime   Int
}

model rcloud_sms_box {
  id          Int    @id @default(autoincrement())
  mobile      Float  @default(0)
  content     String @default("") @db.VarChar(512)
  log_id      Int    @default(0)
  create_time Float
  result      String @default("") @db.VarChar(512)
  code        Int?   @default(0)
}

model rcloud_sms_box_2023 {
  id          Int    @id @default(autoincrement())
  mobile      Float  @default(0)
  content     String @default("") @db.VarChar(512)
  log_id      Int    @default(0)
  create_time Float
  result      String @default("") @db.VarChar(512)
  code        Int?   @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_chenxi_stat {
  id          Int      @id @default(autoincrement())
  user_id     Int      @default(0)
  app_id      Int      @default(0)
  env         Int      @default(0) @db.TinyInt
  date        Int      @default(0)
  providers   Int      @default(0) @db.TinyInt
  type        Int      @default(0) @db.TinyInt
  total       Int      @default(0)
  success     Int      @default(0)
  deliver     Int      @default(0)
  safe        Int      @default(0)
  data        String   @db.Text
  money       Decimal  @default(0.0000) @db.Decimal(12, 4)
  create_time DateTime @default(now()) @db.Timestamp(0)
  update_time DateTime @default(now()) @db.Timestamp(0)

  @@index([total], map: "foreign_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_group {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0)
  app_key     String  @default("") @db.VarChar(50)
  month       String  @default("") @db.VarChar(20)
  total       Int     @default(0)
  create_time Int     @default(0)
  update_time Int     @default(0)
  is_del      Boolean @default(false)
  money       Decimal @default(0.0000) @db.Decimal(15, 4)
  type        Boolean @default(false)
  provider    String  @default("") @db.VarChar(10)

  @@index([app_key], map: "app_key_index")
  @@index([user_id], map: "user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_international_detail {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  app_id      Int     @default(0) @db.UnsignedInt
  status      Int     @default(0) @db.UnsignedTinyInt
  consumer    Decimal @default(0.0000) @db.Decimal(15, 4)
  date        String  @default("") @db.VarChar(15)
  create_time String  @default("") @db.VarChar(15)
  update_time String  @default("") @db.VarChar(15)
  is_del      Int     @default(0) @db.UnsignedTinyInt
  remark      String  @default("") @db.VarChar(200)
  config      String? @db.Text

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_log {
  id              Int      @id @default(autoincrement())
  admin_id        Int?     @default(0)
  content         String?  @default("") @db.VarChar(512)
  create_time     String?  @default("0") @db.VarChar(32)
  num             Int?     @default(0)
  target          String?  @default("") @db.VarChar(16)
  admin_user_name String?  @default("") @db.VarChar(16)
  sms_type        Boolean? @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_market_account {
  id               Int     @id @default(autoincrement())
  user_id          Int
  total            Int     @default(0)
  consumer         Int     @default(0)
  surplus          Int     @default(0)
  send_surplus     Int     @default(0)
  update_time      String  @default("") @db.VarChar(15)
  create_time      String  @default("") @db.VarChar(15)
  notice           Int     @default(0) @db.TinyInt
  notice_threshold Int     @default(100)
  config           String? @default("") @db.VarChar(255)
  is_del           Boolean @default(false)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_market_config {
  id       Int    @id @default(autoincrement()) @db.UnsignedInt
  type     Int    @default(0) @db.UnsignedTinyInt
  service  Int    @default(0) @db.UnsignedTinyInt
  account  String @default("") @db.VarChar(200)
  showName String @default("") @db.VarChar(20)
  remark   String @default("") @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_operation_log {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  relation_id Int    @default(0) @db.UnsignedInt
  type        Int    @default(0) @db.UnsignedTinyInt
  admin_id    Int    @default(0) @db.UnsignedInt
  content     String @db.Text
  create_time String @default("") @db.VarChar(12)
  update_time String @default("") @db.VarChar(12)
  remark      String @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_order {
  id       Int     @id @default(autoincrement())
  order_id BigInt
  user_id  Int
  payment  Int     @db.TinyInt
  price    Decimal @db.Decimal(11, 3)
  money    Decimal @db.Decimal(11, 0)
  total    Int
  status   Boolean
  ctime    Int
  utime    Int
}

model rcloud_sms_order_line {
  id       Int     @id @default(autoincrement())
  user_id  Int     @default(0)
  account  Int     @default(0)
  total    Int     @default(0)
  sum      Int     @default(0)
  money    Decimal @default(0.00) @db.Decimal(11, 2)
  status   Int     @default(0)
  admin_id Int?    @default(0)
  audit_id Int?    @default(0)
  attr     String? @default("") @db.VarChar(255)
  reason   String? @default("") @db.VarChar(255)
  remark   String? @default("") @db.VarChar(255)
  ctime    Int     @default(0)
  utime    Int?    @default(0)
  invoice  Int?    @default(0)
  cdate    String? @db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_sign {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  appid      String   @db.VarChar(64)
  name       String?  @default("") @db.VarChar(200)
  content    String?  @default("") @db.VarChar(200)
  subcode    String?  @db.Char(5)
  status     Int?     @default(0) @db.TinyInt
  createtime Int?
  updatetime Int?
  detail     String?  @default("") @db.VarChar(500)
  admin_id   Int?     @default(0)
  is_del     Boolean? @default(false)
  source     Int?     @default(0) @db.TinyInt

  @@index([status], map: "NewIndex1")
  @@index([appid], map: "rcloud_sms_sign_appid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_sign_template {
  id             Int    @id @default(autoincrement()) @db.UnsignedInt
  templateid     Int
  signid         Int
  appid          Int
  javatemplateid String @db.VarChar(32)
  createtime     Int?
  type           Int?   @default(1) @db.TinyInt
  status         Int?   @default(1) @db.TinyInt

  @@index([appid], map: "NewIndex1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_stat {
  id                Int     @id @default(autoincrement())
  app_id            Int
  env               Int     @db.TinyInt
  user_id           Int     @default(0)
  real_app_id       Int
  app_key           String  @db.VarChar(50)
  send_num          Int
  send_success      Int
  send_success_rate Decimal @db.Decimal(11, 2)
  verifiy_num       Int
  verifiy_rate      Decimal @db.Decimal(11, 2)
  ctime             Int

  @@index([app_id], map: "app_id")
  @@index([ctime], map: "create_time")
  @@index([real_app_id], map: "real_app")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_template {
  id               Int      @id @default(autoincrement()) @db.UnsignedInt
  name             String?  @db.VarChar(64)
  content          String?  @default("") @db.VarChar(500)
  type             Int?     @db.TinyInt
  status           Boolean? @default(true)
  createtime       Int?
  appid            Int?
  mid              String?  @default("") @db.VarChar(50)
  scontent         String?  @db.VarChar(500)
  lsm_mid          String?  @default("") @db.VarChar(50)
  lsm_remark       String?  @default("") @db.VarChar(500)
  lsm_status       Int?     @default(0) @db.TinyInt
  admin_id         Int?     @default(0)
  server_id        String?  @default("") @db.VarChar(45)
  update_time      String?  @default("") @db.VarChar(12)
  config           String?  @default("") @db.VarChar(200)
  sign_id          Int?     @default(0)
  variate_describe String?  @db.Text
  reason           String?  @default("") @db.VarChar(200)
  template_status  Boolean? @default(false)
  is_del           Boolean? @default(false)
  sms_package      Boolean? @default(false)
  source           Int?     @default(0) @db.TinyInt
  xyc_status       Int?     @default(0) @db.TinyInt
  xyc_remark       String?  @default("") @db.VarChar(200)
  market_type      Int?     @default(0) @db.TinyInt
  yun_mid          String?  @default("") @db.VarChar(50)
  yun_status       Int?     @default(0) @db.TinyInt
  yun_remark       String?  @default("") @db.VarChar(200)
  account          String?  @default("") @db.VarChar(200)
  market_service   Int?     @default(0) @db.TinyInt

  @@index([lsm_mid], map: "lsm_mid")
  @@index([mid], map: "mid")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_template1121_1 {
  id         Int      @default(0) @db.UnsignedInt
  name       String?  @db.VarChar(64)
  content    String?  @default("") @db.VarChar(500)
  type       Int?     @db.TinyInt
  status     Boolean? @default(true)
  createtime Int?
  appid      Int?
  mid        Int?     @default(0)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_template_1203 {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  name       String?  @db.VarChar(64)
  content    String?  @default("") @db.VarChar(500)
  type       Int?     @db.TinyInt
  status     Boolean? @default(true)
  createtime Int?
  appid      Int?
  mid        String?  @default("") @db.VarChar(50)
  scontent   String?  @db.VarChar(500)
  lsm_mid    String?  @default("") @db.VarChar(50)
  lsm_remark String?  @default("") @db.VarChar(500)
  lsm_status Int?     @default(0) @db.TinyInt

  @@index([lsm_mid], map: "lsm_mid")
  @@index([mid], map: "mid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sms_template_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id      Int     @default(0) @db.UnsignedInt
  type        Int     @default(1) @db.UnsignedTinyInt
  content     String? @db.Text
  tid         Int     @default(0) @db.UnsignedInt
  params      String? @db.Text
  create_time String? @default("") @db.VarChar(12)
  config      String? @default("") @db.VarChar(100)

  @@index([app_id], map: "app_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_sql_log {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  admin_id    Int
  info        String @default("") @db.VarChar(5000)
  create_time Int
}

model rcloud_stat_contrast {
  id       Int    @id @default(autoincrement())
  user_id  Int    @default(0)
  app_id   Int    @default(0)
  tag      String @default("") @db.VarChar(255)
  contrast Int    @default(0)
  split    Int    @default(0)
  date     Int    @default(0)
  ctime    Int    @default(0)
  day      Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_stat_flow {
  id    Int     @id @default(autoincrement())
  ctime String  @default("0") @db.VarChar(11)
  ip    Int     @default(0)
  pv    Int     @default(0)
  uv    Int     @default(0)
  site  String? @db.VarChar(20)
}

model rcloud_stat_page {
  id      Int    @id @default(autoincrement())
  user_id Int    @default(0)
  app_id  Int    @default(0)
  tag     String @default("") @db.VarChar(255)
  name    String @default("") @db.VarChar(255)
  ctime   Int    @default(0)
  day     Int    @default(0)
  tlong   Int?   @default(0)
}

model rcloud_stat_user_plat {
  id      Int    @id @default(autoincrement())
  user_id Int    @default(0)
  app_id  Int    @default(0)
  tag     String @default("") @db.VarChar(255)
  plat    String @default("") @db.VarChar(255)
  ctime   Int    @default(0)
  day     Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_subscribe {
  id          Int     @id @default(autoincrement()) @db.MediumInt
  username    String  @db.VarChar(16)
  mobile      String  @default("") @db.VarChar(11)
  email       String  @default("") @db.VarChar(40)
  invite_code String  @default("") @db.Char(10)
  token       String  @default("") @db.Char(32)
  application String  @db.Text
  client_ip   String  @db.VarChar(15)
  create_time Int
  status      Boolean @default(false)
}

model rcloud_switch {
  id     Int     @id @default(autoincrement()) @db.UnsignedInt
  type   String? @db.VarChar(16)
  switch Int?
}

model rcloud_tag_relation {
  id         Int  @id @default(autoincrement()) @db.UnsignedInt
  customer   Int  @default(0) @db.UnsignedInt
  tag        Int  @default(0) @db.UnsignedInt
  user_id    Int  @default(0) @db.UnsignedInt
  created_at Int  @default(0) @db.UnsignedInt
  updated_at Int  @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_talk_show {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0)
  title       String  @default("") @db.VarChar(200)
  category    Int     @default(0) @db.UnsignedTinyInt
  create_time Int     @default(0)
  update_time Int     @default(0)
  is_del      Boolean @default(false)
  content     String? @db.Text
  remark      String  @default("") @db.VarChar(200)
  status      Int?    @default(1) @db.TinyInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_talk_show_reply {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  talk_id     Int     @default(0)
  admin_id    Int     @default(0)
  create_time Int     @default(0)
  update_time Int     @default(0)
  content     String? @db.Text
  remark      String  @default("") @db.VarChar(200)
  is_del      Boolean @default(false)

  @@index([talk_id], map: "talk_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_talk_show_template {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  admin_id    Int     @default(0)
  content     String? @db.Text
  create_time Int     @default(0)
  update_time Int     @default(0)
  is_del      Boolean @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tel_ticket {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int     @default(0) @db.UnsignedInt
  app_id       Int     @default(0) @db.UnsignedInt
  source       Int     @default(0) @db.UnsignedTinyInt
  phone        String  @default("") @db.VarChar(15)
  platform     Int     @default(0) @db.UnsignedTinyInt
  config       String? @default("") @db.VarChar(100)
  param        String? @default("") @db.VarChar(100)
  status       Int     @default(0) @db.UnsignedTinyInt
  is_del       Int     @default(0) @db.UnsignedTinyInt
  create_time  String  @default("") @db.VarChar(12)
  update_time  String  @default("") @db.VarChar(12)
  admin_id     Int     @default(0)
  support_time Int     @default(0)
  support_id   Int     @default(0) @db.UnsignedInt
  description  String? @db.Text
  env          Int     @default(1) @db.UnsignedTinyInt
  ticket_id    Int?    @default(0)

  @@index([app_id], map: "app_id")
  @@index([ticket_id], map: "ticket_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tel_ticket_log {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  log_content  String  @default("") @db.VarChar(255)
  ticket_id    Int
  admin_id     Int     @default(0)
  create_time  String  @default("") @db.VarChar(12)
  update_time  String  @default("") @db.VarChar(12)
  is_del       Boolean @default(false)
  support_id   Int     @default(0) @db.UnsignedInt
  support_name String  @default("") @db.VarChar(15)

  @@index([ticket_id], map: "ticket_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket {
  id                     Int                      @id @default(autoincrement()) @db.UnsignedInt
  title                  String                   @default("") @db.VarChar(200)
  content                String?                  @db.Text
  tag                    String                   @default("") @db.VarChar(100)
  status                 Boolean                  @default(true)
  create_time            Int                      @default(0)
  user_id                Int                      @default(0)
  close_user_id          Int                      @default(0)
  close_admin_user_id    Int                      @default(0)
  num                    Int                      @default(0)
  is_read                Boolean                  @default(false)
  last_reply_time        Int                      @default(0)
  lock_status            Boolean                  @default(false)
  lock_admin_user_id     Int                      @default(0)
  lock_time              Int                      @default(0)
  platform               Int?                     @default(0)
  sdk_id                 Int                      @default(0)
  is_admin_reply         Boolean                  @default(false)
  is_develop_reply       Boolean                  @default(false)
  article_id             Int                      @default(0)
  slack_push             Boolean                  @default(false)
  ticket_type            Int                      @default(0) @db.SmallInt
  ticket_test_type       Int                      @default(0) @db.SmallInt
  ticket_component       String?                  @default("") @db.VarChar(50)
  is_public              Boolean?                 @default(true)
  vesion_info            String?                  @default("") @db.VarChar(16)
  app_id                 Int?                     @default(0)
  kaifa_id               Int?                     @default(0) @db.TinyInt
  func_id                Int?                     @default(0) @db.TinyInt
  system_id              Int?                     @default(0) @db.TinyInt
  delayed                Boolean?                 @default(false)
  tag_id                 String                   @default("") @db.VarChar(32)
  first                  Int                      @default(1) @db.TinyInt
  mobile                 String?                  @default("") @db.VarChar(15)
  email                  String                   @default("") @db.VarChar(127)
  create_role            Int?                     @default(0) @db.TinyInt
  exceed_reply           Boolean?                 @default(false)
  rcloud_ticket_transfer rcloud_ticket_transfer[]

  @@index([is_read], map: "index_isRead")
  @@index([user_id], map: "index_name")
  @@index([is_admin_reply, is_develop_reply], map: "index_newreply")
  @@index([status], map: "index_status")
  @@index([last_reply_time], map: "ix_last_reply_time")
  @@index([status, is_public, platform], map: "ix_status_public_plat")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_alert {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  time_type  Int?   @unique(map: "alert_type") @db.TinyInt
  content    String @db.VarChar(256)
  createtime Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_category_remind {
  id        Int      @id @default(autoincrement())
  business  String?  @default("") @db.VarChar(512)
  platform  String?  @default("") @db.VarChar(24)
  os        String?  @default("") @db.VarChar(24)
  category  String   @db.VarChar(255)
  tip       String?  @db.LongText
  template  String?  @db.Text
  status    Int      @default(0) @db.TinyInt
  operator  String?  @db.VarChar(255)
  last_data String?  @db.LongText
  update_at DateTime @default(now()) @db.DateTime(0)
  create_at DateTime @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_delay {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  ticket_id   Int     @default(0) @db.UnsignedInt
  overtime    String  @default("0") @db.VarChar(20)
  date        Int     @default(0) @db.UnsignedInt
  admin_id    Int     @default(0) @db.UnsignedInt
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Boolean @default(false)
  remark      String? @db.Text

  @@index([admin_id], map: "admin_id")
  @@index([date], map: "date")
  @@index([ticket_id], map: "ticket_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_grade {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  ticket_id    Int     @default(0) @db.UnsignedInt
  type         Int     @default(0) @db.UnsignedTinyInt
  status       Int     @default(0) @db.UnsignedTinyInt
  grade        Int     @default(1) @db.UnsignedTinyInt
  explain      String? @default("") @db.VarChar(50)
  is_del       Int     @default(0) @db.UnsignedTinyInt
  create_time  String  @default("") @db.VarChar(12)
  update_time  String  @default("") @db.VarChar(12)
  content      String? @db.Text
  remark       String  @default("") @db.VarChar(255)
  is_replace   Int     @default(0) @db.TinyInt
  audit_status Int?    @default(1) @db.TinyInt
  audit_admin  Int?    @default(0)

  @@index([ticket_id], map: "ticket_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_grade_explain {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  grade_id    Int    @default(0) @db.UnsignedInt
  type        Int    @default(0) @db.UnsignedTinyInt
  is_del      Int    @default(0) @db.UnsignedTinyInt
  create_time String @default("") @db.VarChar(12)
  admin_id    Int    @default(0) @db.UnsignedInt
  explain     String @default("") @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_label {
  id           Int      @id @default(autoincrement())
  level        Int      @default(0) @db.TinyInt
  relevance_id Int      @default(0)
  status       Boolean  @default(true)
  name         String   @default("") @db.VarChar(50)
  admin_uid    Int      @default(0)
  create_time  DateTime @default(now()) @db.DateTime(0)
  update_time  DateTime @default(now()) @db.DateTime(0)

  @@index([level], map: "level")
  @@index([relevance_id], map: "relevance_id")
  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_label_relation {
  id          Int      @id @default(autoincrement())
  ticket_id   Int      @default(0)
  label_id    Int      @default(0)
  level       Int      @default(0)
  admin_uid   Int      @default(0)
  create_time DateTime @default(now()) @db.DateTime(0)

  @@index([label_id], map: "label_id")
  @@index([ticket_id], map: "ticket_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_leader {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  admin_id    Int
  user_id     String   @db.VarChar(32)
  leader_id   String   @db.VarChar(32)
  create_time DateTime @default(now()) @db.Timestamp(0)
  status      Boolean  @default(true)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_log {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  log_content String?   @db.VarChar(500)
  ticket_id   Int?      @db.UnsignedInt
  admin_id    Int?      @db.UnsignedSmallInt
  op_name     String?   @db.VarChar(20)
  create_time Int?      @db.UnsignedInt
  update_time DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_onduty {
  id          Int       @id @default(autoincrement())
  uid         Int       @default(0) @db.UnsignedSmallInt
  week        Int?      @default(1) @db.UnsignedSmallInt
  start_time  String?   @default("09:30") @db.VarChar(10)
  end_time    String?   @default("18:30") @db.VarChar(10)
  otime_id    Int?      @db.UnsignedInt
  platform    Int?      @default(1) @db.UnsignedSmallInt
  create_time DateTime? @default(now()) @db.Timestamp(0)
  update_time Int?      @db.UnsignedInt
  category    Int?      @default(1) @db.TinyInt

  @@index([uid], map: "FK_ADMINUSER_ID")
  @@index([otime_id], map: "FK_ONDUTY_TIME_ID")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_onduty_copy {
  id          Int       @id @default(autoincrement())
  uid         Int       @default(0) @db.UnsignedSmallInt
  week        Int?      @default(1) @db.UnsignedSmallInt
  start_time  String?   @default("09:30") @db.VarChar(10)
  end_time    String?   @default("18:30") @db.VarChar(10)
  otime_id    Int?      @db.UnsignedInt
  platform    Int?      @default(1) @db.UnsignedSmallInt
  create_time DateTime? @default(now()) @db.Timestamp(0)
  update_time Int?      @db.UnsignedInt
  category    Int?      @default(1) @db.TinyInt

  @@index([uid], map: "FK_ADMINUSER_ID")
  @@index([otime_id], map: "FK_ONDUTY_TIME_ID")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_onduty_date {
  id          Int       @id @default(autoincrement())
  date        Int       @default(0)
  uid         Int       @default(0) @db.UnsignedSmallInt
  week        Int?      @default(1) @db.UnsignedSmallInt
  start_time  String?   @default("09:30") @db.VarChar(10)
  end_time    String?   @default("18:30") @db.VarChar(10)
  otime_id    Int?      @db.UnsignedInt
  platform    Int?      @default(1) @db.UnsignedSmallInt
  create_time DateTime? @default(now()) @db.Timestamp(0)
  update_time Int?      @db.UnsignedInt
  category    Int?      @default(1) @db.TinyInt

  @@index([uid], map: "FK_ADMINUSER_ID")
  @@index([otime_id], map: "FK_ONDUTY_TIME_ID")
  @@index([date], map: "date")
}

model rcloud_ticket_onduty_remind {
  id              Int    @id @default(autoincrement())
  one             String @default("") @db.VarChar(1024)
  two             String @default("") @db.VarChar(1024)
  three           String @default("") @db.VarChar(1024)
  four            String @default("") @db.VarChar(1024)
  five            String @default("") @db.VarChar(1024)
  six             String @db.VarChar(1024)
  seven           String @db.VarChar(1024)
  create_time     Int    @default(0)
  admin_user_id   Int    @default(0)
  admin_user_name String @default("") @db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_onduty_time {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  start_time  String?   @db.VarChar(10)
  end_time    String?   @db.VarChar(10)
  admin_id    Int       @db.UnsignedInt
  create_time DateTime? @default(now()) @db.Timestamp(0)
  update_time Int?      @db.UnsignedInt
}

model rcloud_ticket_onduty_user {
  id       Int     @id @default(autoincrement())
  platform Int     @default(0) @db.TinyInt
  time_id  Int     @default(0)
  week_id  Boolean @default(false)
  times    Int     @default(0)
  ctime    Int     @default(0)
  user_id  Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_remind {
  id             Int    @id @default(autoincrement()) @db.UnsignedInt
  ticket_type    Int    @db.TinyInt
  date_attribute Int    @db.TinyInt
  starttime      String @default("0") @db.VarChar(11)
  endtime        String @default("0") @db.VarChar(11)
  createtime     String @db.VarChar(11)
  content        String @db.VarChar(256)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_reply {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  ticket_id       Int       @default(0)
  user_id         Int       @default(0)
  content         String?   @db.Text
  create_time     Int       @default(0)
  status          Boolean   @default(false)
  reply_time      DateTime? @db.Timestamp(0)
  last_reply_time Int?
  is_read         Boolean   @default(false)
  admin_user_id   Int       @default(0)
  is_attachment   Boolean   @default(false)
  is_new          Boolean   @default(false)
  reply_type      Int       @default(0) @db.TinyInt
  score           Int?      @default(0) @db.TinyInt
  score_time      String?   @default("") @db.VarChar(12)

  @@index([admin_user_id], map: "index_admin_user_id")
  @@index([ticket_id], map: "index_ticketId")
  @@index([user_id], map: "ix_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_reply_limit {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  work        Int    @default(0) @db.UnsignedTinyInt
  first       Int    @default(1) @db.UnsignedTinyInt
  type        Int    @default(1) @db.UnsignedTinyInt
  limit_type  Int    @default(1) @db.UnsignedTinyInt
  limit_time  String @default("0") @db.VarChar(11)
  start_time  Int    @default(0) @db.UnsignedInt
  end_time    Int    @default(0) @db.UnsignedInt
  create_time String @default("") @db.VarChar(15)
  update_time String @default("") @db.VarChar(15)
  remark      String @default("") @db.VarChar(100)
  is_del      Int    @default(0) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_support_change {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  ticket_id   Int     @default(0) @db.UnsignedInt
  type        Int     @default(0) @db.UnsignedTinyInt
  status      Int     @default(0) @db.UnsignedTinyInt
  is_del      Int     @default(0) @db.UnsignedTinyInt
  support_id  Int     @default(0)
  admin_id    Int     @default(0)
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  phone       String  @default("") @db.VarChar(20)
  reason      String  @default("") @db.VarChar(255)
  remark      String? @db.Text

  @@index([support_id], map: "support_id")
  @@index([ticket_id], map: "ticket_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_support_statistics {
  id                     Int     @id @default(autoincrement()) @db.UnsignedInt
  admin_id               Int     @default(0) @db.UnsignedInt
  admin_name             String  @default("") @db.VarChar(20)
  ticket_total           Int     @default(0)
  ticket_reply_total     Int     @default(0)
  overtime_total         Int     @default(0)
  overtime_rate          Decimal @default(0.00) @db.Decimal(6, 2)
  evaluate_total         Int     @default(0)
  evaluate_negative      Int     @default(0)
  evaluate_negative_rate Decimal @default(0.00) @db.Decimal(6, 2)
  evaluate_medium        Int     @default(0)
  evaluate_medium_rate   Decimal @default(0.00) @db.Decimal(6, 2)
  evaluate_good          Int     @default(0)
  evaluate_good_rate     Decimal @default(0.00) @db.Decimal(6, 2)
  tel_support_total      Int     @default(0)
  change_total           Int     @default(0)
  date                   Int     @default(0)
  is_del                 Boolean @default(false)
  remark                 String? @default("") @db.VarChar(255)

  @@index([admin_id], map: "admin_id")
  @@index([date], map: "date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_tag {
  id       Int     @id @default(autoincrement()) @db.UnsignedInt
  admin_id Int
  name     String  @db.VarChar(32)
  status   Boolean @default(true)
  platform Int?    @default(-1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_talk {
  id           Int      @id @default(autoincrement())
  ticket_id    BigInt
  talk_mate_id Int      @default(0) @db.SmallInt
  talk_notes   String?  @db.VarChar(512)
  ctime        DateTime @default(now()) @db.Timestamp(0)

  @@index([ticket_id], map: "ticket_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_talk_mate {
  mate_id  Int       @id @default(autoincrement())
  m_title  String    @db.VarChar(250)
  m_note   String?   @db.VarChar(250)
  m_ctime  DateTime? @db.Timestamp(0)
  m_status Int       @default(0) @db.SmallInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_ticket_transfer {
  id                Int                @id @default(autoincrement()) @db.UnsignedInt
  ticket_id         Int?               @db.UnsignedInt
  admin_id          Int?               @db.UnsignedSmallInt
  transfer_uid      Int?               @db.UnsignedSmallInt
  is_current        Int                @default(1) @db.UnsignedSmallInt
  create_time       Int?               @db.UnsignedInt
  update_time       DateTime?          @db.Timestamp(0)
  transfer_content  String?            @db.Text
  rcloud_admin_user rcloud_admin_user? @relation(fields: [admin_id], references: [id], onDelete: Cascade, map: "FK_admin_user_id")
  rcloud_ticket     rcloud_ticket?     @relation(fields: [ticket_id], references: [id], onDelete: Cascade, map: "FK_ticket_id")

  @@index([admin_id], map: "FK_admin_user_id")
  @@index([ticket_id], map: "FK_ticket_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tools_task {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  type        Int     @default(0) @db.UnsignedInt
  status      Int     @default(0) @db.UnsignedTinyInt
  fixed_param String? @db.Text
  data        String? @db.Text
  deal_result String? @db.Text
  remark      String  @default("") @db.VarChar(255)
  ctime       Int     @default(0) @db.UnsignedInt
  utime       Int     @default(0) @db.UnsignedInt
  admin_id    Int     @default(0) @db.UnsignedInt

  @@index([status, type, utime], map: "idx_status_type_utime")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tools_task_detail {
  id          Int     @id @default(autoincrement())
  type        Int     @default(0) @db.UnsignedTinyInt
  task_id     Int     @default(0) @db.UnsignedInt
  item_data   String  @default("") @db.VarChar(255)
  data        String  @db.Text
  result_code Boolean @default(false)
  result_info String  @default("") @db.VarChar(255)
  ctime       Int     @default(0) @db.UnsignedInt

  @@index([task_id], map: "idx_task_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  name        String @default("") @db.VarChar(64)
  desc        String @default("") @db.VarChar(256)
  is_del      Int    @default(0) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track_detail {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  type        Int    @default(0) @db.TinyInt
  code        String @default("0") @db.VarChar(32)
  desc        String @default("0") @db.VarChar(256)
  is_del      Int    @default(0) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)

  @@index([track_id], map: "idx_track_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track_op_log {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  user_id     Int    @default(0)
  user_token  String @default("0") @db.VarChar(256)
  event_type  String @default("0") @db.VarChar(64)
  from_page   String @default("0") @db.VarChar(64)
  from_module String @default("0") @db.VarChar(64)
  ext_data    String @db.Text
  op_date     Int    @default(0)
  create_time Int    @default(0)

  @@index([op_date], map: "idx_op_date")
  @@index([track_id], map: "idx_track_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track_op_log_2021 {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  user_id     Int    @default(0)
  user_token  String @default("0") @db.VarChar(256)
  event_type  String @default("0") @db.VarChar(32)
  from_page   String @default("0") @db.VarChar(32)
  from_module String @default("0") @db.VarChar(32)
  ext_data    String @db.Text
  op_date     Int    @default(0)
  create_time Int    @default(0)

  @@index([op_date], map: "idx_op_date")
  @@index([track_id], map: "idx_track_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track_op_log_2022 {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  user_id     Int    @default(0)
  user_token  String @default("0") @db.VarChar(256)
  event_type  String @default("0") @db.VarChar(64)
  from_page   String @default("0") @db.VarChar(64)
  from_module String @default("0") @db.VarChar(64)
  ext_data    String @db.Text
  op_date     Int    @default(0)
  create_time Int    @default(0)

  @@index([op_date], map: "idx_op_date")
  @@index([track_id], map: "idx_track_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track_op_log_2023 {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  user_id     Int    @default(0)
  user_token  String @default("0") @db.VarChar(256)
  event_type  String @default("0") @db.VarChar(64)
  from_page   String @default("0") @db.VarChar(64)
  from_module String @default("0") @db.VarChar(64)
  ext_data    String @db.Text
  op_date     Int    @default(0)
  create_time Int    @default(0)

  @@index([op_date], map: "idx_op_date")
  @@index([track_id], map: "idx_track_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_track_op_log_2024 {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  track_id    Int    @default(0)
  user_id     Int    @default(0)
  user_token  String @default("0") @db.VarChar(256)
  event_type  String @default("0") @db.VarChar(64)
  from_page   String @default("0") @db.VarChar(64)
  from_module String @default("0") @db.VarChar(64)
  ext_data    String @db.Text
  op_date     Int    @default(0)
  create_time Int    @default(0)

  @@index([op_date], map: "idx_op_date")
  @@index([track_id], map: "idx_track_id")
}

model rcloud_trade {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  parent_id  Int    @default(0) @db.UnsignedInt
  name       String @default("") @db.VarChar(50)
  admin      Int    @default(0) @db.UnsignedInt
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

model rcloud_trade_test {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  parent_id  Int    @default(0) @db.UnsignedInt
  name       String @default("") @db.VarChar(50)
  admin      Int    @default(0) @db.UnsignedInt
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tupu_account {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int    @default(0) @db.UnsignedInt
  register_info String @default("") @db.VarChar(255)
  account_id    String @default("") @db.VarChar(255)
  account_name  String @default("") @db.VarChar(100)
  created_at    Int    @default(0)
  updated_at    Int    @default(0)

  @@index([user_id], map: "rcloud_tupu_account_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tupu_app {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  account_id   Int     @default(0) @db.UnsignedInt
  user_id      Int     @default(0) @db.UnsignedInt
  env          Boolean @default(false)
  app_key      String  @default("") @db.VarChar(50)
  conversation String  @default("") @db.VarChar(50)
  type         String  @default("") @db.VarChar(20)
  free         Int     @default(0)
  app_name     String  @default("") @db.VarChar(100)
  task_id      Json
  app_id       String  @default("") @db.VarChar(100)
  created_at   Int     @default(0)
  updated_at   Int     @default(0)

  @@index([account_id], map: "rcloud_tupu_account_account_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_tupu_statistic {
  id             Int     @id @default(autoincrement())
  user_id        Int     @default(0)
  app_id         Int     @default(0)
  is_abroad      Boolean @default(false)
  env            Boolean @default(false)
  date           Int     @default(0)
  conversation   String  @default("'") @db.VarChar(50)
  type           String  @default("") @db.VarChar(20)
  tupu_app_id    String  @default("") @db.VarChar(100)
  tupu_task_id   String  @default("") @db.VarChar(100)
  total_value    Int     @default(0)
  succeed_value  Int     @default(0)
  failed_value   Int     @default(0)
  review_value   Int     @default(0)
  duration_value Int     @default(0)
  pay_value      Int     @default(0)
  money          Decimal @default(0.0000) @db.Decimal(12, 4)
  created_at     Int     @default(0)
  updated_at     Int     @default(0)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_universal_key_value {
  id          Int      @id @default(autoincrement())
  category    String   @db.VarChar(255)
  attname     String   @db.VarChar(255)
  attvalue    String?  @db.LongText
  description String?  @db.VarChar(255)
  updated     DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user {
  id                   Int       @id @default(autoincrement())
  name                 String    @default("") @db.VarChar(100)
  email                String    @default("") @db.VarChar(100)
  password             String    @default("") @db.Char(32)
  mobile               String    @default("") @db.VarChar(255)
  login_ip             String    @default("") @db.VarChar(256)
  login_time           Int       @default(0)
  create_time          Int       @default(0)
  last_modified_time   DateTime? @default(now()) @db.Timestamp(0)
  status               Boolean   @default(false)
  login_count          Int       @default(0)
  register_ip          String    @default("0") @db.VarChar(64)
  ip_area              String    @default("") @db.VarChar(255)
  certify              Boolean   @default(false)
  certify_time         Int       @default(0)
  channel              Boolean   @default(false)
  lead_status          Boolean   @default(false)
  ticket_lead_status   Boolean   @default(false)
  customer_status      Boolean   @default(false)
  customer_tips        Boolean   @default(false)
  user_statu           Int       @default(0)
  cooperate_id         Int       @default(0)
  certificate_lead     Boolean   @default(false)
  email_subscribe      Boolean   @default(false)
  is_vip               Int?      @default(2) @db.TinyInt
  remark               String    @default("") @db.VarChar(255)
  user_type            Int?      @default(0) @db.TinyInt
  sourceLink           String?   @default("") @db.VarChar(64)
  sourceKey            String    @default("") @db.VarChar(256)
  trajectoryId         String?   @default("") @db.VarChar(255)
  fromActive           String?   @db.VarChar(64)
  app_analysis_remark  String?   @db.Text
  register_method      Boolean   @default(false)
  country_code         String    @default("86") @db.VarChar(7)
  country              String    @default("中国") @db.VarChar(64)
  qq                   String?   @default("") @db.VarChar(255)
  weixin               String?   @default("") @db.VarChar(255)
  contact_method       String?   @default("") @db.VarChar(50)
  contact_name         String?   @default("") @db.VarChar(255)
  abbreviation         String?   @default("") @db.VarChar(30)
  oem_management_email String?   @default("") @db.VarChar(60)
  public_cloud         Boolean?  @default(false)
  read_private         Boolean?  @default(false)
  read_service_terms   Boolean?
  register_type        Int?      @default(0) @db.TinyInt
  purpose              String?   @default("") @db.VarChar(20)
  net_safe             Int       @default(0) @db.UnsignedTinyInt
  net_safe_time        Int       @default(0) @db.UnsignedInt
  scope                String    @default("console.rongcloud.net") @db.VarChar(31)
  official_site_url    String?   @db.Text
  official_site_param  String?   @db.Text

  @@index([email], map: "index_email")
  @@index([mobile], map: "index_mobile")
  @@index([name], map: "index_name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_account {
  id      Int     @id @default(autoincrement())
  user_id Int
  balance Decimal @default(0.00) @db.Decimal(11, 2)
  type    Int     @db.TinyInt
  arrears Decimal @default(0.00) @db.Decimal(11, 2)
  end     String? @db.VarChar(32)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_account_day {
  id      Int      @id @default(autoincrement())
  user_id Int?     @default(0)
  type    Int?     @default(0)
  account Int?     @default(0)
  real    Int?     @default(0)
  dir     Int?     @default(0)
  money   Decimal? @default(0.00) @db.Decimal(11, 2)
  today   Int?     @default(0)
  month   Int?     @default(0)
  rid     Int?     @default(0)
  server  Int?     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_account_month {
  id          Int      @id @default(autoincrement())
  user_id     Int?     @default(0)
  month       Int?     @default(0)
  m_r_balance Decimal? @default(0.00) @db.Decimal(11, 2)
  m_g_balance Decimal? @default(0.00) @db.Decimal(11, 2)
  t_r_balance Decimal? @default(0.00) @db.Decimal(11, 2)
  t_g_balance Decimal? @default(0.00) @db.Decimal(11, 2)
  r_real      Decimal? @default(0.00) @db.Decimal(11, 2)
  r_give      Decimal? @default(0.00) @db.Decimal(11, 2)
  r_order     Decimal? @default(0.00) @db.Decimal(11, 2)
  c_real      Decimal? @default(0.00) @db.Decimal(11, 2)
  c_give      Decimal? @default(0.00) @db.Decimal(11, 2)
  c_order     Decimal? @default(0.00) @db.Decimal(11, 2)
  a_r_consum  Decimal? @default(0.00) @db.Decimal(11, 2)
  a_g_consum  Decimal? @default(0.00) @db.Decimal(11, 2)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_account_record {
  id           Int      @id @default(autoincrement())
  order_id     String   @db.VarChar(30)
  money        Decimal  @db.Decimal(11, 2)
  user_id      Int
  admin_id     Int      @default(0)
  audit_id     Int      @default(0)
  type         Int
  account_type Int      @default(1)
  reason       String?  @db.Text
  file         String   @default("") @db.VarChar(255)
  service_type Int      @default(0)
  remark       String?  @db.Text
  status       Int      @default(2) @db.TinyInt
  ctime        Int      @default(0)
  audit_time   Int      @default(0)
  invoice      Boolean  @default(false)
  balance      Decimal? @default(0.00) @db.Decimal(11, 2)
  consum_type  Int?     @default(0)
  app_ids      String?  @default("") @db.VarChar(255)
  start        Int?     @default(0)
  end          Int?     @default(0)
  directional  String?  @default("") @db.VarChar(255)
  sid          Int?     @default(0)
  fill         Int?     @default(0)
  settle       Int?     @default(0)
  cdate        String?  @db.VarChar(32)
  app_id       Int      @default(0)
  source       Int      @default(0)
  invoice_back Boolean  @default(false)
  month        Int      @default(0)
  refund       Decimal? @default(0.00) @db.Decimal(11, 2)

  @@index([user_id], map: "ix_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_additional_info {
  id                     Int     @id @default(autoincrement())
  user_id                Int     @default(0) @db.UnsignedInt
  wechat_source          String  @default("") @db.VarChar(50)
  wechat_is_subscribe    Boolean @default(false)
  wechat_subscribe_at    Int     @default(0)
  wechat_unsubscribe_at  Int     @default(0)
  wechat_subscribe_scene String  @default("") @db.VarChar(255)
  wechat_nickname        String  @default("") @db.VarChar(255)
  wechat_sex             Boolean @default(false)
  wechat_province        String  @default("") @db.VarChar(255)
  wechat_city            String  @default("") @db.VarChar(255)
  wechat_country         String  @default("") @db.VarChar(255)
  wechat_avatar          String  @default("") @db.VarChar(255)
  wechat_union_id        String  @default("") @db.VarChar(255)
  wechat_remark          String  @default("") @db.VarChar(255)
  wechat_bind_at         Int
  study_list             String  @db.Text
  created_at             Int     @default(0) @db.UnsignedInt
  updated_at             Int     @default(0) @db.UnsignedInt
  deleted_at             Int?    @db.UnsignedInt
  wechat_open_id         String? @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_balance_log {
  id        Int     @id @default(autoincrement())
  day       Int
  user_id   Int     @default(0)
  balance   Float   @default(0.00) @db.Float
  rechange  Float?  @default(0.00) @db.Float
  day_close Int?
  service   String? @db.VarChar(512)
  runstatus Int?    @default(0) @db.SmallInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_contract {
  id                  Int      @id @default(autoincrement())
  company             String   @db.VarChar(255)
  name                String   @db.VarChar(30)
  user_id             Int
  phone               String   @db.VarChar(20)
  address             String   @db.VarChar(255)
  admin_id            Int
  status              Int      @db.TinyInt
  ctime               Int
  utime               Int
  express             String?  @default("") @db.VarChar(255)
  application_id      Int      @default(0)
  dcp_status          Int      @default(0) @db.TinyInt
  contract_no         String   @default("") @db.VarChar(20)
  contract_no_pre     String   @default("") @db.VarChar(20)
  contract_name       String   @default("") @db.VarChar(100)
  start_date          Int      @default(0)
  end_date            Int      @default(0)
  contract_duration   Int      @default(0)
  cooperation_matters String   @db.Text
  contract_attribute  Int      @default(0) @db.TinyInt
  contract_type       Int      @default(0) @db.TinyInt
  money               Decimal? @default(0.0000) @db.Decimal(12, 4)
  currency            Int      @default(0) @db.TinyInt
  invoice_detail      String   @default("") @db.VarChar(255)
  payment_type        String   @default("") @db.VarChar(50)
  remittance_account  String   @default("") @db.VarChar(255)
  indemnity_clause    String   @db.Text
  remark              String   @default("") @db.VarChar(255)
  file                String   @default("") @db.VarChar(255)
  contract_source     Boolean  @default(false)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_contract_boss {
  id          Int     @id @default(autoincrement())
  user_id     Int?
  contract_id String? @default("0") @db.VarChar(255)
  ctime       String? @db.VarChar(50)
  utime       String? @db.VarChar(50)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_data_show {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  app_id      Int     @default(0) @db.UnsignedInt
  menu_id     Int     @default(0) @db.UnsignedInt
  env         Int     @default(1) @db.UnsignedTinyInt
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  show_time   String  @default("") @db.VarChar(12)
  is_del      Boolean @default(false)

  @@index([app_id], map: "index_1")
  @@index([user_id], map: "index_2")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_developer_level {
  id          Int @id @default(autoincrement())
  user_id     Int @default(0)
  admin_id    Int @default(0)
  score       Int @default(0) @db.TinyInt
  create_time Int @default(0)
  ticket_id   Int @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_event_category {
  id           Int     @id @default(autoincrement())
  platform     String  @default("") @db.VarChar(64)
  platform_cn  String  @default("") @db.VarChar(64)
  name         String  @default("") @db.VarChar(64)
  name_cn      String  @default("") @db.VarChar(128)
  color        String? @default("") @db.VarChar(32)
  icon         String? @default("") @db.VarChar(32)
  operation    String? @db.Text
  fields       String? @db.Text
  url          String? @default("") @db.VarChar(512)
  remarks      String? @default("") @db.VarChar(512)
  confidential Int     @default(3)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_event_log {
  id             Int       @id @default(autoincrement())
  platform       String    @db.VarChar(64)
  category_name  String    @db.VarChar(64)
  operation      String?   @default("") @db.VarChar(255)
  raw_id         String    @db.VarChar(128)
  raw_param      String?   @default("") @db.VarChar(255)
  user_id        Int
  app_key        String?   @default("") @db.VarChar(32)
  start_time     DateTime  @default(now()) @db.Timestamp(0)
  end_time       DateTime? @db.Timestamp(0)
  fields         String?   @db.LongText
  remarks        String?   @db.LongText
  related_person String?   @db.VarChar(64)
  sync_source    String?   @db.VarChar(64)
  update_time    DateTime? @db.Timestamp(0)

  @@index([app_key], map: "ruel_app_key")
  @@index([end_time], map: "ruel_end_time")
  @@index([raw_id], map: "ruel_raw_id")
  @@index([start_time], map: "ruel_start_time")
  @@index([update_time], map: "ruel_update_time")
  @@index([user_id], map: "ruel_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_expand {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  type        Int     @default(0) @db.UnsignedTinyInt
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Int     @default(0) @db.UnsignedTinyInt
  value1      String? @default("") @db.VarChar(255)
  value2      String? @default("") @db.VarChar(255)
  value3      String? @default("") @db.VarChar(255)
  value4      String? @default("") @db.VarChar(255)
  value5      String? @default("") @db.VarChar(255)
  remark      String? @default("") @db.VarChar(255)
  config      String? @db.Text

  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_expand_20210104 {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  type        Int     @default(0) @db.UnsignedTinyInt
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Int     @default(0) @db.UnsignedTinyInt
  value1      String? @default("") @db.VarChar(255)
  value2      String? @default("") @db.VarChar(255)
  value3      String? @default("") @db.VarChar(255)
  value4      String? @default("") @db.VarChar(255)
  value5      String? @default("") @db.VarChar(255)
  remark      String? @default("") @db.VarChar(255)
  config      String? @db.Text

  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_faq {
  id                    Int    @id @default(autoincrement()) @db.UnsignedInt
  business_and_platform String @db.Text
  type                  Int    @db.UnsignedTinyInt
  title                 String @default("") @db.VarChar(255)
  content               String @db.Text
  url                   String @default("") @db.VarChar(255)
  author                Int    @db.UnsignedInt
  visit_count           Int    @db.UnsignedInt
  status                Int    @db.UnsignedTinyInt
  created_at            Int    @db.UnsignedInt
  updated_at            Int?   @db.UnsignedInt
  deleted_at            Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_first {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id         Int     @unique(map: "user_id") @default(0)
  home_page_date  Int     @default(0)
  login_date      Int     @default(0)
  dynamic_date    Int     @default(0)
  user_add_date   Int     @default(0)
  message_date    Int     @default(0)
  im_open_date    Int     @default(0)
  rtc_open_date   Int     @default(0)
  rtc_value_date  Int     @default(0)
  create_time     Int     @default(0)
  update_time     Int     @default(0)
  remark          String? @default("") @db.VarChar(100)
  create_date     Int     @default(0)
  app_create_date Int     @default(0)
  online_date     Int     @default(0)
  recharge_date   Int     @default(0)

  @@index([app_create_date], map: "app_create_date")
  @@index([create_date], map: "create_date")
  @@index([dynamic_date], map: "dynamic_date")
  @@index([home_page_date], map: "home_page_date")
  @@index([im_open_date], map: "im_open_date")
  @@index([login_date], map: "login_date")
  @@index([message_date], map: "message_date")
  @@index([online_date], map: "online_date")
  @@index([recharge_date], map: "recharge_date")
  @@index([rtc_open_date], map: "rtc_open_date")
  @@index([rtc_value_date], map: "rtc_value_date")
  @@index([user_add_date], map: "user_add_date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_follow_record {
  id       Int     @id @default(autoincrement())
  type     Boolean
  t_id     Int
  admin_id Int
  user_id  Int
  title    String  @db.VarChar(255)
  content  String  @db.Text
  ctime    Int
  item_id  String  @default("") @db.VarChar(25)
  money    Float   @default(0.00)
  status   Int     @default(1) @db.UnsignedTinyInt
  response String  @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_follow_record_copy {
  id       Int     @id @default(autoincrement())
  type     Boolean
  t_id     Int
  admin_id Int
  user_id  Int
  title    String  @db.VarChar(255)
  content  String  @db.VarChar(255)
  ctime    Int
  item_id  String  @default("") @db.VarChar(25)
  money    Float   @default(0.00)
  status   Int     @default(1) @db.UnsignedTinyInt
  response String  @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  name        String @default("") @db.VarChar(256)
  create_type Int    @default(0) @db.TinyInt
  member_num  Int    @default(0)
  user_attr   String @default("") @db.VarChar(512)
  condition_y String @default("") @db.VarChar(512)
  condition_n String @default("") @db.VarChar(512)
  state       Int    @default(0) @db.TinyInt
  is_del      Int    @default(0) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group_config {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  type         Int    @default(0) @db.TinyInt
  col_type     Int    @default(0) @db.TinyInt
  code         String @unique(map: "code") @default("") @db.VarChar(64)
  name         String @default("") @db.VarChar(256)
  compare_type String @default("") @db.VarChar(256)
  compare_val  String @default("") @db.VarChar(1024)
  is_radio     Int    @default(0) @db.TinyInt
  sort         Int    @default(0) @db.TinyInt
  is_del       Int    @default(0) @db.TinyInt
  create_by    Int    @default(0)
  update_by    Int    @default(0)
  create_time  Int    @default(0)
  update_time  Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group_detail {
  id                   Int    @id @default(autoincrement()) @db.UnsignedInt
  user_group_id        Int    @default(0)
  user_group_config_id Int    @default(0)
  compare_type         String @default("") @db.VarChar(256)
  compare_val          String @default("") @db.VarChar(256)
  start_date           Int    @default(0)
  end_date             Int    @default(0)
  is_del               Int    @default(0) @db.TinyInt
  create_by            Int    @default(0)
  update_by            Int    @default(0)
  create_time          Int    @default(0)
  update_time          Int    @default(0)

  @@index([user_group_id], map: "idx_user_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group_item {
  id            Int @id @default(autoincrement()) @db.UnsignedInt
  user_group_id Int @default(0)
  user_id       Int @default(0)
  is_del        Int @default(0) @db.TinyInt
  create_by     Int @default(0)
  update_by     Int @default(0)
  create_time   Int @default(0)
  update_time   Int @default(0)

  @@index([user_group_id], map: "idx_user_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group_notice {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  user_group_id Int    @default(0)
  type          Int    @default(0) @db.TinyInt
  sms_type      Int    @default(0)
  title         String @default("") @db.VarChar(256)
  email_title   String @default("") @db.VarChar(256)
  content       String @default("") @db.VarChar(256)
  send_time     Int    @default(0)
  state         Int    @default(0) @db.TinyInt
  is_del        Int    @default(0) @db.TinyInt
  create_by     Int    @default(0)
  update_by     Int    @default(0)
  create_time   Int    @default(0)
  update_time   Int    @default(0)

  @@index([user_group_id], map: "idx_user_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group_notice_c {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  user_group_id Int    @default(0)
  type          Int    @default(0) @db.TinyInt
  title         String @default("") @db.VarChar(256)
  email_title   String @default("") @db.VarChar(256)
  content       String @db.Text
  send_time     Int    @default(0)
  state         Int    @default(0) @db.TinyInt
  is_del        Int    @default(0) @db.TinyInt
  create_by     Int    @default(0)
  update_by     Int    @default(0)
  create_time   Int    @default(0)
  update_time   Int    @default(0)

  @@index([user_group_id], map: "idx_user_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_group_send_record {
  id                   Int @id @default(autoincrement()) @db.UnsignedInt
  user_group_id        Int @default(0)
  user_group_notice_id Int @default(0)
  email_queue_id       Int @default(0)
  sms_log_id           Int @default(0)
  is_del               Int @default(0) @db.TinyInt
  create_time          Int @default(0)

  @@index([email_queue_id], map: "idx_email_queue_id")
  @@index([user_group_id], map: "idx_user_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_h5 {
  id          Int     @id @default(autoincrement())
  mobile      String? @db.VarChar(11)
  name        String? @db.VarChar(20)
  ctime       String? @db.VarChar(50)
  register_ip String? @db.VarChar(256)
  sourceLink  Bytes?  @db.VarBinary(64)
  sourceWord  Bytes?  @db.VarBinary(64)
  trajectory  String? @db.Text
  user_id     Int     @default(0)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_invoice {
  id           Int     @id @default(autoincrement())
  user_id      Int
  title        String  @db.VarChar(255)
  type         Int     @default(1) @db.TinyInt
  money        Decimal @db.Decimal(11, 2)
  name         String  @db.VarChar(30)
  phone        String  @db.VarChar(20)
  address      String  @db.VarChar(255)
  admin_id     Int     @default(0)
  status       Int     @default(0) @db.TinyInt
  ctime        Int     @default(0)
  utime        Int     @default(0)
  records      String  @default("0") @db.VarChar(255)
  attr         String  @default("") @db.VarChar(255)
  bank_num     String? @default("") @db.VarChar(255)
  express      String? @default("") @db.VarChar(255)
  sign_address String? @default("") @db.VarChar(255)
  bank         String? @default("") @db.VarChar(255)
  remark       String? @default("") @db.VarChar(255)
  taxpayer     String? @default("") @db.VarChar(255)
  sign_phone   String? @default("") @db.VarChar(255)
  last_title   String? @default("") @db.VarChar(255)
  read_time    Int     @default(0) @db.UnsignedInt
  open         String? @default("0") @db.VarChar(255)
  backtime     Int     @default(0)
  backfile     String? @default("") @db.VarChar(255)
  email        String  @default("") @db.VarChar(128)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_line_order {
  id         Int      @id @default(autoincrement())
  account    Int?     @default(43)
  user_id    Int      @default(0)
  app_id     Int      @default(0)
  money      Decimal  @default(0.00) @db.Decimal(11, 2)
  start      Int      @default(0)
  end        Int      @default(0)
  line_num   String   @default("0") @db.VarChar(50)
  server     Int
  admin_id   Int
  audit_id   Int
  reason     String   @db.VarChar(255)
  remark     String   @db.VarChar(255)
  file       String   @db.VarChar(255)
  status     Int      @default(1) @db.TinyInt
  ctime      Int
  utime      Int
  auto_close Int?     @default(0)
  invoice    Int?     @default(0)
  balnace    Decimal? @default(0.00) @db.Decimal(11, 2)
  refund     Decimal? @default(0.00) @db.Decimal(11, 2)
  cdate      String?  @db.VarChar(32)
  type       Int?     @default(0) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_login_log {
  id             Int     @id @default(autoincrement())
  username       String  @db.VarChar(64)
  ip             String  @db.VarChar(32)
  create_time    Int
  error          String  @default("") @db.VarChar(128)
  login_date     Int     @default(0)
  change_user_id Int     @default(0)
  sourceLink     String? @db.VarChar(255)
  sourceKey      String? @db.VarChar(255)
  user_id        Int     @default(0)

  @@index([login_date], map: "login_date")
  @@index([user_id], map: "user_id")
  @@index([username], map: "username")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_manage {
  id              Int     @id @default(autoincrement())
  user_id         Int
  app_id          Int
  manage_user_id  Int     @default(0)
  manage_email    String? @default("") @db.VarChar(50)
  permission_info String? @db.Text
  status          Boolean @default(false)
  create_time     Int
  update_time     Int

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_manage_invite {
  id             Int     @id @default(autoincrement())
  user_id        Int     @default(0)
  manage_user_id Int     @default(0)
  status         Int?    @default(10) @db.TinyInt
  is_del         Int     @default(0) @db.TinyInt
  create_time    String? @default("") @db.VarChar(12)
  update_time    String? @default("") @db.VarChar(12)
  invite_time    String? @default("") @db.VarChar(12)
  agree_time     String? @default("") @db.VarChar(12)
  role_id        String? @db.Text
  company_name   String? @default("") @db.VarChar(50)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_manage_role {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  name        String? @default("") @db.VarChar(20)
  description String? @default("") @db.VarChar(50)
  menu        String? @db.Text
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Int     @default(0) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_mc_order {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  order_type  Int      @default(1)
  app_id      Int?
  environment Int      @db.TinyInt
  appKey      String   @db.VarChar(45)
  user_id     Int?
  mcId        String?  @db.VarChar(255)
  mc_id       Int?
  cate        Int?
  type        Int?
  money       Decimal? @db.Decimal(11, 2)
  status      Int?
  len         Int?
  num         Int      @db.UnsignedInt
  start       Int?
  end         Int?
  desp        String   @db.VarChar(128)
  type_num_1  Int      @db.UnsignedTinyInt
  type_num_2  Int      @db.UnsignedTinyInt
  type_num_3  Int      @db.UnsignedTinyInt
  type_num_4  Int      @db.UnsignedTinyInt
  ctime       Int?
  close_time  Int?
  order_id    Int?     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_mc_order_line {
  id               Int      @id @default(autoincrement()) @db.UnsignedInt
  admin_id         Int?
  audit_id         Int?     @default(0)
  attr             String?  @db.VarChar(255)
  reason           String?  @db.VarChar(255)
  app_id           Int?     @default(0)
  environment      Int?     @default(0) @db.TinyInt
  out_email        String?  @db.VarChar(255)
  user_id          Int?
  mcId             String?  @default("") @db.VarChar(255)
  mc_id            Int?     @default(0)
  cate             Int?
  type             Int?
  money            Decimal? @db.Decimal(11, 2)
  real_mon         Decimal? @db.Decimal(11, 2)
  order_type       Int?
  remind_status    Int?
  status           Int?
  len              Int?
  num              Int      @db.UnsignedInt
  account          Int?
  start            Int?
  end              Int?
  remind           Int?
  ctime            Int?
  utime            Int?
  remark           String?  @db.VarChar(255)
  invoice          Int?     @default(0)
  balance          Decimal? @default(0.00) @db.Decimal(11, 2)
  cdate            String?  @db.VarChar(32)
  consumption_type Int?     @default(0) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_name_log {
  id       Int     @id @default(autoincrement())
  user_id  Int?
  type     Int     @default(1) @db.TinyInt
  name_new String? @db.VarChar(60)
  name_log String? @db.VarChar(60)
  ctime    Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_public_relation {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id      Int    @default(0) @db.UnsignedInt
  developer_id Int    @default(0) @db.UnsignedInt
  status       Int    @default(1) @db.UnsignedTinyInt
  create_time  String @default("") @db.VarChar(12)
  update_time  String @default("") @db.VarChar(12)
  unbind_time  String @default("") @db.VarChar(12)
  is_del       Int    @default(0) @db.UnsignedTinyInt
  admin_id     String @default("") @db.VarChar(12)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service {
  id      Int     @id @default(autoincrement())
  user_id Int
  app_id  Int
  server  Int
  ctime   Int
  cltime  Int?
  status  Boolean @default(true)
  env     Int     @default(2) @db.TinyInt
  source  Boolean @default(false)
  config  String  @default("") @db.VarChar(2000)

  @@index([app_id], map: "app_id")
  @@index([user_id, app_id, status], map: "indexs")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_copy1 {
  id      Int     @id @default(autoincrement())
  user_id Int
  app_id  Int
  server  Int
  ctime   Int
  cltime  Int?
  status  Boolean @default(true)
  env     Int     @default(2) @db.TinyInt
  source  Boolean @default(false)
  config  String  @default("") @db.VarChar(2000)

  @@index([app_id], map: "app_id")
  @@index([user_id, app_id, status], map: "indexs")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_first {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int     @default(0) @db.UnsignedInt
  app_id        Int     @default(0) @db.UnsignedInt
  server_id     Int     @default(0)
  service       Int     @default(0)
  open_date     Int     @default(0)
  order_date    Int     @default(0)
  charging_date Int     @default(0)
  create_date   Int     @default(0)
  update_time   Int     @default(0)
  create_time   Int     @default(0)
  remark        String? @default("") @db.VarChar(100)

  @@index([app_id], map: "app_id")
  @@index([charging_date], map: "charging_date")
  @@index([create_date], map: "create_date")
  @@index([open_date], map: "open_date")
  @@index([order_date], map: "order_date")
  @@index([server_id], map: "server_id")
  @@index([service], map: "service")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_first_copy {
  id            Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int     @default(0) @db.UnsignedInt
  app_id        Int     @default(0) @db.UnsignedInt
  server_id     Int     @default(0)
  service       Int     @default(0)
  open_date     Int     @default(0)
  order_date    Int     @default(0)
  charging_date Int     @default(0)
  create_date   Int     @default(0)
  update_time   Int     @default(0)
  create_time   Int     @default(0)
  remark        String? @default("") @db.VarChar(100)

  @@index([app_id], map: "app_id")
  @@index([charging_date], map: "charging_date")
  @@index([create_date], map: "create_date")
  @@index([open_date], map: "open_date")
  @@index([order_date], map: "order_date")
  @@index([server_id], map: "server_id")
  @@index([service], map: "service")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_free {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0)
  admin_id    Int
  app_id      Int     @default(0)
  service     Int     @default(0)
  type        Int     @default(0)
  config      String? @db.Text
  remark      String  @default("") @db.VarChar(500)
  reason      String  @db.VarChar(500)
  file_path   String  @default("") @db.VarChar(255)
  status      Boolean @default(false)
  create_time Int     @default(0)
  update_time Int

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_free_copy {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0)
  admin_id    Int
  app_id      Int     @default(0)
  service     Int     @default(0)
  type        Int     @default(0)
  config      String  @default("") @db.VarChar(2000)
  remark      String  @default("") @db.VarChar(500)
  reason      String  @db.VarChar(500)
  file_path   String  @default("") @db.VarChar(255)
  status      Boolean @default(false)
  create_time Int     @default(0)
  update_time Int

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_give_activity {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int      @default(0)
  app_id      Int      @default(0)
  service_id  Int      @default(0)
  config      Json
  admin_id    Int      @default(0)
  start_date  Int      @default(0)
  end_date    Int      @default(0)
  create_time DateTime @default(now()) @db.DateTime(0)
  update_time DateTime @default(now()) @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  admin_id    Int     @default(0)
  create_time Int     @default(0)
  ip          String  @default("") @db.Char(15)
  action      Int     @default(0)
  url         String  @default("") @db.VarChar(255)
  content     String  @default("") @db.VarChar(2000)
  app_id      Int     @default(0)
  env         Boolean @default(false)
  server      Int     @default(0)
  mark        String  @default("") @db.VarChar(255)
  new_data    String? @db.LongText
  old_data    String? @db.LongText

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_operation_log {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int      @db.UnsignedInt
  app_id     Int      @db.UnsignedInt
  env        Int      @db.UnsignedTinyInt
  service_id Int      @db.UnsignedInt
  admin_id   Int      @default(0) @db.UnsignedInt
  operation  Int      @db.UnsignedTinyInt
  ip         String   @db.VarChar(255)
  config     String   @db.Text
  loop       Boolean  @default(false)
  status     Int      @default(1) @db.UnsignedTinyInt
  source     Boolean?
  queue      String   @default("") @db.VarChar(50)
  created_at Int      @db.UnsignedInt
  updated_at Int      @db.UnsignedInt
  deleted_at Int?

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_order {
  id           Int     @id @default(autoincrement())
  order_id     String  @default("0") @db.VarChar(30)
  user_id      Int
  app_id       Int
  app_key      String  @db.VarChar(255)
  server       Int
  start        Int     @default(0)
  end          Int     @default(0)
  create_time  Int     @default(0)
  close_admin  Int     @default(0)
  close_reason String  @default("") @db.VarChar(255)
  close_time   Int     @default(0)
  is_first     Int     @default(0) @db.TinyInt
  status       Boolean @default(true)
  auto_close   Int?    @default(0)
  type         Int?    @default(0) @db.TinyInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_record {
  id         Int     @id @default(autoincrement())
  uid        Int
  app_id     Int
  env        Boolean
  admin_id   Int     @default(0)
  rc_user_id BigInt? @default(0)
  server     Int
  action     Int     @db.TinyInt
  content    String? @default("") @db.VarChar(10000)
  ctime      Int
  ip         String  @default("") @db.VarChar(16)
  new_data   String  @db.Text
  old_data   String  @db.Text

  @@index([app_id], map: "app_id")
  @@index([uid], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_status_check {
  id              Int       @id @default(autoincrement())
  appId           Int?
  config_items    String?   @db.Text
  frequency_items String?   @db.Text
  config_count    Int?
  frequency_count Int?
  date            Int?
  create_time     DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_service_white {
  id       Int     @id @default(autoincrement())
  white_id String? @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_settlement {
  id         Int     @id @default(autoincrement())
  user_id    Int
  app_id     Int
  order_id   String  @db.VarChar(20)
  money      Decimal @db.Decimal(11, 2)
  server     Int
  ctime      Int
  month      Int
  status     Int     @db.TinyInt
  expect     Decimal @default(0.00) @db.Decimal(11, 2)
  useday     Int?    @default(0)
  daylive    Int?    @default(0)
  limitid    Int?    @default(0)
  order_type Int?    @default(1)
}

model rcloud_user_tips {
  id      Int @id @default(autoincrement())
  user_id Int
  type    Int @db.TinyInt
  ctime   Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_trade {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int    @default(0) @db.UnsignedInt
  trade      Int    @default(0) @db.UnsignedInt
  scene      String @default("") @db.VarChar(500)
  product    String @default("") @db.VarChar(500)
  remark     String @default("") @db.VarChar(255)
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_user_trade_test {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int    @default(0) @db.UnsignedInt
  trade      Int    @default(0) @db.UnsignedInt
  scene      String @default("") @db.VarChar(500)
  product    String @default("") @db.VarChar(500)
  remark     String @default("") @db.VarChar(255)
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_usertomail {
  id              Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id         Int     @db.UnsignedInt
  name            String  @default("") @db.VarChar(60)
  mobile          String  @db.Char(11)
  email           String  @default("") @db.VarChar(100)
  type            Int     @default(1)
  phoneaddress    String  @db.VarChar(100)
  email_subscribe Boolean @default(false)

  @@index([phoneaddress], map: "index_phoneaddress")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_video {
  id                Int    @id @default(autoincrement()) @db.UnsignedInt
  video_category_id Int    @default(0)
  title             String @default("") @db.VarChar(64)
  url               String @default("") @db.VarChar(256)
  logo              String @default("") @db.VarChar(256)
  content_desc_logo String @default("") @db.VarChar(256)
  content_desc      String @default("") @db.VarChar(256)
  start_time        Int    @default(0)
  end_time          Int    @default(0)
  is_del            Int    @default(2) @db.TinyInt
  create_by         Int    @default(0)
  update_by         Int    @default(0)
  create_time       Int    @default(0)
  update_time       Int    @default(0)

  @@index([video_category_id], map: "idx_video_category_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_video_category {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  title       String @default("") @db.VarChar(64)
  is_del      Int    @default(2) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_video_detail {
  id         Int    @id @default(autoincrement())
  voip_id    Int    @default(0)
  listener   String @default("0") @db.VarChar(16)
  start_time Int    @default(0)
  seconds    Int    @default(0)
  res_name   String @default("0") @db.VarChar(4)
  channel_id String @default("0") @db.VarChar(16)
  resolution String @default("0") @db.VarChar(16)

  @@index([start_time], map: "start_time")
  @@index([voip_id, start_time], map: "voip_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_video_reserve {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  video_id    Int    @default(0)
  user_id     Int    @default(0)
  channel     String @default("") @db.VarChar(32)
  is_del      Int    @default(2) @db.TinyInt
  create_by   Int    @default(0)
  update_by   Int    @default(0)
  create_time Int    @default(0)
  update_time Int    @default(0)

  @@index([user_id], map: "idx_user_id")
  @@index([video_id], map: "idx_video_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_vip_support {
  id                 Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id            Int     @default(0) @db.UnsignedInt
  app_id             Int     @default(0) @db.UnsignedInt
  mobile             String  @default("") @db.VarChar(20)
  qq                 String  @default("0") @db.VarChar(64)
  group_id           String  @default("") @db.VarChar(20)
  group_name         String  @default("") @db.VarChar(30)
  group_type         Int     @default(1) @db.UnsignedTinyInt
  group_status       Int     @default(1) @db.UnsignedTinyInt
  group_create_time  String  @default("") @db.VarChar(15)
  group_dismiss_time String  @default("") @db.VarChar(15)
  is_del             Int     @default(0) @db.UnsignedTinyInt
  create_time        String  @default("") @db.VarChar(15)
  update_time        String  @default("") @db.VarChar(15)
  remark             String? @default("") @db.VarChar(200)
  config             String? @db.Text
  sale_id            Int?    @default(0)

  @@index([app_id], map: "app_id")
  @@index([group_dismiss_time], map: "dismiss_time")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_vip_support_log {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  log_content String  @default("") @db.VarChar(255)
  vid         Int     @default(0)
  admin_id    Int     @default(0)
  create_time String  @default("") @db.VarChar(12)
  update_time String  @default("") @db.VarChar(12)
  is_del      Boolean @default(false)

  @@index([vid], map: "vid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_website_content {
  id                   Int     @id @default(autoincrement())
  url                  String  @default("") @db.VarChar(255)
  content              String  @db.LongText
  status               Boolean @default(false)
  title                String  @db.VarChar(1000)
  keyword              String  @default("") @db.VarChar(1000)
  describe             String  @default("") @db.VarChar(2000)
  client               Boolean @default(false)
  create_time          Int     @default(0)
  update_time          Int
  admin_id             Int     @default(0)
  type                 Int     @default(0) @db.TinyInt
  relation_website_ids String  @default("") @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_website_log {
  id          Int     @id @default(autoincrement())
  website_id  Int     @default(0)
  url         String  @default("") @db.VarChar(255)
  content     String  @db.LongText
  title       String  @db.VarChar(1000)
  keyword     String  @default("") @db.VarChar(1000)
  describe    String  @default("") @db.VarChar(2000)
  client      Boolean @default(false)
  create_time Int     @default(0)
  admin_id    Int     @default(0)
  type        Int     @default(0) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wechat_event {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  tousername   String @default("") @db.VarChar(255)
  fromusername String @default("") @db.VarChar(255)
  msgtype      String @default("") @db.VarChar(50)
  event        String @default("") @db.VarChar(100)
  eventkey     String @default("") @db.VarChar(255)
  msgid        String @default("") @db.VarChar(50)
  status       String @default("") @db.VarChar(50)
  createtime   Int    @default(0)
  ticket       String @default("") @db.VarChar(500)
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int?

  @@index([msgid], map: "msg_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wechat_keyword {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  keyword    String @default("") @db.VarChar(25)
  match      Int    @default(0) @db.UnsignedTinyInt
  action     String @default("") @db.VarChar(1000)
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wechat_message {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  tousername   String @default("") @db.VarChar(255)
  fromusername String @default("") @db.VarChar(255)
  createtime   String @default("") @db.VarChar(20)
  msgtype      String @default("") @db.VarChar(50)
  content      String @default("") @db.VarChar(100)
  picurl       String @default("") @db.VarChar(100)
  mediaid      String @default("") @db.VarChar(100)
  format       String @default("") @db.VarChar(50)
  recognition  String @default("") @db.VarChar(100)
  thumbmediaid String @default("") @db.VarChar(100)
  location_x   String @default("") @db.VarChar(20)
  location_y   String @default("") @db.VarChar(20)
  scale        String @default("") @db.VarChar(20)
  label        String @default("") @db.VarChar(20)
  title        String @default("") @db.VarChar(50)
  description  String @default("") @db.VarChar(255)
  url          String @default("") @db.VarChar(255)
  msgid        String @default("") @db.VarChar(50)
  created_at   Int    @db.UnsignedInt
  updated_at   Int    @db.UnsignedInt
  deleted_at   Int?

  @@index([mediaid], map: "msg_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wechat_subscribe_log {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  open_id    String  @db.VarChar(255)
  operate    Boolean
  created_at Int
  updated_at Int
  deleted_at Int?
}

model rcloud_wework_customer {
  id               Int    @id @default(autoincrement()) @db.UnsignedInt
  external_userid  String @default("") @db.VarChar(255)
  name             String @default("") @db.VarChar(255)
  avatar           String @default("") @db.VarChar(255)
  type             Int    @default(0) @db.UnsignedTinyInt
  gender           Int    @default(0) @db.UnsignedTinyInt
  unionid          String @default("") @db.VarChar(255)
  position         String @default("") @db.VarChar(255)
  corp_name        String @default("") @db.VarChar(255)
  corp_full_name   String @default("") @db.VarChar(255)
  external_profile String @db.Text
  follow_user      String @db.Text
  is_deleted       Int    @default(0) @db.UnsignedTinyInt
  created_at       Int    @default(0) @db.UnsignedInt
  updated_at       Int    @default(0) @db.UnsignedInt
  deleted_at       Int?
}

model rcloud_wework_department {
  id            Int    @id @default(autoincrement()) @db.UnsignedInt
  department_id Int    @default(0) @db.UnsignedInt
  name          String @default("") @db.Char(32)
  name_en       String @default("") @db.Char(32)
  parentid      Int    @default(0) @db.UnsignedInt
  order         Int    @default(0) @db.UnsignedInt
  is_deleted    Int    @default(0) @db.UnsignedTinyInt
  created_at    Int    @default(0) @db.UnsignedInt
  updated_at    Int    @default(0) @db.UnsignedInt
  deleted_at    Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wework_events {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  content    String @db.Text
  status     Int    @default(0) @db.UnsignedTinyInt
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int?
}

model rcloud_wework_user {
  id                Int    @id @default(autoincrement()) @db.UnsignedInt
  userid            String @default("") @db.Char(64)
  name              String @default("") @db.VarChar(255)
  mobile            String @default("") @db.Char(11)
  department        String @default("") @db.VarChar(255)
  order             String @default("") @db.VarChar(255)
  position          String @default("") @db.VarChar(255)
  gender            Int    @default(0) @db.UnsignedTinyInt
  email             String @default("") @db.VarChar(255)
  is_leader_in_dept String @default("") @db.VarChar(255)
  avatar            String @default("") @db.VarChar(255)
  thumb_avatar      String @default("") @db.VarChar(255)
  telephone         String @default("") @db.VarChar(255)
  alias             String @default("") @db.VarChar(255)
  extattr           String @db.Text
  status            Int    @default(0) @db.UnsignedTinyInt
  qr_code           String @default("") @db.VarChar(255)
  external_profile  String @db.Text
  external_position String @default("") @db.VarChar(255)
  address           String @default("") @db.VarChar(255)
  open_userid       String @default("") @db.VarChar(255)
  main_department   Int    @default(0) @db.UnsignedInt
  isleader          Int    @default(0) @db.UnsignedTinyInt
  enable            Int    @default(0) @db.UnsignedTinyInt
  hide_mobile       Int    @default(0) @db.UnsignedTinyInt
  is_deleted        Int    @default(0) @db.UnsignedTinyInt
  created_at        Int    @default(0) @db.UnsignedInt
  updated_at        Int    @default(0) @db.UnsignedInt
  deleted_at        Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_white_user {
  id            Int     @id @default(autoincrement())
  cate          Int
  user_id       Int     @default(0)
  app_id        Int?    @default(0)
  ctime         Int
  admin_id      Int
  status        Int     @default(0)
  keyword_count Int?    @default(0)
  remark        String? @default("") @db.VarChar(255)
  remark_file   String? @default("") @db.VarChar(255)
  end_date      Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_whiteboard_statistic {
  id           Int      @id @default(autoincrement())
  user_id      Int      @default(0)
  app_id       Int      @default(0)
  server_id    Int
  env          Boolean  @default(false)
  is_abroad    Int      @default(0) @db.UnsignedTinyInt
  service_type Int      @default(0)
  value        Int      @default(0)
  pay_value    Int      @default(0)
  money        Decimal  @default(0.0000) @db.Decimal(10, 4)
  date         Int      @default(0)
  create_time  DateTime @default(now()) @db.DateTime(0)
  update_time  DateTime @default(now()) @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([env], map: "env")
  @@index([user_id], map: "user_id")
}

model rcloud_work_log {
  id          Int    @id @default(autoincrement())
  user_id     Int    @default(0)
  create_time Int    @default(0)
  time        Int    @default(0)
  location    String @default("") @db.VarChar(128)
  contact     String @default("") @db.VarChar(64)
  worker      String @default("") @db.VarChar(32)
  infomation  String @default("") @db.VarChar(1024)
  follow      String @default("") @db.VarChar(1024)
  type        Int    @default(0) @db.TinyInt
  admin_user  Int    @default(0)
  email       String @default("") @db.VarChar(128)
  basics      String @default("") @db.VarChar(1000)
  contact_way String @default("") @db.VarChar(128)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wx_template_message {
  id                  Int    @id @default(autoincrement()) @db.UnsignedInt
  wx_template_id      String @default("") @db.VarChar(64)
  wx_template_content String @default("") @db.VarChar(512)
  wx_template_title   String @default("") @db.VarChar(32)
  url                 String @default("") @db.VarChar(256)
  user_tag            String @default("") @db.VarChar(256)
  send_num            Int    @default(0)
  title               String @default("") @db.VarChar(32)
  template_params     String @default("") @db.VarChar(256)
  send_time           Int    @default(0)
  status              Int    @default(0) @db.TinyInt
  is_del              Int    @default(2) @db.TinyInt
  create_by           Int    @default(0)
  update_by           Int    @default(0)
  create_time         Int    @default(0)
  update_time         Int    @default(0)

  @@index([create_by], map: "idx_create_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_wx_template_message_item {
  id                     Int    @id @default(autoincrement()) @db.UnsignedInt
  wx_template_message_id Int    @default(0)
  wx_msg_id              String @default("") @db.VarChar(32)
  user_id                Int    @default(0)
  open_id                String @default("") @db.VarChar(128)
  status                 Int    @default(0) @db.TinyInt
  send_fail_reason       String @default("") @db.VarChar(256)
  create_time            Int    @default(0)
  update_time            Int    @default(0)

  @@index([user_id], map: "idx_user_id")
  @@index([wx_template_message_id], map: "idx_wx_template_message_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yitu_account {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int    @default(0) @db.UnsignedInt
  app_key    String @default("") @db.VarChar(50)
  name       String @default("") @db.VarChar(100)
  dev_id     Int    @default(0)
  dev_key    String @default("") @db.VarChar(255)
  app_id     String @default("") @db.VarChar(50)
  app_name   String @default("") @db.VarChar(50)
  category   String @default("") @db.VarChar(50)
  moderation String @default("") @db.VarChar(50)
  created_at Int    @default(0) @db.UnsignedInt
  updated_at Int    @default(0) @db.UnsignedInt
  deleted_at Int    @default(0)

  @@index([user_id], map: "rcloud_yitu_account_user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yitu_statistic {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int     @default(0) @db.UnsignedInt
  app_id     Int     @default(0)
  env        Int     @default(0) @db.TinyInt
  moderation String  @default("") @db.VarChar(50)
  amount     Int     @default(0)
  money      Decimal @default(0.0000) @db.Decimal(12, 4)
  date       Int     @default(0)
  is_abroad  Boolean @default(false)
  created_at Int     @default(0) @db.UnsignedInt
  updated_at Int     @default(0) @db.UnsignedInt
  deleted_at Int     @default(0)

  @@index([app_id], map: "rcloud_yitu_statistic_app_id_index")
  @@index([user_id], map: "rcloud_yitu_statistic_user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yongyou_job {
  id             Int      @id @default(autoincrement())
  user_id        Int      @default(0)
  type           Int      @default(0) @db.TinyInt
  foreign_id     Int      @default(0)
  foreign_status Boolean  @default(false)
  status         Boolean  @default(false)
  version        Boolean  @default(false)
  trade_id       String   @default("") @db.VarChar(50)
  request        String   @default("") @db.VarChar(10000)
  response       String   @default("") @db.VarChar(5000)
  description    String   @default("") @db.VarChar(1000)
  create_time    DateTime @default(now()) @db.Timestamp(0)
  update_time    DateTime @default(now()) @db.Timestamp(0)

  @@index([foreign_id], map: "foreign_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yun_clean {
  id           Int     @id @default(autoincrement())
  user_id      Int     @default(0)
  app_id       Int     @default(0)
  user_name    String  @default("") @db.VarChar(128)
  password     String  @default("") @db.VarChar(64)
  appIds       String  @default("") @db.VarChar(1000)
  create_time  Int     @default(0)
  status       Boolean @default(false)
  api_close    Boolean @default(false)
  policy       String  @default("") @db.VarChar(256)
  conversation String  @default("") @db.VarChar(256)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yun_clean_statistics {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id          Int     @default(0) @db.UnsignedInt
  app_id           Int     @default(0) @db.UnsignedInt
  env              Int     @default(1) @db.UnsignedTinyInt
  server_id        Int     @default(0) @db.UnsignedInt
  date             Int     @default(0) @db.UnsignedInt
  text_total       Int     @default(0) @db.UnsignedInt
  text_pass        Int     @default(0) @db.UnsignedInt
  text_del         Int
  text_billing     Int     @default(0) @db.UnsignedInt
  text_money       Decimal @default(0.000) @db.Decimal(11, 3)
  image_total      Int     @default(0) @db.UnsignedInt
  image_pass       Int     @default(0) @db.UnsignedInt
  image_del        Int     @default(0) @db.UnsignedInt
  image_billing    Int     @default(0) @db.UnsignedInt
  image_money      Decimal @default(0.00) @db.Decimal(11, 2)
  voice_total      Int     @default(0) @db.UnsignedInt
  voice_pass       Int     @default(0) @db.UnsignedInt
  voice_del        Int     @default(0)
  voice_billing    Int     @default(0)
  voice_money      Decimal @default(0.000) @db.Decimal(11, 3)
  hq_voice_total   Int     @default(0)
  hq_voice_pass    Int     @default(0)
  hq_voice_del     Int     @default(0)
  hq_voice_billing Int     @default(0)
  hq_voice_money   Decimal @default(0.000) @db.Decimal(11, 3)
  created_at       Int     @default(0) @db.UnsignedInt
  updated_at       Int     @db.UnsignedInt
  deleted_at       Int?

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([server_id], map: "server_id")
  @@index([user_id, app_id, date], map: "user_app_idx")
  @@index([user_id], map: "user_id")
}

model rcloud_yun_clean_statistics_export {
  id       Int    @id @default(autoincrement()) @db.UnsignedInt
  uid      Int    @default(0) @db.UnsignedInt
  username String @default("") @db.VarChar(255)
  app_name String @default("") @db.VarChar(255)
  env      String @default("") @db.VarChar(255)
  app_key  String @default("") @db.VarChar(255)
  status   String @default("") @db.VarChar(255)
  type     String @default("") @db.VarChar(255)
  text     Int    @default(0) @db.UnsignedInt
  image    Int    @default(0) @db.UnsignedInt
  voice    Int    @default(0) @db.UnsignedInt
  hq_voice Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yun_data {
  id           Int     @id @default(autoincrement())
  app_key      String  @default("0") @db.VarChar(20)
  text_del     Int     @default(0)
  text_pass    Int     @default(0)
  ctime        String? @db.VarChar(11)
  refine       Int     @default(0)
  picture_del  Int?    @default(0)
  picture_pass Int?    @default(0)
  service      String? @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yun_log {
  id          Int     @id @default(autoincrement())
  user_id     Int     @default(0)
  app_id      Int     @default(0)
  url         String  @default("") @db.VarChar(255)
  header      String  @default("") @db.VarChar(1024)
  params      String  @default("") @db.VarChar(1024)
  code        Int     @default(0)
  msg         String  @default("") @db.VarChar(128)
  data        String  @db.Text
  create_time Int     @default(0)
  type        Int     @default(1) @db.TinyInt
  deal_status Boolean @default(false)
  deal_res    String  @default("") @db.VarChar(1024)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yun_middle {
  id      Int     @id @default(autoincrement())
  app_key String  @default("0") @db.VarChar(50)
  app_id  String  @default("0") @db.VarChar(20)
  service String  @default("0") @db.VarChar(20)
  type    String? @db.VarChar(10)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yunshangqulv_app {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  channel_id  Int     @default(0) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  app_key     String  @default("") @db.VarChar(50)
  env         Boolean @default(false)
  rule_name   String? @default("") @db.VarChar(50)
  chan_id     String  @default("") @db.VarChar(100)
  is_test     Int     @default(0) @db.UnsignedInt
  c_id        String  @default("") @db.VarChar(100)
  openapi_key String  @default("") @db.VarChar(200)
  app_id      Int     @default(0) @db.UnsignedInt
  created_at  Int     @default(0)
  updated_at  Int     @default(0)

  @@index([app_key], map: "rcloud_yunshangqulv_app_key")
  @@index([user_id], map: "rcloud_yunshangqulv_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yunshangqulv_channel {
  id         Int     @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int     @default(0) @db.UnsignedInt
  rule_name  String? @default("") @db.VarChar(100)
  email      String  @default("") @db.VarChar(100)
  corp_id    String  @default("") @db.VarChar(100)
  channel_id Int     @default(0)
  created_at Int     @default(0)
  updated_at Int     @default(0)

  @@index([user_id], map: "rcloud_yunshangqulv_channel_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yunshangqulv_project {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  app_id      Int     @default(0) @db.UnsignedInt
  user_id     Int     @default(0) @db.UnsignedInt
  app_key     String  @default("") @db.VarChar(50)
  env         Boolean @default(false)
  rule_type   String  @default("") @db.VarChar(20)
  project_id  Int     @default(0) @db.UnsignedInt
  project_pid String  @default("") @db.VarChar(50)
  rule_name   String? @default("") @db.VarChar(100)
  category    Int     @default(3) @db.UnsignedInt
  service     Int     @default(0) @db.UnsignedInt
  secret_key  String  @default("") @db.VarChar(255)
  created_at  Int     @default(0)
  updated_at  Int     @default(0)

  @@index([app_key], map: "rcloud_yunshangqulv_app_key")
  @@index([user_id], map: "rcloud_yunshangqulv_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rcloud_yunshangqulv_statistic {
  id            Int     @id @default(autoincrement())
  user_id       Int     @default(0)
  app_id        Int     @default(0)
  rcloud_app_id Int?    @default(0)
  is_abroad     Boolean @default(false)
  env           Boolean @default(false)
  date          Int     @default(0)
  project_id    String  @db.VarChar(10)
  rule_type     String  @default("") @db.VarChar(20)
  item_id       Int
  item_name     String  @db.VarChar(50)
  value         Int     @default(0)
  pay_value     Int     @default(0)
  money         Decimal @default(0.0000) @db.Decimal(12, 4)
  created_at    Int     @default(0)
  updated_at    Int     @default(0)

  @@index([app_id], map: "app_id")
  @@index([date], map: "date")
  @@index([user_id], map: "user_id")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model redpacke_middle {
  app_key String @db.VarChar(20)
  active  Int
  dredge  String @db.VarChar(5)
  num     Int

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_app {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_pool_id Int    @default(0) @db.UnsignedInt
  uuid         String @unique(map: "service_app_key_uindex") @default("") @db.Char(100)
  secret       String @default("") @db.VarChar(100)
  name         String @default("") @db.VarChar(100)
  description  String @default("") @db.VarChar(1000)
  config       Json
  status       Int    @default(0) @db.TinyInt
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_audit_log {
  id             BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  user_pool_id   Int     @default(0) @db.UnsignedInt
  application_id Int     @default(0) @db.UnsignedInt
  user_id        Int     @default(0) @db.UnsignedInt
  url            String  @default("") @db.VarChar(500)
  ip             String  @default("") @db.VarChar(50)
  action         Int     @default(0) @db.UnsignedTinyInt
  created_at     Int     @default(0) @db.UnsignedInt
  content        String? @db.Text

  @@index([user_id], map: "service_resource_owner_resource_id_index")
  @@index([application_id], map: "service_resource_owner_target_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_policy {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_pool_id Int    @default(0) @db.UnsignedInt
  uuid         String @unique(map: "service_policy_uuid_uindex") @default("") @db.VarChar(100)
  name         String @default("") @db.VarChar(100)
  description  String @default("") @db.VarChar(1000)
  status       Int?   @default(0) @db.UnsignedTinyInt
  config       Json
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_resource {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_pool_id Int    @default(0) @db.UnsignedInt
  parent_id    Int    @default(0)
  uuid         String @default("") @db.VarChar(100)
  namespace    String @default("") @db.VarChar(100)
  code         String @default("") @db.VarChar(100)
  name         String @default("") @db.VarChar(500)
  description  String @default("") @db.VarChar(1000)
  type         Int    @default(0) @db.UnsignedTinyInt
  status       Int?   @default(0) @db.UnsignedTinyInt
  config       Json
  sort         Int    @default(0) @db.UnsignedInt
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int    @default(0) @db.UnsignedInt

  @@index([uuid], map: "service_resource_uuid_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_resource_owner {
  id          Int  @id @default(autoincrement()) @db.UnsignedInt
  resource_id Int  @default(0) @db.UnsignedInt
  target_id   Int  @default(0) @db.UnsignedInt
  target_type Int  @default(0) @db.UnsignedTinyInt
  action      Int  @default(0) @db.UnsignedTinyInt
  status      Int? @default(0) @db.UnsignedTinyInt
  created_at  Int  @default(0) @db.UnsignedInt
  updated_at  Int  @default(0) @db.UnsignedInt
  deleted_at  Int  @default(0) @db.UnsignedInt

  @@index([resource_id], map: "service_resource_owner_resource_id_index")
  @@index([target_id], map: "service_resource_owner_target_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_role {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_pool_id Int    @default(0) @db.UnsignedInt
  uuid         String @default("") @db.VarChar(100)
  name         String @default("") @db.VarChar(100)
  description  String @default("") @db.VarChar(1000)
  status       Int?   @default(0) @db.UnsignedTinyInt
  config       Json
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_type {
  id      Int     @id @db.UnsignedInt
  name    String? @db.VarChar(32)
  finance String? @db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_user {
  id             Int    @id @default(autoincrement()) @db.UnsignedInt
  user_pool_id   Int    @default(0) @db.UnsignedInt
  uuid           String @unique(map: "service_user_uuid_uindex") @default("") @db.VarChar(100)
  username       String @default("") @db.VarChar(100)
  name           String @default("") @db.VarChar(100)
  phone          String @default("") @db.VarChar(50)
  email          String @default("") @db.VarChar(100)
  password       String @default("") @db.VarChar(100)
  origin_user_id String @default("") @db.VarChar(100)
  status         Int?   @default(0) @db.UnsignedTinyInt
  created_at     Int    @default(0) @db.UnsignedInt
  updated_at     Int    @default(0) @db.UnsignedInt
  deleted_at     Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_user_group {
  id           Int    @id @default(autoincrement()) @db.UnsignedInt
  user_pool_id Int    @default(0) @db.UnsignedInt
  uuid         String @unique(map: "service_user_group_uuid_uindex") @default("") @db.VarChar(100)
  name         String @default("") @db.VarChar(100)
  description  String @default("") @db.VarChar(1000)
  status       Int?   @default(0) @db.UnsignedTinyInt
  created_at   Int    @default(0) @db.UnsignedInt
  updated_at   Int    @default(0) @db.UnsignedInt
  deleted_at   Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_user_pool {
  id          Int    @id @default(autoincrement()) @db.UnsignedInt
  uuid        BigInt @unique(map: "service_user_pool_uuid_uindex") @default(0)
  name        String @default("") @db.VarChar(100)
  description String @default("") @db.VarChar(1000)
  status      Int?   @default(0) @db.UnsignedTinyInt
  config      Json
  created_at  Int    @default(0) @db.UnsignedInt
  updated_at  Int    @default(0) @db.UnsignedInt
  deleted_at  Int    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_user_role {
  id         Int  @id @default(autoincrement()) @db.UnsignedInt
  user_id    Int  @default(0) @db.UnsignedInt
  role_id    Int  @default(0) @db.UnsignedInt
  status     Int? @default(0) @db.UnsignedTinyInt
  created_at Int  @default(0) @db.UnsignedInt
  updated_at Int  @default(0) @db.UnsignedInt
  deleted_at Int  @default(0) @db.UnsignedInt

  @@index([user_id], map: "service_user_role_user_id_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model service_user_user_group {
  id            Int  @id @default(autoincrement()) @db.UnsignedInt
  user_id       Int  @default(0) @db.UnsignedInt
  user_group_id Int  @default(0) @db.UnsignedInt
  status        Int? @default(0) @db.UnsignedTinyInt
  created_at    Int  @default(0) @db.UnsignedInt
  updated_at    Int  @default(0) @db.UnsignedInt
  deleted_at    Int  @default(0) @db.UnsignedInt

  @@index([user_id], map: "service_user_user_group_user_id_index")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_01 {
  field1 String @db.VarChar(255)
  field2 Int
  field3 Int
  field4 Int
  field5 Float  @db.Float

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_02 {
  field1 String @db.VarChar(255)
  field2 Int
  field3 Int
  field4 Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_06 {
  field1 String @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_21 {
  field1 String @db.VarChar(255)
  field2 Int
  field3 Int
  field4 Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_acc {
  app_name               String @db.VarChar(255)
  user_id                Int
  app_id                 Int
  app_key                String @db.VarChar(255)
  person_up              Float  @db.Float
  person_server          Float  @db.Float
  person_down            Float  @db.Float
  group_up               Float  @db.Float
  group_server           Float  @db.Float
  group_down             Float  @db.Float
  persons_up             Float  @db.Float
  persons_server         Float  @db.Float
  persons_down           Float  @db.Float
  tempgroup_up           Float  @db.Float
  tempgroup_server       Float  @db.Float
  tempgroup_down         Float  @db.Float
  customerservice_up     Float  @db.Float
  customerservice_server Float  @db.Float
  customerservice_down   Float  @db.Float
  public_up              Float  @db.Float
  public_server          Float  @db.Float
  public_down            Float  @db.Float
  notify_up              Float  @db.Float
  notify_server          Float  @db.Float
  notify_down            Float  @db.Float

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_acc_two {
  app_name               String @db.VarChar(255)
  user_id                Int
  app_id                 Int
  app_key                String @db.VarChar(255)
  person_up              Float  @db.Float
  person_server          Float  @db.Float
  person_down            Float  @db.Float
  group_up               Float  @db.Float
  group_server           Float  @db.Float
  group_down             Float  @db.Float
  persons_up             Float  @db.Float
  persons_server         Float  @db.Float
  persons_down           Float  @db.Float
  tempgroup_up           Float  @db.Float
  tempgroup_server       Float  @db.Float
  tempgroup_down         Float  @db.Float
  customerservice_up     Float  @db.Float
  customerservice_server Float  @db.Float
  customerservice_down   Float  @db.Float
  public_up              Float  @db.Float
  public_server          Float  @db.Float
  public_down            Float  @db.Float
  notify_up              Float  @db.Float
  notify_server          Float  @db.Float
  notify_down            Float  @db.Float
  active                 Float  @db.Float

  @@ignore
}

model temp_redbao {
  id          Int     @id @default(autoincrement())
  appkey      String  @default("") @db.VarChar(16)
  allin       Decimal @db.Decimal(20, 3)
  allout      Decimal @db.Decimal(20, 3)
  allfa       Decimal @db.Decimal(20, 3)
  allcount    Int
  fivefa      Int
  create_time String? @db.VarChar(32)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_t01 {
  tdate                  Int
  app_id                 Int
  app_key                String @db.VarChar(255)
  person_up              Float  @db.Float
  person_server          Float  @db.Float
  person_down            Float  @db.Float
  group_up               Float  @db.Float
  group_server           Float  @db.Float
  group_down             Float  @db.Float
  persons_up             Float  @db.Float
  persons_server         Float  @db.Float
  persons_down           Float  @db.Float
  tempgroup_up           Float  @db.Float
  tempgroup_server       Float  @db.Float
  tempgroup_down         Float  @db.Float
  customerservice_up     Float  @db.Float
  customerservice_server Float  @db.Float
  customerservice_down   Float  @db.Float
  public_up              Float  @db.Float
  public_server          Float  @db.Float
  public_down            Float  @db.Float
  notify_up              Float  @db.Float
  notify_server          Float  @db.Float
  notify_down            Float  @db.Float
  active                 Float  @db.Float
  tonline                Float  @db.Float

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_t02 {
  app_name               String @db.VarChar(255)
  user_id                String @db.VarChar(255)
  tdate                  Int
  app_id                 Int
  app_key                String @db.VarChar(255)
  person_up              Float  @db.Float
  person_server          Float  @db.Float
  person_down            Float  @db.Float
  group_up               Float  @db.Float
  group_server           Float  @db.Float
  group_down             Float  @db.Float
  persons_up             Float  @db.Float
  persons_server         Float  @db.Float
  persons_down           Float  @db.Float
  tempgroup_up           Float  @db.Float
  tempgroup_server       Float  @db.Float
  tempgroup_down         Float  @db.Float
  customerservice_up     Float  @db.Float
  customerservice_server Float  @db.Float
  customerservice_down   Float  @db.Float
  public_up              Float  @db.Float
  public_server          Float  @db.Float
  public_down            Float  @db.Float
  notify_up              Float  @db.Float
  notify_server          Float  @db.Float
  notify_down            Float  @db.Float
  active                 Float  @db.Float
  tonline                Float  @db.Float

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_weekly_newspaper {
  app_id                 Int
  app_key                String @db.VarChar(255)
  person_up              Float  @db.Float
  person_server          Float  @db.Float
  person_down            Float  @db.Float
  group_up               Float  @db.Float
  group_server           Float  @db.Float
  group_down             Float  @db.Float
  persons_up             Float  @db.Float
  persons_server         Float  @db.Float
  persons_down           Float  @db.Float
  tempgroup_up           Float  @db.Float
  tempgroup_server       Float  @db.Float
  tempgroup_down         Float  @db.Float
  customerservice_up     Float  @db.Float
  customerservice_server Float  @db.Float
  customerservice_down   Float  @db.Float
  public_up              Float  @db.Float
  public_server          Float  @db.Float
  public_down            Float  @db.Float
  notify_up              Float  @db.Float
  notify_server          Float  @db.Float
  notify_down            Float  @db.Float

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model test511 {
  appkey String? @db.VarChar(30)
  msgcnt BigInt?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_appbase {
  appId        Int     @db.UnsignedInt
  env          Int     @db.UnsignedTinyInt
  name         String  @default("") @db.VarChar(125)
  user_id      Int     @default(0)
  app_status   String  @default("") @db.VarChar(1)
  category     Int     @default(0)
  sales        String? @db.VarChar(30)
  email        String  @default("") @db.VarChar(150)
  cooperate_id Int     @default(0)

  @@index([user_id], map: "ix_user_id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_appservice {
  appId        Int     @db.UnsignedInt
  service      BigInt  @default(0)
  service_name String? @db.VarChar(45)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_dts {
  id Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_dts2 {
  id Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_dts3 {
  id Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_email_test_index {
  id              Int     @default(0) @db.UnsignedInt
  email           String  @default("") @db.VarChar(100)
  status          Boolean @default(false)
  email_subscribe Boolean @default(false)

  @@index([email], map: "ix_email")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_five {
  app_id Int
  fiv    String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_jicheng {
  appkey      String? @db.VarChar(20)
  create_time String? @db.VarChar(20)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_leiji {
  appkey         String? @db.VarChar(20)
  create_time    String? @db.VarChar(20)
  send_all_money Int?
  send_count     Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_pay {
  uid   Int
  money String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_pay1 {
  appid Int
  pay   String? @db.VarChar(255)
  uid   Int

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_rcloud_user_follow_record {
  id       Int     @id @default(autoincrement())
  type     Boolean
  t_id     Int
  admin_id Int
  user_id  Int
  title    String  @db.VarChar(255)
  content  String  @db.VarChar(255)
  ctime    Int
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_redbao {
  appkey      String? @db.VarChar(12)
  create_time String? @db.VarChar(20)
  allout      Int?
  allmoney    Int?
  cout        Int?
  cmoney      Int?

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_rtc_shumei_charge {
  id          Int      @id @default(autoincrement())
  orderNo     String?  @db.VarChar(50)
  userId      String?  @db.VarChar(50)
  itemId      String?  @db.VarChar(50)
  appId       String?  @db.VarChar(50)
  service     String?  @db.VarChar(50)
  charge      String?  @db.VarChar(50)
  money       Decimal? @db.Decimal(10, 2)
  chargeMoney Decimal? @db.Decimal(10, 2)
  date        Int?
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_seve {
  appkey      String? @db.VarChar(20)
  seven_out   Int?
  seven_count Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_seven {
  appkey String? @db.VarChar(14)
  snum   Int?
  scount Int?

  @@ignore
}

model tmp_user {
  id             Int     @id @default(autoincrement())
  total          String? @db.VarChar(16)
  balance        String? @db.VarChar(16)
  last           String? @db.VarChar(16)
  sms            String? @db.VarChar(16)
  audio          String? @db.VarChar(16)
  consume        String? @db.VarChar(16)
  userId         String? @db.VarChar(16)
  serviceBalance String? @db.VarChar(16)
  type           Int     @default(1)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_user_money {
  user_id         Int
  money           Decimal? @db.Decimal(33, 2)
  add_money_times BigInt   @default(0)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_userapponline {
  userid       Int?
  regdate      DateTime? @db.Date
  name         String?   @db.VarChar(150)
  app_key      String?   @db.VarChar(50)
  apptime      DateTime? @db.DateTime(0)
  cooperate_id Int?      @db.TinyInt
  status       Int?      @db.TinyInt
  onlinetime   DateTime? @db.DateTime(0)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_usermoney {
  user_id         Int
  money           Decimal? @db.Decimal(33, 2)
  add_money_times BigInt   @default(0)

  @@index([user_id], map: "ix_user_id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_vip {
  app_id            Int
  created           Int?
  online            Int?
  hour              Int?
  second            Int?
  roomSize          Float?
  groupSize         Float?
  sizeOld           Float?
  noChatroomSizeOld Float?
  second_all        Int?
  hour_all          Int?
  groupOnline       Int?
  message_server    BigInt?
  chatroom_server   BigInt?
  tally             BigInt?
  item_id           Int?
  appname           String? @db.VarChar(255)
  username          String? @db.VarChar(255)

  @@index([app_id], map: "app_id")
  @@index([created], map: "created")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_cost_detail {
  id              Int      @id @default(autoincrement())
  vendor          Int      @default(0) @db.TinyInt
  user_id         Int      @default(0)
  app_id          Int      @default(0)
  cost_type       Int      @default(0) @db.TinyInt
  amount          Decimal  @default(0.00) @db.Decimal(10, 2)
  cost_count      Decimal  @default(0.00) @db.Decimal(10, 2)
  discount_amount Decimal  @default(0.00) @db.Decimal(10, 2)
  cost_date       DateTime @default(dbgenerated("'0000-00-00'")) @db.Date
  region          String   @default("") @db.VarChar(100)
  create_time     DateTime @default(now()) @db.DateTime(0)
  update_time     DateTime @default(now()) @db.DateTime(0)
  product_type    Int      @default(0) @db.TinyInt

  @@index([app_id], map: "app_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_customer {
  id                  Int      @id @default(autoincrement())
  user_id             Int      @default(0)
  app_key             String   @default("") @db.VarChar(50)
  vendor              Int      @default(0) @db.TinyInt
  estimate_balance    Decimal  @default(0.00) @db.Decimal(10, 2)
  risk_threshold_flag Int      @default(0) @db.TinyInt
  customer_status     Int      @default(0) @db.TinyInt
  create_time         DateTime @default(now()) @db.DateTime(0)
  update_time         DateTime @default(now()) @db.DateTime(0)

  @@index([app_key], map: "app_key")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_customer_domains {
  id            Int       @id @default(autoincrement())
  domain_name   String    @default("") @db.VarChar(100)
  app_key       String    @default("") @db.VarChar(50)
  vendor        Int       @default(0) @db.TinyInt
  product_type  Int       @default(0) @db.TinyInt
  domain_status Int       @default(0) @db.TinyInt
  create_time   DateTime  @default(now()) @db.DateTime(0)
  update_time   DateTime  @default(now()) @db.DateTime(0)
  delete_time   DateTime? @db.Date

  @@index([app_key], map: "idx_appKey")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_customer_product_billing {
  id                          Int      @id @default(autoincrement())
  app_key                     String   @default("") @db.VarChar(50)
  json_string                 String   @default("") @db.VarChar(500)
  start_date                  BigInt   @default(0)
  end_date                    BigInt   @default(0)
  risk_threshold              Int      @default(0)
  shutdown_amount_threshold   Int      @default(0)
  shutdown_duration_threshold Int      @default(0)
  create_time                 DateTime @default(now()) @db.DateTime(0)
  update_time                 DateTime @default(now()) @db.DateTime(0)

  @@index([app_key], map: "app_key")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_flow_statistic {
  id           Int      @id @default(autoincrement())
  vendor       Int      @default(0) @db.TinyInt
  user_id      Int      @default(0)
  app_id       Int      @default(0)
  region       String   @default("") @db.VarChar(100)
  domains      String   @default("") @db.VarChar(5000)
  pull_date    DateTime @db.Date
  flow_value   Decimal  @default(0.00) @db.Decimal(50, 2)
  create_time  DateTime @default(now()) @db.DateTime(0)
  update_time  DateTime @default(now()) @db.DateTime(0)
  product_type Int      @default(0) @db.TinyInt
  pull_time    BigInt

  @@index([app_id], map: "app_id")
  @@index([pull_date], map: "idx_date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_owe_log {
  id          Int      @id @default(autoincrement())
  user_id     Int      @default(0)
  app_key     String   @default("") @db.VarChar(50)
  vendor      Int      @default(0) @db.TinyInt
  owe_status  Int      @default(0) @db.TinyInt
  owe_date    DateTime @default(dbgenerated("'0000-00-00'")) @db.Date
  create_time DateTime @default(now()) @db.DateTime(0)
  update_time DateTime @default(now()) @db.DateTime(0)

  @@index([app_key], map: "app_key")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_peak_statistic {
  id           Int      @id @default(autoincrement())
  vendor       Int      @default(0) @db.TinyInt
  user_id      Int      @default(0)
  app_id       Int      @default(0)
  product_type Int      @default(0) @db.TinyInt
  region       String   @default("") @db.VarChar(100)
  bandwidth    Decimal  @default(0.00) @db.Decimal(50, 2)
  domains      String   @default("") @db.VarChar(5000)
  pull_date    DateTime @default(dbgenerated("'0000-00-00'")) @db.Date
  pull_time    BigInt
  create_time  DateTime @default(now()) @db.DateTime(0)
  update_time  DateTime @default(now()) @db.DateTime(0)

  @@index([app_id], map: "app_id")
  @@index([pull_date], map: "idx_date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_regular_price {
  id             Int       @id @default(autoincrement())
  area           String    @default("") @db.VarChar(100)
  step_range     String    @default("") @db.VarChar(100)
  regular_type   Int       @default(0) @db.TinyInt
  single_price   Decimal?  @db.Decimal(10, 3)
  regular_status Int       @default(0) @db.TinyInt
  create_time    DateTime  @default(now()) @db.DateTime(0)
  update_time    DateTime? @default(now()) @db.DateTime(0)
  product_type   Int       @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tpcloud_cdn_transcoding_statistic {
  id           Int      @id @default(autoincrement())
  vendor       Int      @default(0) @db.TinyInt
  user_id      Int      @default(0)
  app_id       Int      @default(0)
  domains      String   @default("") @db.VarChar(5000)
  pull_date    DateTime @default(dbgenerated("'0000-00-00'")) @db.Date
  minute_value Decimal  @default(0.00) @db.Decimal(10, 2)
  pull_data    String   @default("") @db.VarChar(5000)
  create_time  DateTime @default(now()) @db.DateTime(0)
  update_time  DateTime @default(now()) @db.DateTime(0)
  product_type Int      @default(0) @db.TinyInt
  trans_code   String   @default("") @db.VarChar(100)
  pull_time    BigInt

  @@index([app_id], map: "app_id")
  @@index([pull_date], map: "idx_date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_custome_record {
  id           Int      @id @default(autoincrement())
  order_id     String   @db.VarChar(30)
  money        Decimal  @db.Decimal(11, 2)
  user_id      Int
  admin_id     Int      @default(0)
  audit_id     Int      @default(0)
  type         Int
  account_type Int      @default(1)
  reason       String   @db.Text
  file         String   @db.VarChar(255)
  service_type Int      @default(0)
  remark       String   @db.Text
  status       Int      @default(2) @db.TinyInt
  ctime        Int      @default(0)
  audit_time   Int      @default(0)
  invoice      Boolean  @default(false)
  balance      Decimal? @default(0.00) @db.Decimal(11, 2)
  consum_type  Int?     @default(0)
  app_ids      String?  @default("") @db.VarChar(255)
  start        Int?     @default(0)
  end          Int?     @default(0)
  directional  String?  @default("") @db.VarChar(255)
  sid          Int?     @default(0)
  fill         Int?     @default(0)
  settle       Int?     @default(0)
  cdate        String?  @db.VarChar(32)
  app_id       Int      @default(0)
  source       Int      @default(0)
  invoice_back Boolean  @default(false)

  @@index([user_id], map: "ix_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wx_chat_data {
  id      Int     @id @default(autoincrement())
  seq     BigInt?
  msgid   String? @default("") @db.VarChar(255)
  action  String? @default("") @db.VarChar(255)
  roomid  String? @default("") @db.VarChar(255)
  data    String? @db.LongText
  img     String? @db.LongText
  msgtime BigInt?

  @@index([msgtime], map: "wx_c_msgtime")
  @@index([roomid], map: "wx_c_roomid")
  @@index([seq(sort: Desc)], map: "wx_c_seq")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wx_group {
  id           Int     @id @default(autoincrement())
  chatid       String  @default("") @db.VarChar(255)
  chatname     String? @db.VarChar(255)
  notice       String? @db.Text
  owner        String? @db.VarChar(255)
  member_list  String? @db.LongText
  uid          Int?
  create_time  BigInt?
  new_msg_time BigInt?

  @@index([chatid], map: "wx_g_chatid")
  @@index([new_msg_time(sort: Desc)], map: "wx_g_new_msg_time")
}

model xjp_cc_exclusivelog {
  id          Int       @id @default(autoincrement())
  appId       String    @db.VarChar(45)
  enableTime  DateTime  @default(now()) @db.Timestamp(0)
  status      Boolean   @default(true)
  disableTime DateTime? @db.Timestamp(0)

  @@unique([appId, status], map: "appId_status")
}

model z_android_package {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  server_id    Int?
  package_type String?  @db.VarChar(255)
  package_name String?  @db.VarChar(255)
  app_id       Int?
  env          Boolean?
  user_id      Int?
  cooperate_id Int?
}

enum popc_notice_subscription_template_type {
  announcement
  finance
  product
  activity
  security
  ticket
  other
}

enum popc_notice_template_type {
  announcement
  finance
  product
  activity
  security
  ticket
  other
}

enum popc_notice_inbox_type {
  announcement
  finance
  product
  activity
  security
  ticket
  other
}

// enum rcloud_red_account_old_or_new {
//   // 新用户 @map("新用户")
//   // 老用户 @map("老用户")
// }
